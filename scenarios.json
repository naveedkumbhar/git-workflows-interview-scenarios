[
  {
    "id": "k8s-deployment-breaks-prod-rollback",
    "title": "New Deployment Breaks Production — Fast & Safe Rollback",
    "category": "Kubernetes",
    "subCategory": "Deployment Strategies & Rollbacks",
    "level": "Senior DevOps / SRE",
    "badge": "Incident Recovery",
    "tags": [
      "Kubernetes",
      "Deployment",
      "Rollback",
      "GitOps",
      "ArgoCD",
      "Database Migrations"
    ],
    "summary": "Fast-response production incident runbook for rolling back broken Kubernetes deployments across native kubectl, GitOps (ArgoCD/Flux), and handling database migration traps.",
    "question": "New deployment breaks production — how would you rollback?",
    "roleContext": "SRE Incident Management",
    "intro": "When a new deployment causes production degradation, the priority is mean time to recovery (MTTR). The rollback path depends on whether you run imperative deployments or declarative GitOps.",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "native-rollback",
        "title": "Immediate Imperative Rollback (kubectl rollout undo)",
        "description": "If using native Kubernetes deployments:",
        "items": [
          "<code>kubectl rollout undo deployment/&lt;deployment-name&gt; -n &lt;ns&gt;</code>: Instantly rolls back to the previous revision.",
          "<code>kubectl rollout status deployment/&lt;deployment-name&gt;</code>: Monitor the rollback progress in real-time.",
          "To target a specific revision: <code>kubectl rollout history deployment/&lt;name&gt;</code> followed by <code>kubectl rollout undo deployment/&lt;name&gt; --to-revision=3</code>.",
          "<strong>How it works under the hood:</strong> Kubernetes points the Deployment back to the previous healthy <code>ReplicaSet</code>, scaling it up while scaling down the broken ReplicaSet."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "gitops-rollback",
        "title": "GitOps Rollback (ArgoCD / Flux Reality)",
        "description": "In GitOps, manual kubectl rollouts will be reverted by self-healing:",
        "items": [
          "<strong>The GitOps Trap:</strong> If you run <code>kubectl rollout undo</code> while ArgoCD has <code>auto-sync</code> + <code>self-heal</code> enabled, ArgoCD will detect drift and immediately re-deploy the broken version!",
          "<strong>Proper GitOps Rollback:</strong> Run <code>git revert HEAD &amp;&amp; git push origin main</code> in the manifest repository. ArgoCD syncs and restores the previous commit cleanly.",
          "<strong>Emergency Fast-Path:</strong> In ArgoCD UI/CLI, click <strong>Disable Auto-Sync</strong>, roll back revision, then fix Git repository."
        ]
      },
      {
        "stepNumber": "3️⃣",
        "stepId": "db-migration-trap",
        "title": "The Database Migration Trap",
        "description": "Can code be safely rolled back if database schema migrated?",
        "items": [
          "If the release included destructive database schema migrations (e.g. dropped a column or renamed a table), rolling back application code will crash the previous version because old code expects the old schema.",
          "<strong>The Rule:</strong> Production deployments must follow <strong>Expand and Contract</strong> database migrations (additive changes first, deploy code, cleanup in next release).",
          "If a migration broke backward compatibility: Coordinate with DBAs to apply a compensating forward migration or restore DB snapshot before reverting code."
        ]
      },
      {
        "stepNumber": "4️⃣",
        "stepId": "post-incident",
        "title": "Post-Incident & Blameless Post-Mortem",
        "description": "Preventing the same failure in future releases:",
        "items": [
          "Conduct blameless post-mortem: Why didn't automated CI tests or staging catch this?",
          "Implement <strong>Canary Deployments with Argo Rollouts or Flagger</strong>: Route 5% traffic to canary; automatically abort if 5xx errors spike without human intervention.",
          "Add automated smoke tests and readiness probe validation."
        ]
      }
    ],
    "keyTakeaway": "Know your deployment mechanism: In pure K8s, use 'kubectl rollout undo'; in GitOps (ArgoCD), disable auto-sync or 'git revert' to prevent self-heal fighting. Never do destructive DB migrations in single-step releases.",
    "elevatorPitch": [
      "Immediate action: Run 'kubectl rollout undo deployment/<name>' to revert to previous ReplicaSet.",
      "If GitOps (ArgoCD/Flux): Disable auto-sync immediately or 'git revert HEAD && git push' so self-heal doesn't re-break it.",
      "Database check: Verify if DB migrations ran. If additive, rollback is safe. If destructive, apply compensating migration.",
      "Verify recovery: Monitor 'kubectl rollout status', ALB 5xx metrics, and application logs.",
      "Post-mortem: Implement progressive delivery (Argo Rollouts/Canary) with automatic metric-based rollback."
    ]
  },
  {
    "id": "cicd-pipeline-succeeds-not-deployed",
    "title": "CI/CD Pipeline Succeeds but New Version Isn't Deployed — Debugging",
    "category": "CI/CD",
    "subCategory": "Pipelines & Delivery",
    "level": "Senior DevOps / SRE",
    "badge": "Pipeline Triage",
    "tags": [
      "CI/CD",
      "Docker",
      "Image Tagging",
      "GitOps",
      "Kubernetes",
      "GitHub Actions"
    ],
    "summary": "Diagnostic guide for when CI/CD logs show green checkmarks but the target production environment continues running old code: image tag caching, branch rules, and GitOps sync gaps.",
    "question": "Pipeline succeeds but the new version isn't deployed — how would you debug?",
    "roleContext": "CI/CD & Delivery Engineering",
    "intro": "When a CI/CD pipeline shows green but production is unchanged, the issue lies in artifact immutability, deployment trigger conditions, or GitOps reconciliation gaps.",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "verify-live-version",
        "title": "Verify What is Actually Running in Production",
        "description": "Start by inspecting the live cluster/server before checking pipeline scripts:",
        "items": [
          "Run: <code>kubectl get deployment &lt;app&gt; -o jsonpath='{.spec.template.spec.containers[0].image}'</code>.",
          "Check the image tag and digest. Does it match the newly built Git commit SHA?",
          "Hit the application's version endpoint: <code>curl https://app.example.com/version</code>."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "the-latest-tag-trap",
        "title": "The ':latest' Tag & imagePullPolicy Trap",
        "description": "The single most common root cause in container CI/CD:",
        "items": [
          "If the pipeline pushes <code>myapp:latest</code> and the Kubernetes Deployment manifest says <code>image: myapp:latest</code> with <code>imagePullPolicy: IfNotPresent</code>:",
          "Kubernetes checks if a tag named <code>latest</code> exists locally on the node. If yes, it <strong>never pulls the new image from the registry!</strong>",
          "Furthermore, Kubernetes detects no change in the Deployment manifest (the image string is still <code>myapp:latest</code>), so it triggers <strong>zero rollout!</strong>",
          "<strong>Fix:</strong> Always use immutable image tags based on Git SHA or semantic release (e.g. <code>myapp:sha-7f3a9b2</code>)."
        ]
      },
      {
        "stepNumber": "3️⃣",
        "stepId": "pipeline-step-conditions",
        "title": "Pipeline Step Conditions & Environment Mismatch",
        "description": "Audit pipeline execution steps:",
        "items": [
          "<strong>Skipped Deploy Step:</strong> Did the build/test job succeed, but the deploy job was skipped because of a condition like <code>if: github.ref == 'refs/heads/main'</code> when building a feature branch?",
          "<strong>Target Environment Mismatch:</strong> Did the pipeline deploy to Staging instead of Production due to environment variable configuration?",
          "<strong>Manual Approval Gate:</strong> Is the pipeline waiting on manual approval in GitHub Actions Environments / GitLab Protected Environments?"
        ]
      },
      {
        "stepNumber": "4️⃣",
        "stepId": "gitops-sync-audit",
        "title": "GitOps Manifest Repo & Controller Audit",
        "description": "If using a separate manifest repository (ArgoCD / Flux):",
        "items": [
          "Did the CI pipeline successfully commit and push the updated image tag to the config repo? (Check git credentials and branch protection rules).",
          "Check ArgoCD sync status: Is the application in <code>OutOfSync</code> or <code>Sync Failed</code> state? Is auto-sync paused?",
          "Check for Kubernetes manifest validation failure (e.g. invalid YAML or unaccepted CPU limit)."
        ]
      }
    ],
    "keyTakeaway": "Check the live running image tag first. Avoid mutable ':latest' tags that bypass k8s rollouts. Verify pipeline conditions, approval gates, and GitOps manifest commit chains.",
    "elevatorPitch": [
      "Verify running container image: 'kubectl get deploy <app> -o jsonpath={..image}'.",
      "Check image tagging: Avoid ':latest' with 'imagePullPolicy: IfNotPresent' which ignores new image pushes.",
      "Audit CI logs: Ensure the deploy job actually executed and was not skipped by branch/tag conditions.",
      "Check GitOps repo: Verify CI successfully pushed new image tag commit to the manifest repository.",
      "Check ArgoCD/Flux: Inspect sync status, controller errors, or paused auto-sync."
    ]
  },
  {
    "id": "cicd-enterprise-pipeline-multiple-teams",
    "title": "Design an Enterprise-Grade CI/CD Pipeline for Multiple Microservices Teams",
    "category": "CI/CD",
    "subCategory": "Platform Engineering & Delivery",
    "level": "Staff / Principal SRE",
    "badge": "Enterprise Platform",
    "tags": [
      "CI/CD",
      "GitOps",
      "ArgoCD",
      "Security",
      "SBOM",
      "Cosign",
      "Canary",
      "Testing"
    ],
    "summary": "Full blueprint for an enterprise CI/CD platform supporting autonomous microservices teams: trunk-based CI, immutable container signing with Cosign, GitOps deployment with ArgoCD, and automated canary verification.",
    "question": "Design an enterprise-grade CI/CD pipeline for multiple teams deploying microservices independently. How would you implement automated testing, artifact management, security scanning, approvals, deployment strategies, and rollback?",
    "roleContext": "Platform Engineering & Delivery Architecture",
    "intro": "An enterprise CI/CD system must provide autonomous developer self-service while enforcing strict organizational security, automated progressive delivery, and zero-downtime rollbacks.",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "ci-stage-testing-security",
        "title": "CI Stage: Automated Testing & Security Shift-Left",
        "description": "Triggered on PR and commit using GitHub Actions / GitLab CI:",
        "items": [
          "<strong>Fast Feedback Loop (<10 min):</strong> Parallel execution of unit tests, code linting, and contract tests (using Pact for inter-service API compatibility).",
          "<strong>Static Code Analysis (SAST):</strong> SonarQube / Semgrep enforcing code quality and vulnerability gates.",
          "<strong>Secret Scanning:</strong> TruffleHog / Gitleaks blocking commits containing API keys or private tokens.",
          "<strong>Software Composition Analysis (SCA):</strong> Snyk / Trivy scanning third-party dependencies for known CVEs (CVSS > 7.0 fails build)."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "artifact-management-signing",
        "title": "Artifact Management, SBOM & Cryptographic Signing",
        "description": "Securing the software supply chain from build to registry:",
        "items": [
          "<strong>Immutable Builds:</strong> Multi-stage Dockerfile built using BuildKit / Kaniko, tagged strictly with Git commit SHA (never mutable <code>latest</code>).",
          "<strong>Artifact Storage:</strong> Pushed to Amazon ECR with image tag immutability enabled and automated vulnerability scanning.",
          "<strong>Software Bill of Materials (SBOM):</strong> Generated using <strong>Syft</strong> and attached to the image.",
          "<strong>Cryptographic Signing (Cosign):</strong> Keyless signing via <strong>Sigstore/Cosign</strong> using OIDC identity. The Kubernetes cluster will reject any unsigned image via Kyverno admission policies."
        ]
      },
      {
        "stepNumber": "3️⃣",
        "stepId": "gitops-cd-delivery",
        "title": "CD Stage: Declarative GitOps with ArgoCD",
        "description": "Decoupling continuous integration from continuous deployment:",
        "items": [
          "<strong>Two-Repository Pattern:</strong> Developers push to Application Source Repo; the CI pipeline automatically opens a PR or commits the new image tag to the <strong>Environments/Config GitOps Repo</strong>.",
          "<strong>ArgoCD Orchestration:</strong> ArgoCD controllers continuously poll the GitOps repo and reconcile cluster state.",
          "<strong>Environment Promotion:</strong> Automatic sync to <code>Dev</code> → Automated integration tests → Automatic sync to <code>Staging</code>."
        ]
      },
      {
        "stepNumber": "4️⃣",
        "stepId": "progressive-delivery-rollback",
        "title": "Progressive Delivery (Canary) & Automated Rollback",
        "description": "Safe, zero-downtime deployment to Production:",
        "items": [
          "<strong>Automated Canary (Argo Rollouts / Flagger):</strong> Routes 5% traffic to new version for 10 minutes. Prometheus analyzes real-time error rates (<code>HTTP 5xx &lt; 0.5%</code>) and latency (<code>p99 &lt; 200ms</code>).",
          "Progresses automatically to 25% → 50% → 100% upon passing metric gates.",
          "<strong>Automated Rollback:</strong> If error rates spike, Argo Rollouts automatically aborts and instantly restores 100% traffic to stable pods without waking on-call engineers."
        ]
      }
    ],
    "keyTakeaway": "Decouple CI from CD via GitOps (ArgoCD). Enforce shift-left security (Trivy, Cosign, SBOM), use immutable Git SHA tags, and deploy to production via metric-driven automated canary analysis with instant rollback.",
    "elevatorPitch": [
      "CI Phase: Parallel unit/contract tests, SAST (SonarQube), SCA (Trivy), secret scanning (TruffleHog) in <10 mins.",
      "Artifacts: Immutable image tagged by Git SHA, SBOM generated via Syft, cryptographically signed with Cosign/Sigstore.",
      "CD Phase: Two-repo GitOps pattern with ArgoCD; CI commits updated image tag to environment manifest repo.",
      "Deployment Strategy: Progressive Canary rollout (5% -> 25% -> 100%) via Argo Rollouts with Prometheus metric gates.",
      "Rollback: Automated abort if canary error rate exceeds 0.5%; one-click git revert in GitOps repo."
    ]
  },
  {
    "id": "helm-multi-microservices-structure",
    "title": "Deploying 20 Microservices with Helm — Umbrella vs Independent Charts",
    "category": "Helm & GitOps",
    "subCategory": "Package Management & Architecture",
    "level": "Senior DevOps / Platform Engineer",
    "badge": "Enterprise Helm",
    "tags": [
      "Helm",
      "Microservices",
      "Umbrella Chart",
      "GitOps",
      "ArgoCD",
      "CI/CD"
    ],
    "summary": "Architectural trade-off analysis between Helm Umbrella Charts (one parent chart with 20 subcharts) vs Independent Helm Charts per microservice for deploying 20 microservices in enterprise environments.",
    "question": "Suppose we have 20 microservices. Can we deploy all these services using one Helm deployment? How would you structure a Helm chart for multiple microservices?",
    "roleContext": "Enterprise Packaging & Platform Engineering",
    "intro": "Technically, yes—you can deploy 20 microservices using a single Helm Umbrella Chart with dependencies. However, doing so in Production creates severe blast-radius and deployment bottleneck problems.",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "umbrella-chart-pattern",
        "title": "Approach A: The Umbrella Chart Pattern (Can We?)",
        "description": "How an Umbrella Chart works in Helm:",
        "items": [
          "<strong>Chart.yaml:</strong> Lists all 20 services as subchart dependencies under <code>dependencies:</code> with local file paths (<code>file://../service-a</code>) or chart repository versions.",
          "<strong>values.yaml:</strong> Single root values file overriding child values using namespace blocks (e.g. <code>serviceA.replicaCount: 3</code>, <code>serviceB.resources.limits.cpu: 500m</code>).",
          "<strong>When to use:</strong> Ephemeral PR staging environments, local Minikube/Kind testing, or unified product version bundles."
        ],
        "highlight": "The Trap: If one developer makes a syntax error in Service #19, the entire Helm release fails and rolls back all 20 microservices together! Release velocity drops to the speed of the slowest service."
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "independent-chart-pattern",
        "title": "Approach B: Standard Enterprise Pattern (How We Should Structure)",
        "description": "Independent microservice deployments powered by a shared Common/Library Chart:",
        "pillars": [
          {
            "icon": "📦",
            "name": "Shared Library Chart",
            "detail": "Create a centralized 'microservice-base' library chart defining standardized Deployment, Service, HPA, PDB, and SecurityContext templates."
          },
          {
            "icon": "🚀",
            "name": "Dedicated Service Charts",
            "detail": "Each of the 20 services has its own lightweight chart referencing the library chart, containing only its unique 'values.yaml' and 'Chart.yaml'."
          },
          {
            "icon": "🔄",
            "name": "Independent CI/CD Lifecycles",
            "detail": "Service A can deploy 10 times a day to Production without touching, risking, or triggering rollouts of Services B through T."
          }
        ]
      },
      {
        "stepNumber": "3️⃣",
        "stepId": "gitops-orchestration",
        "title": "Orchestrating 20 Charts with ArgoCD / Helmfile",
        "description": "Managing deployment across all 20 services cleanly without a monolithic umbrella chart:",
        "items": [
          "<strong>ArgoCD ApplicationSet:</strong> Uses a Git directory generator to automatically discover all 20 service charts in Git and deploy each as an isolated ArgoCD Application.",
          "<strong>Helmfile:</strong> Declarative wrapper (`helmfile.yaml`) that specifies release order, environment values, and concurrent execution across all 20 releases."
        ]
      }
    ],
    "keyTakeaway": "Can you deploy 20 microservices with one Helm chart? Yes, via an Umbrella chart with 20 dependencies. Should you in Production? No. The enterprise best practice is a shared Library Chart with 20 independent releases orchestrated via ArgoCD ApplicationSets to eliminate blast radius.",
    "elevatorPitch": [
      "Yes, technically possible via an Umbrella Chart (Chart.yaml dependencies pointing to 20 subcharts).",
      "Why it fails in Production: Massive blast radius, slow releases, merge conflicts in values.yaml, and all-or-nothing rollback failure.",
      "Recommended Architecture: Build a standardized 'base-microservice' Library Chart; each service maintains its own lightweight chart in its repo.",
      "Deploy independently via GitOps (ArgoCD ApplicationSets or Helmfile) so Team A can ship without risking Team B."
    ]
  },
  {
    "id": "helm-multi-env-configuration",
    "title": "Managing Dev, QA, UAT, and Production Environments in Helm",
    "category": "Helm & GitOps",
    "subCategory": "Configuration Management",
    "level": "Senior DevOps / SRE",
    "badge": "Configuration Architecture",
    "tags": [
      "Helm",
      "Environments",
      "values.yaml",
      "Helmfile",
      "ArgoCD",
      "Secrets"
    ],
    "summary": "Standard operating framework for managing multi-environment Kubernetes configurations across Dev, QA, UAT, and Production using values hierarchies, Helmfile, and secure parameter overrides.",
    "question": "How would you manage different configurations for Dev, QA, UAT, and Production using Helm?",
    "roleContext": "Enterprise Helm Architecture",
    "intro": "The golden rule of enterprise Helm configuration is: 'One Chart, Multiple Values'. Never duplicate template manifests across environments. Separate the chart engine from the environment data.",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "values-hierarchy",
        "title": "Layered Values File Strategy",
        "description": "Organize configuration by inheritance and environment overrides:",
        "items": [
          "<code>values.yaml</code>: Default base configuration common across all environments (container ports, health check paths, labels).",
          "<code>values-dev.yaml</code>: Low resource requests (100m CPU), replicaCount: 1, spot nodeSelectors, debug logging.",
          "<code>values-qa.yaml</code>: Mock third-party endpoints, automated integration test secrets.",
          "<code>values-uat.yaml</code>: Production-parity sizing, performance test configs.",
          "<code>values-prod.yaml</code>: Multiple replicas (≥3), strict PDBs, topologySpreadConstraints across 3 AZs, high CPU/memory limits, Datadog/Splunk production logging."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "cli-and-deployment-command",
        "title": "Deployment Command Chain",
        "description": "Helm merges multiple <code>-f</code> flags in order from left to right, with subsequent files overriding earlier ones:",
        "items": [
          "<code>helm upgrade --install payment-service ./charts/payment-service -f values.yaml -f envs/values-prod.yaml --set image.tag=${GIT_SHA}</code>",
          "<strong>Validation Pre-flight:</strong> Always run <code>helm template ... --debug</code> and <code>helm lint</code> in CI to catch syntax and indentation errors before applying."
        ]
      },
      {
        "stepNumber": "3️⃣",
        "stepId": "helmfile-and-gitops",
        "title": "Enterprise Automation: Helmfile or ArgoCD",
        "description": "How senior teams prevent human error in CI pipelines:",
        "items": [
          "<strong>Helmfile:</strong> <code>helmfile -e production apply</code> automatically injects <code>environments/production/values.yaml</code> and verifies state.",
          "<strong>ArgoCD Application per Environment:</strong> GitOps repo structure with folders <code>overlays/dev</code>, <code>overlays/prod</code> pointing to the same Helm chart with different values files."
        ]
      }
    ],
    "keyTakeaway": "Follow the 'One Chart, Environment-Specific Values' principle. Base defaults in values.yaml, environment overrides in values-<env>.yaml merged via `-f` flags, image tags passed dynamically via CI/CD Git SHA, and secret values injected via External Secrets Operator rather than committed to Git.",
    "elevatorPitch": [
      "Maintain a single Helm chart; never duplicate templates per environment.",
      "Layered values: Base values.yaml for common config, layered with values-dev.yaml, values-prod.yaml via '-f values.yaml -f values-prod.yaml'.",
      "Environment differences: Dev uses 1 replica and spot nodes; Prod uses 3+ replicas, multi-AZ topology spread, strict PDBs, and production secrets.",
      "Dynamic parameters: Image tag passed as Git SHA via '--set image.tag=$SHA' in pipeline.",
      "GitOps integration: ArgoCD Application manifests define the target cluster, namespace, and values file per environment."
    ]
  },
  {
    "id": "helm-rollback-mechanism-limits",
    "title": "Helm Rollback — Execution, Internal Mechanics & Database Traps",
    "category": "Helm & GitOps",
    "subCategory": "Release Engineering",
    "level": "Senior DevOps / SRE",
    "badge": "Release Recovery",
    "tags": [
      "Helm",
      "Rollback",
      "helm rollback",
      "Release Secrets",
      "CRD",
      "Database"
    ],
    "summary": "Technical deep dive into 'helm rollback': how Helm stores release history in Kubernetes Secrets, three-way merge patching, and the critical limitations around CRDs and database schema migrations.",
    "question": "How do you perform a Helm rollback?",
    "roleContext": "Release Management & Emergency Recovery",
    "intro": "A Helm rollback is not just 're-running old YAML'. It is a precise historical reconciliation against Helm's release metadata stored inside cluster Secrets.",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "execution-steps",
        "title": "Step 1: Inspect History & Trigger Rollback",
        "description": "Standard CLI operational runbook:",
        "items": [
          "<code>helm history &lt;release-name&gt; -n &lt;namespace&gt;</code>: Lists all previous revisions, dates, statuses (DEPLOYED, FAILED), and descriptions.",
          "Identify the last known healthy revision (e.g. revision 4).",
          "<code>helm rollback &lt;release-name&gt; 4 -n &lt;namespace&gt; --wait --timeout 3m</code>: Rolls back to revision 4 and waits for pods to pass readiness probes."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "under-the-hood-mechanics",
        "title": "Step 2: What Happens Under the Hood?",
        "description": "How Helm executes the rollback internally:",
        "items": [
          "<strong>Release Secrets:</strong> Helm stores every revision as a compressed, base64-encoded Secret in the release namespace (e.g. <code>sh.helm.release.v1.payment-svc.v4</code>).",
          "<strong>Three-Way Merge:</strong> Helm calculates a three-way merge patch between the current live cluster state, the target revision manifest, and the old manifest.",
          "<strong>New Revision Increment:</strong> Rolling back to revision 4 does NOT delete revision 5. It creates <strong>Revision 6</strong> whose content matches Revision 4. History is strictly append-only."
        ]
      },
      {
        "stepNumber": "3️⃣",
        "stepId": "helm-rollback-gotchas",
        "title": "Step 3: Critical Limitations & The Database Trap",
        "description": "What senior engineers know that juniors miss:",
        "items": [
          "<strong>Helm NEVER Touches CRDs:</strong> CustomResourceDefinitions in the <code>crds/</code> directory are never upgraded or rolled back by Helm by design (to protect existing custom resources from deletion).",
          "<strong>The Database Schema Trap:</strong> Helm only rolls back Kubernetes resources (Deployments, ConfigMaps). It cannot roll back a database migration executed by a Helm pre-install job. Application code must be backward-compatible with the schema."
        ]
      }
    ],
    "keyTakeaway": "Helm rollback creates a brand new revision that matches the target historical revision manifest using a 3-way merge against cluster release Secrets. Be aware that Helm never rolls back CRDs or database state.",
    "elevatorPitch": [
      "Check history: 'helm history <release> -n <ns>' to find the last healthy revision number.",
      "Roll back: 'helm rollback <release> <revision_number> --wait'.",
      "Under the hood: Helm reads the compressed release Secret (sh.helm.release.v1.*), computes a 3-way strategic merge patch, and creates a NEW incremented revision.",
      "Limitation 1: Helm does not manage or roll back CRDs.",
      "Limitation 2: Database migrations are not rolled back by Helm; migrations must be backward-compatible."
    ]
  },
  {
    "id": "aws-q041-walk-me-through-building-a-ci-cd-pi",
    "title": "AWS Q41: Walk me through building a CI/CD pipeline for a containerized app using AWS-native services [L2]",
    "category": "AWS",
    "subCategory": "CI/CD on AWS",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "AWS",
      "CI/CD on AWS",
      "L2",
      "Cloud",
      "Infrastructure",
      "Git"
    ],
    "summary": "1. CodeCommit or GitHub — source code repository. Push triggers the pipeline.",
    "question": "Walk me through building a CI/CD pipeline for a containerized app using AWS-native services.",
    "roleContext": "AWS Scenario #41 (L2) · CI/CD on AWS",
    "intro": "\"In our AWS cloud environment, we managed high-traffic microservices where this exact scenario occurred. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "For ECS Blue/Green with CodeDeploy:",
        "items": [
          "<strong>CodeCommit or GitHub</strong> — source code repository. Push triggers the pipeline.",
          "<strong>CodeBuild</strong> — builds the Docker image, runs tests, pushes image to <strong>ECR</strong> (Elastic Container Registry).",
          "<strong>ECR</strong> — stores the Docker image.",
          "<strong>CodeDeploy or ECS Blue/Green</strong> — deploys the new image to ECS/EKS.",
          "<strong>CodePipeline</strong> — orchestrates the full pipeline: Source → Build → Test → Deploy.",
          "CodeDeploy creates a new task set with the new image."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "---",
        "items": [
          "Routes test traffic to it.",
          "After validation, shifts production traffic over.",
          "Terminates old task set.",
          "Supports instant rollback."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: CodeCommit or GitHub — source code repository. Push triggers the pipeline..",
    "elevatorPitch": [
      "CodeCommit or GitHub — source code repository. Push triggers the pipeline.",
      "CodeBuild — builds the Docker image, runs tests, pushes image to ECR (Elastic Container Registry).",
      "ECR — stores the Docker image."
    ]
  },
  {
    "id": "ci-cd-q007-explain-the-difference-between-blue",
    "title": "CI/CD Q7: Explain the difference between blue-green and canary deployments When would you use each [L2]",
    "category": "CI/CD",
    "subCategory": "GitOps & Deployment Strategies",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "CI/CD",
      "GitOps & Deployment Strategies",
      "L2",
      "DevOps",
      "Automation",
      "Pipelines"
    ],
    "summary": "Blue-Green:",
    "question": "Explain the difference between blue-green and canary deployments. When would you use each?",
    "roleContext": "CI/CD Scenario #7 (L2) · GitOps & Deployment Strategies",
    "intro": "\"During a high-stakes release, we hit a similar deployment challenge and resolved it with automated safeguards. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "<strong>Blue-Green:</strong>",
        "items": [
          "Two identical environments: Blue (current live), Green (new version).",
          "Deploy to Green, test it, then flip traffic 100% from Blue to Green at once.",
          "Instant rollback: flip back to Blue.",
          "Requires 2x infrastructure cost during deployment.",
          "Best for: apps where a partial rollout would create incompatibility (DB schema changes), or where you need instant rollback capability.",
          "Roll out to a small percentage of users (1-10%) first."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>Canary:</strong> <strong>When to use each</strong>: Blue-green for infrastructure changes or when you need cleanest rollback. Canary for application changes where you want gradual rollout and real-user testing. ---",
        "items": [
          "Monitor error rates and latency.",
          "Gradually increase percentage (10% → 25% → 50% → 100%).",
          "Rollback: reduce canary percentage to 0%.",
          "Best for: catching production-specific issues that staging missed, feature releases where you want gradual user exposure."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: Two identical environments: Blue (current live), Green (new version)..",
    "elevatorPitch": [
      "Two identical environments: Blue (current live), Green (new version).",
      "Deploy to Green, test it, then flip traffic 100% from Blue to Green at once.",
      "Instant rollback: flip back to Blue."
    ]
  },
  {
    "id": "ci-cd-q008-your-team-practices-trunk-based-dev",
    "title": "CI/CD Q8: Your team practices trunk-based development A long-running feature takes 3 weeks to build How do you keep it out of production [L3]",
    "category": "CI/CD",
    "subCategory": "GitOps & Deployment Strategies",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "CI/CD",
      "GitOps & Deployment Strategies",
      "L3",
      "DevOps",
      "Automation",
      "Pipelines"
    ],
    "summary": "Use feature flags (feature toggles):",
    "question": "Your team practices trunk-based development. A long-running feature takes 3 weeks to build. How do you keep it out of production?",
    "roleContext": "CI/CD Scenario #8 (L3) · GitOps & Deployment Strategies",
    "intro": "\"In enterprise CI/CD, you cannot rely on manual interventions; every rollback and promotion must be declarative. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Use <strong>feature flags</strong> (feature toggles):",
        "items": [
          "Wrap the new feature code in a flag: <code>if (featureFlags.isEnabled(\"new-checkout-flow\")) { ... }</code>.",
          "The code is deployed to production but the feature is OFF by default.",
          "Enable it gradually: for internal users first → beta users → all users.",
          "Rollback is instant — just turn the flag off without redeployment.",
          "Continuous deployment without exposing incomplete features."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Tools: LaunchDarkly, Unleash, AWS AppConfig, or a simple Redis/DynamoDB-backed flag store. Benefits: Downside: flag debt — flags must be cleaned up after feature fully rolls out. Accumulating old flags makes code messy. ---",
        "items": [
          "Separate deployment from release.",
          "Easy A/B testing.",
          "Instant kill switch in production."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: Wrap the new feature code in a flag: if (featureFlags.isEnabled(\"new-checkout-flow\")) { ... }..",
    "elevatorPitch": [
      "Wrap the new feature code in a flag: if (featureFlags.isEnabled(\"new-checkout-flow\")) { ... }.",
      "The code is deployed to production but the feature is OFF by default.",
      "Enable it gradually: for internal users first → beta users → all users."
    ]
  },
  {
    "id": "ci-cd-q009-how-do-you-implement-gitops-with-ar",
    "title": "CI/CD Q9: How do you implement GitOps with ArgoCD for a multi-environment setup (dev staging prod) [L3]",
    "category": "CI/CD",
    "subCategory": "GitOps & Deployment Strategies",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "CI/CD",
      "GitOps & Deployment Strategies",
      "L3",
      "DevOps",
      "Automation",
      "Pipelines"
    ],
    "summary": "Structure: one Git repo for application code, one (or same repo separate path) for Kubernetes manifests/Helm values.",
    "question": "How do you implement GitOps with ArgoCD for a multi-environment setup (dev, staging, prod)?",
    "roleContext": "CI/CD Scenario #9 (L3) · GitOps & Deployment Strategies",
    "intro": "\"In our delivery pipeline supporting multiple engineering squads, pipeline reliability was paramount. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Structure: one Git repo for application code, one (or same repo separate path) for Kubernetes manifests/Helm values.",
        "items": [
          "Create an ArgoCD Application for each environment pointing to the respective environment directory.",
          "Dev: auto-sync ON (deploy on every commit).",
          "Staging: auto-sync ON after CI passes."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "ArgoCD setup: CI pipeline: builds image → pushes to registry → updates image tag in <code>environments/dev/values.yaml</code> via git commit → ArgoCD detects change → deploys to dev. Promotion to staging/prod = PR that updates that environment's values.yaml. ---",
        "items": [
          "Prod: auto-sync OFF. Human approval required. Or auto-sync after staging soak period."
        ],
        "codeBlock": "├── environments/\n│   ├── dev/\n│   │   └── values.yaml   (image tag, replica count, etc.)\n│   ├── staging/\n│   │   └── values.yaml\n│   └── prod/\n│       └── values.yaml\n└── helm-chart/\n    └── ... (base chart)"
      }
    ],
    "keyTakeaway": "Pro-Tip: Create an ArgoCD Application for each environment pointing to the respective environment directory..",
    "elevatorPitch": [
      "Create an ArgoCD Application for each environment pointing to the respective environment directory.",
      "Dev: auto-sync ON (deploy on every commit).",
      "Staging: auto-sync ON after CI passes."
    ]
  },
  {
    "id": "ci-cd-q010-your-team-has-a-monorepo-with-10-se",
    "title": "CI/CD Q10: Your team has a monorepo with 10 services The CI pipeline runs all 10 services tests on every commit How do you optimize this [L2]",
    "category": "CI/CD",
    "subCategory": "GitOps & Deployment Strategies",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "CI/CD",
      "GitOps & Deployment Strategies",
      "L2",
      "DevOps",
      "Automation",
      "Pipelines"
    ],
    "summary": "Use change detection to only build/test what changed:",
    "question": "Your team has a monorepo with 10 services. The CI pipeline runs all 10 services' tests on every commit. How do you optimize this?",
    "roleContext": "CI/CD Scenario #10 (L2) · GitOps & Deployment Strategies",
    "intro": "\"When developers encounter this build or release bottleneck, my first goal is unblocking velocity safely. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Use <strong>change detection</strong> to only build/test what changed: In GitHub Actions: Then: <code>if: steps.changes.outputs.service-a == 'true'</code> — only run service-a jobs if service-a files changed. Tools: Nx (for Node.js monorepos), Bazel (Google's build system, very granular dependency tracking), Turborepo. Also: shared libraries are special — if a shared library changes, all services that depend on it must rebuild/retest. Your dependency graph must be accurate. ---",
        "codeBlock": "- uses: dorny/paths-filter@v2\n  id: changes\n  with:\n    filters: |\n      service-a:\n        - 'services/service-a/**'\n      service-b:\n        - 'services/service-b/**'"
      }
    ],
    "keyTakeaway": "Pro-Tip: Use change detection to only build/test what changed:.",
    "elevatorPitch": [
      "Immediate Triage: Use change detection to only build/test what changed:",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "ci-cd-q011-a-developer-pushed-directly-to-the-",
    "title": "CI/CD Q11: A developer pushed directly to the main branch and broke production How do you prevent this [L2]",
    "category": "CI/CD",
    "subCategory": "GitOps & Deployment Strategies",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "CI/CD",
      "GitOps & Deployment Strategies",
      "L2",
      "DevOps",
      "Automation",
      "Pipelines"
    ],
    "summary": "Branch protection rules (GitHub/GitLab):",
    "question": "A developer pushed directly to the `main` branch and broke production. How do you prevent this?",
    "roleContext": "CI/CD Scenario #11 (L2) · GitOps & Deployment Strategies",
    "intro": "\"During a high-stakes release, we hit a similar deployment challenge and resolved it with automated safeguards. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "<strong>Branch protection rules</strong> (GitHub/GitLab):",
        "items": [
          "<strong>Require pull requests</strong> — no direct pushes to main. All changes must go through a PR.",
          "<strong>Require approvals</strong> — at least 1 (or 2) reviewers must approve before merge.",
          "<strong>Require status checks</strong> — CI must pass before merge is allowed."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Even for small teams: enforce PRs. It takes 10 minutes to set up branch protection and can prevent hours of incident recovery. ---",
        "items": [
          "<strong>Require linear history</strong> — no merge commits allowed, must rebase. Keeps history clean.",
          "<strong>Restrict who can push</strong> — only CI service accounts can push to protected branches."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: Require pull requests — no direct pushes to main. All changes must go through a PR..",
    "elevatorPitch": [
      "Require pull requests — no direct pushes to main. All changes must go through a PR.",
      "Require approvals — at least 1 (or 2) reviewers must approve before merge.",
      "Require status checks — CI must pass before merge is allowed."
    ]
  },
  {
    "id": "ci-cd-q012-your-ci-pipeline-runs-e2e-tests-aga",
    "title": "CI/CD Q12: Your CI pipeline runs E2E tests against a shared staging environment Multiple branches run tests simultaneously and they interfere with each other How do you fix this [L3]",
    "category": "CI/CD",
    "subCategory": "GitOps & Deployment Strategies",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "CI/CD",
      "GitOps & Deployment Strategies",
      "L3",
      "DevOps",
      "Automation",
      "Pipelines"
    ],
    "summary": "Ephemeral environments — create a new environment for each PR/branch, run tests, then tear it down.",
    "question": "Your CI pipeline runs E2E tests against a shared staging environment. Multiple branches run tests simultaneously and they interfere with each other. How do you fix this?",
    "roleContext": "CI/CD Scenario #12 (L3) · GitOps & Deployment Strategies",
    "intro": "\"In enterprise CI/CD, you cannot rely on manual interventions; every rollback and promotion must be declarative. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "<strong>Ephemeral environments</strong> — create a new environment for each PR/branch, run tests, then tear it down.",
        "items": [
          "<strong>Namespace-per-PR in Kubernetes</strong> — each PR creates a new K8s namespace with all services deployed. Delete namespace when PR closes.",
          "<strong>Review Apps in GitLab</strong> — built-in feature. GitLab creates/destroys environments per PR.",
          "<strong>Terraform workspaces</strong> — create infra per environment, destroy after."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Approaches: The cost is slightly higher infra usage, but test reliability is 100x better because there's no shared state pollution. --- ## 🟢 Jenkins & Pipeline Configuration ---",
        "items": [
          "<strong>Database isolation</strong> — each ephemeral env gets its own DB schema or test database."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: Namespace-per-PR in Kubernetes — each PR creates a new K8s namespace with all services deployed. Delete namespace when PR closes..",
    "elevatorPitch": [
      "Namespace-per-PR in Kubernetes — each PR creates a new K8s namespace with all services deployed. ...",
      "Review Apps in GitLab — built-in feature. GitLab creates/destroys environments per PR.",
      "Terraform workspaces — create infra per environment, destroy after."
    ]
  },
  {
    "id": "ci-cd-q016-your-github-actions-workflow-is-run",
    "title": "CI/CD Q16: Your GitHub Actions workflow is running expensive jobs on every push to every branch running up costs How do you optimize [L2]",
    "category": "CI/CD",
    "subCategory": "GitHub Actions",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "CI/CD",
      "GitHub Actions",
      "L2",
      "DevOps",
      "Automation",
      "Pipelines"
    ],
    "summary": "Use conditional triggers and filters:",
    "question": "Your GitHub Actions workflow is running expensive jobs on every push to every branch, running up costs. How do you optimize?",
    "roleContext": "CI/CD Scenario #16 (L2) · GitHub Actions",
    "intro": "\"In enterprise CI/CD, you cannot rely on manual interventions; every rollback and promotion must be declarative. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Use <strong>conditional triggers and filters</strong>:",
        "items": [
          "Use <code>concurrency</code> to cancel in-progress runs when new commit comes:",
          "Cache dependencies aggressively (<code>actions/cache</code>).",
          "Use <code>if:</code> conditions on jobs — skip expensive tests on doc-only changes."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Also: ---",
        "items": [
          "Self-hosted runners for heavy builds (cheaper than GitHub-hosted for large teams)."
        ],
        "codeBlock": "on:\n  push:\n    branches: [main, release/*]  # only specific branches\n    paths:\n      - 'src/**'         # only when source files change\n      - 'package.json'   # or dependencies\n\n  pull_request:\n    types: [opened, synchronize]  # not all PR events"
      }
    ],
    "keyTakeaway": "Pro-Tip: Use concurrency to cancel in-progress runs when new commit comes:.",
    "elevatorPitch": [
      "Use concurrency to cancel in-progress runs when new commit comes:",
      "Cache dependencies aggressively (actions/cache).",
      "Use if: conditions on jobs — skip expensive tests on doc-only changes."
    ]
  },
  {
    "id": "ci-cd-q017-how-do-you-securely-pass-secrets-to",
    "title": "CI/CD Q17: How do you securely pass secrets to a GitHub Actions workflow without hardcoding them [L2]",
    "category": "CI/CD",
    "subCategory": "GitHub Actions",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "CI/CD",
      "GitHub Actions",
      "L2",
      "DevOps",
      "Automation",
      "Pipelines"
    ],
    "summary": "1. GitHub Secrets — go to repo Settings → Secrets → add secrets. Reference in workflow as ${{ secrets.MY_SECRET }}. Never printed in logs.",
    "question": "How do you securely pass secrets to a GitHub Actions workflow without hardcoding them?",
    "roleContext": "CI/CD Scenario #17 (L2) · GitHub Actions",
    "intro": "\"In our delivery pipeline supporting multiple engineering squads, pipeline reliability was paramount. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "No access keys stored. The IAM role trusts GitHub Actions via OIDC. Most secure approach.",
        "items": [
          "<strong>GitHub Secrets</strong> — go to repo Settings → Secrets → add secrets. Reference in workflow as <code>${{ secrets.MY_SECRET }}</code>. Never printed in logs.",
          "<strong>GitHub Environment Secrets</strong> — scope secrets to specific environments (production, staging). Require environment protection rules (manual approval before accessing prod secrets).",
          "<strong>OIDC with AWS/GCP</strong> — instead of storing cloud credentials as secrets, use GitHub's OIDC provider to get short-lived credentials:"
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "---",
        "codeBlock": "- uses: aws-actions/configure-aws-credentials@v2\n  with:\n    role-to-assume: arn:aws:iam::123456789:role/github-actions-role\n    aws-region: us-east-1"
      }
    ],
    "keyTakeaway": "Pro-Tip: GitHub Secrets — go to repo Settings → Secrets → add secrets. Reference in workflow as ${{ secrets.MY_SECRET }}. Never printed in .",
    "elevatorPitch": [
      "GitHub Secrets — go to repo Settings → Secrets → add secrets. Reference in workflow as ${{ secret...",
      "GitHub Environment Secrets — scope secrets to specific environments (production, staging). Requir...",
      "OIDC with AWS/GCP — instead of storing cloud credentials as secrets, use GitHub's OIDC provider t..."
    ]
  },
  {
    "id": "ci-cd-q018-you-need-to-build-a-reusable-ci-cd-",
    "title": "CI/CD Q18: You need to build a reusable CI/CD workflow that can be used by 50 different repositories in your GitHub organization How do you structure this [L3]",
    "category": "CI/CD",
    "subCategory": "GitHub Actions",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "CI/CD",
      "GitHub Actions",
      "L3",
      "DevOps",
      "Automation",
      "Pipelines"
    ],
    "summary": "Use reusable workflows (.github/workflows/ in a central repository):",
    "question": "You need to build a reusable CI/CD workflow that can be used by 50 different repositories in your GitHub organization. How do you structure this?",
    "roleContext": "CI/CD Scenario #18 (L3) · GitHub Actions",
    "intro": "\"When developers encounter this build or release bottleneck, my first goal is unblocking velocity safely. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Use <strong>reusable workflows</strong> (<code>.github/workflows/</code> in a central repository): Central repo (<code>.github/workflows/build-and-push.yml</code>): Consumer repo: Benefits: one place to update the pipeline, all repos get the update. Version it with tags (<code>@v1</code>, <code>@v2</code>) for stability. --- ## 🔵 GitLab CI ---",
        "codeBlock": "on:\n  workflow_call:\n    inputs:\n      image-name:\n        required: true\n        type: string\n    secrets:\n      REGISTRY_TOKEN:\n        required: true"
      }
    ],
    "keyTakeaway": "Pro-Tip: Use reusable workflows (.github/workflows/ in a central repository):.",
    "elevatorPitch": [
      "Immediate Triage: Use reusable workflows (.github/workflows/ in a central repository):",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "ci-cd-q019-your-gitlab-ci-pipeline-has-a-job-t",
    "title": "CI/CD Q19: Your GitLab CI pipeline has a job that needs to run only when a specific file is changed How do you configure this [L2]",
    "category": "CI/CD",
    "subCategory": "GitLab CI",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "CI/CD",
      "GitLab CI",
      "L2",
      "DevOps",
      "Automation",
      "Pipelines"
    ],
    "summary": "Use rules with changes:",
    "question": "Your GitLab CI pipeline has a job that needs to run only when a specific file is changed. How do you configure this?",
    "roleContext": "CI/CD Scenario #19 (L2) · GitLab CI",
    "intro": "\"During a high-stakes release, we hit a similar deployment challenge and resolved it with automated safeguards. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Use <code>rules</code> with <code>changes</code>: This job only runs when files under <code>frontend/</code> or <code>package.json</code> change in the commit. For more complex conditions: ---",
        "codeBlock": "deploy-frontend:\n  rules:\n    - changes:\n        - frontend/**\n        - package.json\n  script:\n    - npm run build\n    - npm run deploy"
      }
    ],
    "keyTakeaway": "Pro-Tip: Use rules with changes:.",
    "elevatorPitch": [
      "Immediate Triage: Use rules with changes:",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "ci-cd-q020-a-gitlab-runner-is-picking-up-jobs-",
    "title": "CI/CD Q20: A GitLab runner is picking up jobs but theyre running much slower than expected What do you investigate [L2]",
    "category": "CI/CD",
    "subCategory": "GitLab CI",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "CI/CD",
      "GitLab CI",
      "L2",
      "DevOps",
      "Automation",
      "Pipelines"
    ],
    "summary": "1. Runner resources — check CPU/memory on the runner machine. Is it overloaded with too many concurrent jobs?",
    "question": "A GitLab runner is picking up jobs but they're running much slower than expected. What do you investigate?",
    "roleContext": "CI/CD Scenario #20 (L2) · GitLab CI",
    "intro": "\"In enterprise CI/CD, you cannot rely on manual interventions; every rollback and promotion must be declarative. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "---",
        "items": [
          "<strong>Runner resources</strong> — check CPU/memory on the runner machine. Is it overloaded with too many concurrent jobs?",
          "<strong>Concurrent job limit</strong> — in runner config, <code>concurrent</code> setting limits how many jobs run simultaneously on one runner. If set to 10 on a 2-CPU machine, jobs compete for CPU.",
          "<strong>Network</strong> — runner pulling Docker images from a slow registry. Add a local registry cache.",
          "<strong>No caching</strong> — dependencies being reinstalled every run. Configure GitLab CI caching."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "## 🟠 Docker in CI/CD ---",
        "items": [
          "<strong>Executor type</strong> — shell executor vs Docker executor vs Kubernetes. Docker adds overhead for image pull.",
          "<strong>Shared runner congestion</strong> — if using GitLab.com shared runners, they're shared across millions of users. Register your own dedicated runner."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: Runner resources — check CPU/memory on the runner machine. Is it overloaded with too many concurrent jobs?.",
    "elevatorPitch": [
      "Runner resources — check CPU/memory on the runner machine. Is it overloaded with too many concurr...",
      "Concurrent job limit — in runner config, concurrent setting limits how many jobs run simultaneous...",
      "Network — runner pulling Docker images from a slow registry. Add a local registry cache."
    ]
  },
  {
    "id": "docker-q017-you-need-to-pass-a-github-token-to-",
    "title": "Docker Q17: You need to pass a GitHub token to npm install during Docker build without it ending up in the image How [L3]",
    "category": "Docker",
    "subCategory": "Docker",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Docker",
      "Docker",
      "L3",
      "Containers",
      "Linux",
      "Git"
    ],
    "summary": "BuildKit secret mounts:",
    "question": "You need to pass a GitHub token to `npm install` during Docker build without it ending up in the image. How?",
    "roleContext": "Docker Scenario #17 (L3) · Docker",
    "intro": "\"When containerizing our microservices stack, container lifecycle and resource management were critical. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "BuildKit secret mounts: Build: <code>docker build --secret id=github_token,src=~/.github_token .</code> The secret is available only during that RUN step and NOT stored in any layer.",
        "codeBlock": "RUN --mount=type=secret,id=github_token \\\n    export GITHUB_TOKEN=$(cat /run/secrets/github_token) && \\\n    npm install"
      }
    ],
    "keyTakeaway": "Pro-Tip: BuildKit secret mounts:.",
    "elevatorPitch": [
      "Immediate Triage: BuildKit secret mounts:",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "docker-q029-what-is-the-purpose-of-dockerignore",
    "title": "Docker Q29: What is the purpose of dockerignore [L2]",
    "category": "Docker",
    "subCategory": "Docker",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Docker",
      "Docker",
      "L2",
      "Containers",
      "Linux",
      "Git"
    ],
    "summary": "Excludes files from the Docker build context. Smaller context = faster builds. Prevents accidentally copying secrets, .git, node_modules ...",
    "question": "What is the purpose of `.dockerignore`?",
    "roleContext": "Docker Scenario #29 (L2) · Docker",
    "intro": "\"When containerizing our microservices stack, container lifecycle and resource management were critical. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Excludes files from the Docker build context. Smaller context = faster builds. Prevents accidentally copying secrets, <code>.git</code>, <code>node_modules</code> into the image."
      }
    ],
    "keyTakeaway": "Pro-Tip: Excludes files from the Docker build context. Smaller context = faster builds. Prevents accidentally copying secrets, .git, node_m.",
    "elevatorPitch": [
      "Immediate Triage: Excludes files from the Docker build context. Smaller context = faster builds. Prevents acciden",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "docker-q058-how-do-you-run-integration-tests-in",
    "title": "Docker Q58: How do you run integration tests in CI that require real external services (Redis Kafka) using Docker [L3]",
    "category": "Docker",
    "subCategory": "Docker",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Docker",
      "Docker",
      "L3",
      "Containers",
      "Linux",
      "Git"
    ],
    "summary": "Docker Compose in CI (all services started via compose). Or CI service containers (GitHub Actions services, GitLab CI services). Or testc...",
    "question": "How do you run integration tests in CI that require real external services (Redis, Kafka) using Docker?",
    "roleContext": "Docker Scenario #58 (L3) · Docker",
    "intro": "\"In an interview, I explain how we diagnosed container runtime failures without guessing. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Docker Compose in CI (all services started via compose). Or CI service containers (GitHub Actions services, GitLab CI services). Or testcontainers — a library that programmatically starts Docker containers from test code. Tests spin up the exact services they need, test runs, containers are destroyed."
      }
    ],
    "keyTakeaway": "Pro-Tip: Docker Compose in CI (all services started via compose). Or CI service containers (GitHub Actions services, GitLab CI services). O.",
    "elevatorPitch": [
      "Immediate Triage: Docker Compose in CI (all services started via compose). Or CI service containers (GitHub Actio",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "docker-q069-what-happens-if-your-ci-pipeline-re",
    "title": "Docker Q69: What happens if your CI pipeline repeatedly builds the exact same Dockerfile using the latest tag and pushes it to an AWS ECR registry every day for a year [L1]",
    "category": "Docker",
    "subCategory": "Docker",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Docker",
      "Docker",
      "L1",
      "Containers",
      "Linux",
      "Git"
    ],
    "summary": "Using the latest tag makes it a mutable tag. ",
    "question": "What happens if your CI pipeline repeatedly builds the exact same Dockerfile using the `latest` tag and pushes it to an AWS ECR registry every day for a year?",
    "roleContext": "Docker Scenario #69 (L1) · Docker",
    "intro": "\"When containerizing our microservices stack, container lifecycle and resource management were critical. The interviewer is testing: Tag mutability, dangling references, registry bloat.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Using the <code>latest</code> tag makes it a <strong>mutable tag</strong>. Every time the CI pipeline pushes, AWS ECR will overwrite the <code>latest</code> tag to point to the brand new image manifest. The older images from previous days will lose their tag and become <strong>Untagged</strong> (Dangling) images in the registry. If no Lifecycle Policy is configured to garbage-collect untagged images, you will accumulate 365 orphaned 1GB images, paying AWS for useless storage bloat. (Also, deploying <code>latest</code> in Kubernetes is dangerous as it breaks rollback determinism). Always use Git SHAs or Semantic Versioning tags. ---"
      }
    ],
    "keyTakeaway": "Pro-Tip: Using the latest tag makes it a mutable tag..",
    "elevatorPitch": [
      "Immediate Triage: Using the latest tag makes it a mutable tag. ",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "docker-q079-youve-developed-an-internal-tool-sp",
    "title": "Docker Q79: Youve developed an internal tool specifically for your SRE team using Python Due to compliance you must heavily sign all your Docker images cryptographically to prove they originated exclusively from your exact CI/CD server before production will run them What Docker technology enforces this [L3]",
    "category": "Docker",
    "subCategory": "Must enable BuildKit",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Docker",
      "Must enable BuildKit",
      "L3",
      "Containers",
      "Linux",
      "Git"
    ],
    "summary": "This is historically managed by Docker Content Trust (DCT), effectively backed by the Notary service. By enabling export DOCKER_CONTENT_T...",
    "question": "You've developed an internal tool specifically for your SRE team using Python. Due to compliance, you must heavily sign all your Docker images cryptographically to prove they originated exclusively from your exact CI/CD server before production will run them. What Docker technology enforces this?",
    "roleContext": "Docker Scenario #79 (L3) · Must enable BuildKit",
    "intro": "\"During an image optimization initiative across our services, we solved this exact problem. The interviewer is testing: Docker Trust, Notary, sigstore/cosign.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "This is historically managed by <strong>Docker Content Trust (DCT)</strong>, effectively backed by the Notary service. By enabling <code>export DOCKER_CONTENT_TRUST=1</code>, the Docker client cryptographically signs the image manifest using private keys before pushing. Production nodes strictly configured with DCT enabled will adamantly refuse to pull or run images missing signatures from trusted cryptographic publishers. Modern approaches strongly lean towards utilizing <strong>Sigstore/Cosign</strong>, which enables keyless signing tied to strict OIDC identities (like GitHub Actions workflows) to sign images seamlessly and generate indisputable transparency logs. ---"
      }
    ],
    "keyTakeaway": "Pro-Tip: This is historically managed by Docker Content Trust (DCT), effectively backed by the Notary service. By enabling export DOCKER_CO.",
    "elevatorPitch": [
      "Immediate Triage: This is historically managed by Docker Content Trust (DCT), effectively backed by the Notary se",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "docker-q089-your-ci-pipeline-runs-dockerized-bu",
    "title": "Docker Q89: Your CI pipeline runs Dockerized build jobs that themselves need to build Docker images (Docker-in-Docker) The team currently mounts the host Docker socket (/var/run/dockersock) The security team rejects this What are the alternatives [L3]",
    "category": "Docker",
    "subCategory": "Must enable BuildKit",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Docker",
      "Must enable BuildKit",
      "L3",
      "Containers",
      "Linux",
      "Git"
    ],
    "summary": "Mounting the Docker socket gives the inner container full root-equivalent access to the host. Secure alternatives:",
    "question": "Your CI pipeline runs Dockerized build jobs that themselves need to build Docker images (Docker-in-Docker). The team currently mounts the host Docker socket (`/var/run/docker.sock`). The security team rejects this. What are the alternatives?",
    "roleContext": "Docker Scenario #89 (L3) · Must enable BuildKit",
    "intro": "\"When containerizing our microservices stack, container lifecycle and resource management were critical. The interviewer is testing: Docker-in-Docker alternatives, Kaniko, Buildah.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Mounting the Docker socket gives the inner container full root-equivalent access to the host. Secure alternatives:",
        "items": [
          "<strong>Kaniko</strong> — Google's tool that builds container images from a Dockerfile *inside* a container without requiring a Docker daemon. It executes each Dockerfile command in userspace, produces an OCI image, and pushes directly to a registry. Runs unprivileged. Ideal for Kubernetes-based CI (Tekton, GitLab Runner).",
          "<strong>Buildah</strong> — Builds OCI images without a daemon. Can run rootless. Supports Dockerfile syntax and its own native commands.",
          "<strong>Docker-in-Docker (dind)</strong> — Run a full Docker daemon inside a privileged container. More secure than socket mounting (isolated daemon), but still requires <code>--privileged</code>. Use only when Kaniko/Buildah can't satisfy the use case."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "---",
        "items": [
          "<strong>BuildKit with remote builder</strong> — Run BuildKit as a separate service and point <code>docker buildx</code> at it remotely: <code>docker buildx create --driver remote --name mybuilder tcp://buildkit:1234</code>."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: Kaniko — Google's tool that builds container images from a Dockerfile *inside* a container without requiring a Docker daemon. It e.",
    "elevatorPitch": [
      "Kaniko — Google's tool that builds container images from a Dockerfile *inside* a container withou...",
      "Buildah — Builds OCI images without a daemon. Can run rootless. Supports Dockerfile syntax and it...",
      "Docker-in-Docker (dind) — Run a full Docker daemon inside a privileged container. More secure tha..."
    ]
  },
  {
    "id": "docker-q093-a-developer-has-a-project-with-a-10",
    "title": "Docker Q93: A developer has a project with a 10GB data/ directory containing training datasets Every docker build takes 15 minutes before even executing the first Dockerfile instruction The Dockerfile doesnt reference the data/ directory at all Why is it so slow [L2]",
    "category": "Docker",
    "subCategory": "Must enable BuildKit",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Docker",
      "Must enable BuildKit",
      "L2",
      "Containers",
      "Linux",
      "Git"
    ],
    "summary": "Before executing any Dockerfile instruction, Docker packages the entire build context (the directory passed to docker build) and transfer...",
    "question": "A developer has a project with a 10GB `data/` directory containing training datasets. Every `docker build` takes 15 minutes before even executing the first Dockerfile instruction. The Dockerfile doesn't reference the `data/` directory at all. Why is it so slow?",
    "roleContext": "Docker Scenario #93 (L2) · Must enable BuildKit",
    "intro": "\"When containerizing our microservices stack, container lifecycle and resource management were critical. The interviewer is testing: Docker build context transfer, `.dockerignore`.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Before executing any Dockerfile instruction, Docker packages the entire <strong>build context</strong> (the directory passed to <code>docker build</code>) and transfers it to the Docker daemon. If the <code>data/</code> directory is inside the build context, Docker transfers all 10GB every single build — even though no <code>COPY</code> or <code>ADD</code> references it. The \"Sending build context to Docker daemon... 10GB\" message in the build output confirms this. *Fix:* Add <code>data/</code> to <code>.dockerignore</code>: This reduces the build context to only the files the Dockerfile actually needs. Alternatively, restructure the project so the Dockerfile lives in a subdirectory without the data, or use BuildKit's ability to specify individual files via <code>--build-context</code>. ---",
        "codeBlock": "data/\n*.csv\n*.parquet\n__pycache__/\n.git/"
      }
    ],
    "keyTakeaway": "Pro-Tip: Before executing any Dockerfile instruction, Docker packages the entire build context (the directory passed to docker build) and t.",
    "elevatorPitch": [
      "Immediate Triage: Before executing any Dockerfile instruction, Docker packages the entire build context (the dire",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "git-q001-you-started-coding-directly-on-main",
    "title": "Git Q1: You started coding directly on main and made three commits You realize this work belongs on a feature branch and you havent pushed yet How do you move those commits to a new branch and clean up main [L1]",
    "category": "Git",
    "subCategory": "Branching, Merging & Conflicts",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "Branching, Merging & Conflicts",
      "L1",
      "Version Control",
      "Collaboration"
    ],
    "summary": "A branch in Git is just a pointer to a commit, so \"moving\" commits is really just moving pointers around.",
    "question": "You started coding directly on `main` and made three commits. You realize this work belongs on a feature branch and you haven't pushed yet. How do you move those commits to a new branch and clean up `main`?",
    "roleContext": "Git Scenario #1 (L1) · Branching, Merging & Conflicts",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Basic branch mechanics, understanding that branches are just movable pointers to commits.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "A branch in Git is just a pointer to a commit, so \"moving\" commits is really just moving pointers around.",
        "items": [
          "<strong>Create the feature branch from where you are right now</strong> — this captures your three commits on the new branch:",
          "<strong>Reset <code>main</code> back to where the remote is</strong> — your local <code>main</code> still points at the third commit; rewind it:",
          "<strong>Switch to the feature branch and continue:</strong>"
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "The order matters: create the new branch *before* you reset, otherwise the commits become unreachable from any branch (they'd still be in <code>reflog</code> for ~90 days, but don't rely on that). Since you never pushed, no one else is affected. ---",
        "codeBlock": "git branch feature/my-work"
      }
    ],
    "keyTakeaway": "Pro-Tip: Create the feature branch from where you are right now — this captures your three commits on the new branch:.",
    "elevatorPitch": [
      "Create the feature branch from where you are right now — this captures your three commits on the ...",
      "Reset main back to where the remote is — your local main still points at the third commit; rewind...",
      "Switch to the feature branch and continue:"
    ]
  },
  {
    "id": "git-q002-what-is-the-difference-between-git-",
    "title": "Git Q2: What is the difference between git clone and git fork and when would you use each in practice [L1]",
    "category": "Git",
    "subCategory": "Branching, Merging & Conflicts",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "Branching, Merging & Conflicts",
      "L1",
      "Version Control",
      "Collaboration"
    ],
    "summary": "git clone is a Git command — git fork is not a Git command, it's a GitHub/GitLab feature.",
    "question": "What is the difference between `git clone` and `git fork`, and when would you use each in practice?",
    "roleContext": "Git Scenario #2 (L1) · Branching, Merging & Conflicts",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Understanding of remote workflows and contribution models.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "<code>git clone</code> is a Git command — <code>git fork</code> is <strong>not</strong> a Git command, it's a GitHub/GitLab feature.",
        "items": [
          "<strong>Clone</strong> (<code>git clone <url></code>) — downloads a copy of a repo to your laptop. You get the full history and a remote called <code>origin</code> pointing back to wherever you cloned from. Use it when you have push access to the repo.",
          "<strong>Fork</strong> — creates a server-side copy of the repo under your own account on GitHub/GitLab. Then you clone *your fork* locally. Use it when you don't have push access to the original (typical for open-source contributions)."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "The contribution flow for an open-source project is: fork on GitHub → clone your fork → add the original repo as <code>upstream</code> → push branches to your fork → open a PR from your fork to the original. For an internal company repo where everyone has write access, just clone directly and push branches. ---"
      }
    ],
    "keyTakeaway": "Pro-Tip: Clone (git clone ) — downloads a copy of a repo to your laptop. You get the full history and a remote called origin pointing back .",
    "elevatorPitch": [
      "Clone (git clone ) — downloads a copy of a repo to your laptop. You get the full history and a re...",
      "Fork — creates a server-side copy of the repo under your own account on GitHub/GitLab. Then you c..."
    ]
  },
  {
    "id": "git-q003-you-ran-git-merge-and-git-stopped-w",
    "title": "Git Q3: You ran git merge and Git stopped with conflicts in three files Walk me through exactly how you resolve them and explain what the conflict markers mean [L2]",
    "category": "Git",
    "subCategory": "Branching, Merging & Conflicts",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "Branching, Merging & Conflicts",
      "L2",
      "Version Control",
      "Collaboration",
      "Terraform State"
    ],
    "summary": "Git stopped because it couldn't auto-merge — both branches changed the same lines.",
    "question": "You ran `git merge` and Git stopped with conflicts in three files. Walk me through exactly how you resolve them, and explain what the conflict markers mean.",
    "roleContext": "Git Scenario #3 (L2) · Branching, Merging & Conflicts",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: Practical conflict resolution, understanding of three-way merge.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Git stopped because it couldn't auto-merge — both branches changed the same lines.",
        "items": [
          "<strong>See what's conflicted:</strong>",
          "<strong>Open each file.</strong> You'll see markers like:",
          "<strong>Edit the file</strong> to the correct final state and remove all <code><<<</code>, <code>===</code>, <code>>>></code> markers. Don't just pick a side blindly — re-read both intents."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<code>HEAD</code> is what's on the branch you're merging *into*. The lines below <code>=======</code> are from the branch you're merging *in*. The <code>|||||||</code> (if <code>merge.conflictstyle = diff3</code> is set) shows the common ancestor — extremely useful for understanding intent. For repeated conflicts on the same hunk in long-lived branches, enable <code>git rerere</code> so Git remembers your resolution next time. ---",
        "items": [
          "<strong>Mark resolved and finish:</strong>",
          "If you panic, <code>git merge --abort</code> puts you back where you started."
        ],
        "codeBlock": "git status   # files marked \"both modified\""
      }
    ],
    "keyTakeaway": "Pro-Tip: See what's conflicted:.",
    "elevatorPitch": [
      "See what's conflicted:",
      "Open each file. You'll see markers like:",
      "Edit the file to the correct final state and remove all , ===, >>> markers. Don't just pick a sid..."
    ]
  },
  {
    "id": "git-q004-when-should-you-use-git-rebase-inst",
    "title": "Git Q4: When should you use git rebase instead of git merge and what is the one rule you must never break [L2]",
    "category": "Git",
    "subCategory": "Branching, Merging & Conflicts",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "Branching, Merging & Conflicts",
      "L2",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "Both integrate changes from one branch into another, but they produce very different histories.",
    "question": "When should you use `git rebase` instead of `git merge`, and what is the one rule you must never break?",
    "roleContext": "Git Scenario #4 (L2) · Branching, Merging & Conflicts",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Workflow tradeoffs, understanding the danger of rewriting shared history.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Both integrate changes from one branch into another, but they produce very different histories.",
        "items": [
          "<strong>Merge</strong> preserves the actual branching history (creates a merge commit). Good for <code>main</code>/release branches where the history of *when things converged* matters.",
          "<strong>Rebase</strong> replays your commits on top of the target branch, producing a linear history with no merge commits. Good for cleaning up your local feature branch *before* opening a PR — it makes review easier and <code>git log</code> readable."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>The one rule: never rebase commits that have been pushed and that other people are working on.</strong> Rebase rewrites commit hashes. If a teammate has pulled the old commits and you force-push rewritten ones, their next pull is a mess and they may re-introduce the old commits. Rebase your private branch all you want; never rebase shared branches like <code>main</code> or <code>develop</code>. Common workflow: rebase your feature branch onto latest <code>main</code> (<code>git pull --rebase origin main</code>), squash interactively (<code>git rebase -i</code>), force-push to your *own* feature branch, then merge the PR. --- ## 🔵 Undo, Recovery & History Rewriting ---"
      }
    ],
    "keyTakeaway": "Pro-Tip: Merge preserves the actual branching history (creates a merge commit). Good for main/release branches where the history of *when t.",
    "elevatorPitch": [
      "Merge preserves the actual branching history (creates a merge commit). Good for main/release bran...",
      "Rebase replays your commits on top of the target branch, producing a linear history with no merge..."
    ]
  },
  {
    "id": "git-q005-you-have-local-changes-in-your-work",
    "title": "Git Q5: You have local changes in your working directory that you dont want anymore How do you discard them and whats the difference between discarding tracked vs untracked changes [L1]",
    "category": "Git",
    "subCategory": "Undo, Recovery & History Rewriting",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "Undo, Recovery & History Rewriting",
      "L1",
      "Version Control",
      "Collaboration",
      "Terraform State"
    ],
    "summary": "Different commands for different states — and one of them is destructive, so know which:",
    "question": "You have local changes in your working directory that you don't want anymore. How do you discard them, and what's the difference between discarding tracked vs untracked changes?",
    "roleContext": "Git Scenario #5 (L1) · Undo, Recovery & History Rewriting",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Understanding of the working tree, staging area, and untracked files.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Different commands for different states — and one of them is destructive, so know which:",
        "items": [
          "<strong>Modified tracked files (not staged):</strong> <code>git restore <file></code> (or older syntax <code>git checkout -- <file></code>) reverts the file to what's in <code>HEAD</code>.",
          "<strong>Staged changes:</strong> <code>git restore --staged <file></code> unstages, leaving the modification in the working tree. Add another <code>git restore <file></code> to discard it too.",
          "<strong>Untracked (new) files:</strong> <code>git restore</code> won't touch them — Git doesn't know they exist. Use <code>git clean -fd</code> to delete them. Run <code>git clean -nd</code> first to preview what will be deleted."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "These are destructive — there's no undo for working-tree changes that were never committed. When unsure, <code>git stash</code> first; you can drop the stash later if you don't need it. ---",
        "items": [
          "<strong>Nuke everything back to a clean checkout:</strong> <code>git reset --hard HEAD && git clean -fd</code>."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: Modified tracked files (not staged): git restore  (or older syntax git checkout -- ) reverts the file to what's in HEAD..",
    "elevatorPitch": [
      "Modified tracked files (not staged): git restore  (or older syntax git checkout -- ) reverts the ...",
      "Staged changes: git restore --staged  unstages, leaving the modification in the working tree. Add...",
      "Untracked (new) files: git restore won't touch them — Git doesn't know they exist. Use git clean ..."
    ]
  },
  {
    "id": "git-q006-you-added-configyaml-to-gitignore-b",
    "title": "Git Q6: You added configyaml to gitignore but Git is still tracking it and showing it as modified Why and how do you fix it [L1]",
    "category": "Git",
    "subCategory": "Undo, Recovery & History Rewriting",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "Undo, Recovery & History Rewriting",
      "L1",
      "Version Control",
      "Collaboration"
    ],
    "summary": ".gitignore only prevents new files from being tracked. It does not untrack files that are already in the repo. Git is still watching conf...",
    "question": "You added `config.yaml` to `.gitignore` but Git is still tracking it and showing it as modified. Why, and how do you fix it?",
    "roleContext": "Git Scenario #6 (L1) · Undo, Recovery & History Rewriting",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Understanding that `.gitignore` only affects untracked files.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "<code>.gitignore</code> only prevents *new* files from being tracked. It does <strong>not</strong> untrack files that are already in the repo. Git is still watching <code>config.yaml</code> because it was committed before the ignore rule existed. Fix it by removing the file from the index while keeping it on disk: The <code>--cached</code> flag is critical — without it, <code>git rm</code> deletes the local file too. After the commit, <code>.gitignore</code> will keep it out going forward. For a whole directory: <code>git rm -r --cached path/to/dir</code>. To re-check what Git is currently tracking that *should* be ignored: <code>git ls-files -ci --exclude-standard</code>. ---",
        "codeBlock": "git rm --cached config.yaml\ngit commit -m \"Stop tracking config.yaml\""
      }
    ],
    "keyTakeaway": "Pro-Tip: .gitignore only prevents *new* files from being tracked. It does not untrack files that are already in the repo. Git is still watc.",
    "elevatorPitch": [
      "Immediate Triage: .gitignore only prevents new files from being tracked. It does not untrack files that are alrea",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "git-q007-youre-in-detached-head-state-what-d",
    "title": "Git Q7: Youre in detached HEAD state What does that mean and how do you get out of it without losing work [L1]",
    "category": "Git",
    "subCategory": "Undo, Recovery & History Rewriting",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "Undo, Recovery & History Rewriting",
      "L1",
      "Version Control",
      "Collaboration",
      "Terraform State"
    ],
    "summary": "Normally HEAD points to a branch (e.g. main), and the branch points to a commit. \"Detached HEAD\" means HEAD points directly at a commit w...",
    "question": "You're in \"detached HEAD\" state. What does that mean, and how do you get out of it without losing work?",
    "roleContext": "Git Scenario #7 (L1) · Undo, Recovery & History Rewriting",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: Mental model of HEAD, branches, and commits.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Normally <code>HEAD</code> points to a branch (e.g. <code>main</code>), and the branch points to a commit. \"Detached HEAD\" means <code>HEAD</code> points directly at a commit with no branch in between — usually because you ran <code>git checkout <commit-hash></code> or <code>git checkout v1.2.0</code> (a tag).",
        "items": [
          "<strong>No new commits made yet?</strong> Just <code>git switch main</code> (or whichever branch). No data at risk.",
          "<strong>Made commits you want to keep?</strong> Create a branch from where you are *before* switching:",
          "<strong>Already switched away and panicking?</strong> <code>git reflog</code> shows everywhere <code>HEAD</code> has been; find your commit hash and <code>git branch rescue-branch <hash></code>."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "It's not broken, just risky: any new commits you make in this state aren't on a branch. If you switch away, those commits become unreachable and will eventually be garbage-collected. To recover: Now those commits are anchored to a branch and safe. ---",
        "codeBlock": "git switch -c rescue-branch"
      }
    ],
    "keyTakeaway": "Pro-Tip: No new commits made yet? Just git switch main (or whichever branch). No data at risk..",
    "elevatorPitch": [
      "No new commits made yet? Just git switch main (or whichever branch). No data at risk.",
      "Made commits you want to keep? Create a branch from where you are *before* switching:",
      "Already switched away and panicking? git reflog shows everywhere HEAD has been; find your commit ..."
    ]
  },
  {
    "id": "git-q008-you-ran-git-push-force-to-your-feat",
    "title": "Git Q8: You ran git push --force to your feature branch but realized you wiped out a teammates commit they had pushed five minutes earlier How do you recover their commit and how do you prevent this next time [L2]",
    "category": "Git",
    "subCategory": "Undo, Recovery & History Rewriting",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "Undo, Recovery & History Rewriting",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": "The commit isn't lost on the remote yet — it's still in the remote's reflog and likely in your teammate's local repo. Recovery:",
    "question": "You ran `git push --force` to your feature branch but realized you wiped out a teammate's commit they had pushed five minutes earlier. How do you recover their commit, and how do you prevent this next time?",
    "roleContext": "Git Scenario #8 (L2) · Undo, Recovery & History Rewriting",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Reflog recovery, `--force-with-lease`, awareness of force-push hazards.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "The commit isn't lost on the remote yet — it's still in the remote's reflog and likely in your teammate's local repo. Recovery:",
        "items": [
          "<strong>Ask the teammate</strong> — their local branch still has the commit. They can push it back (after pulling your changes and rebasing it on top), or share the hash.",
          "<strong>Check the remote's reflog</strong> if you have shell access to the server (rare on managed services). On GitHub, the events API and \"Network\" graph sometimes still show the dangling commit hash for a while; you can fetch it directly: <code>git fetch origin <sha></code>.",
          "<strong>Check your own clone</strong> — if you fetched their commit before force-pushing, <code>git reflog</code> on the remote-tracking branch (<code>git reflog show origin/feature-x</code>) will list it. Cherry-pick it back: <code>git cherry-pick <sha></code> then push.",
          "Use <code>git push --force-with-lease</code> instead of <code>--force</code>. It refuses to push if the remote has commits you haven't seen — exactly this scenario."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>Prevention:</strong> ---",
        "items": [
          "Better: <code>git push --force-if-includes</code> (Git 2.30+) — also verifies your local ref includes the latest fetch.",
          "Branch protection rules on <code>main</code>/<code>develop</code> should block force-push entirely. Force-push should only be allowed (and only via <code>--force-with-lease</code>) on personal feature branches."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: Ask the teammate — their local branch still has the commit. They can push it back (after pulling your changes and rebasing it on t.",
    "elevatorPitch": [
      "Ask the teammate — their local branch still has the commit. They can push it back (after pulling ...",
      "Check the remote's reflog if you have shell access to the server (rare on managed services). On G...",
      "Check your own clone — if you fetched their commit before force-pushing, git reflog on the remote..."
    ]
  },
  {
    "id": "git-q009-you-merged-a-pr-into-main-that-turn",
    "title": "Git Q9: You merged a PR into main that turned out to be broken in production The merge has been there for two hours and 15 commits have landed since How do you back it out safely [L2]",
    "category": "Git",
    "subCategory": "Undo, Recovery & History Rewriting",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "Undo, Recovery & History Rewriting",
      "L2",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "You can't reset main — it's shared and 15 other commits have built on top. The right tool is git revert, which creates a new commit that ...",
    "question": "You merged a PR into `main` that turned out to be broken in production. The merge has been there for two hours and 15 commits have landed since. How do you back it out safely?",
    "roleContext": "Git Scenario #9 (L2) · Undo, Recovery & History Rewriting",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: `git revert` on merge commits, understanding that you can't just delete published history.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "You can't <code>reset</code> <code>main</code> — it's shared and 15 other commits have built on top. The right tool is <code>git revert</code>, which creates a *new* commit that undoes the changes. For a merge commit you need <code>-m</code> to tell Git which parent to revert to (the \"mainline\"): <code>-m 1</code> means \"treat parent 1 (the <code>main</code> side) as the mainline, and revert everything that came in from the feature side.\" <code>-m 2</code> would do the opposite. Push the revert. Now <code>main</code> is back to a working state and the 15 unrelated commits are preserved. <strong>Gotcha:</strong> if later you want to re-merge the fixed version of that feature branch, a plain merge will appear to do nothing because Git thinks those changes are already on <code>main</code> (they were, then reverted). You either revert the revert (<code>git revert <revert-sha></code>) before re-merging, or rebase the feature branch onto current <code>main</code> so the commits get fresh hashes. ---",
        "codeBlock": "git revert -m 1 <merge-commit-sha>"
      }
    ],
    "keyTakeaway": "Pro-Tip: You can't reset main — it's shared and 15 other commits have built on top. The right tool is git revert, which creates a *new* com.",
    "elevatorPitch": [
      "Immediate Triage: You can't reset main — it's shared and 15 other commits have built on top. The right tool is gi",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "git-q010-you-have-eight-messy-wip-fix-typo-m",
    "title": "Git Q10: You have eight messy WIP / fix typo / more fixes commits on your feature branch Reviewers want to see one clean commit per logical change How do you reshape the history before opening the PR [L2]",
    "category": "Git",
    "subCategory": "Undo, Recovery & History Rewriting",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "Undo, Recovery & History Rewriting",
      "L2",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "Use interactive rebase. Pick a base (usually where your branch diverged from main):",
    "question": "You have eight messy \"WIP\" / \"fix typo\" / \"more fixes\" commits on your feature branch. Reviewers want to see one clean commit per logical change. How do you reshape the history before opening the PR?",
    "roleContext": "Git Scenario #10 (L2) · Undo, Recovery & History Rewriting",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Interactive rebase, squash/fixup workflow.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Use interactive rebase. Pick a base (usually where your branch diverged from <code>main</code>):",
        "items": [
          "<code>squash</code> (or <code>s</code>) — combine into the previous commit and let you edit the message.",
          "<code>fixup</code> (or <code>f</code>) — same but discard this commit's message (great for \"fix typo\" commits).",
          "<code>reword</code> (or <code>r</code>) — keep the commit but rewrite its message."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "An editor opens with one line per commit: Change <code>pick</code> to: Save and exit. Git replays the commits according to your plan; resolve any conflicts and <code>git rebase --continue</code>. Force-push your *own* feature branch with <code>--force-with-lease</code>. Never do this on <code>main</code> or any branch others have based work on. Pro tip: as you go, commit with <code>git commit --fixup=<sha></code> and finish with <code>git rebase -i --autosquash</code> — the editor pre-arranges fixups for you. ---",
        "items": [
          "<code>drop</code> (or <code>d</code>) — remove the commit entirely.",
          "Reorder lines to reorder commits."
        ],
        "codeBlock": "git rebase -i origin/main"
      }
    ],
    "keyTakeaway": "Pro-Tip: squash (or s) — combine into the previous commit and let you edit the message..",
    "elevatorPitch": [
      "squash (or s) — combine into the previous commit and let you edit the message.",
      "fixup (or f) — same but discard this commit's message (great for \"fix typo\" commits).",
      "reword (or r) — keep the commit but rewrite its message."
    ]
  },
  {
    "id": "git-q011-a-junior-engineer-ran-git-reset-har",
    "title": "Git Q11: A junior engineer ran git reset --hard HEAD~5 on their local branch and lost five commits of in-progress work Nothing was pushed Walk me through the recovery [L3]",
    "category": "Git",
    "subCategory": "Undo, Recovery & History Rewriting",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Git",
      "Undo, Recovery & History Rewriting",
      "L3",
      "Version Control",
      "Collaboration"
    ],
    "summary": "A --hard reset moves the branch pointer and discards working-tree changes — but the commits themselves are not deleted. They're orphaned ...",
    "question": "A junior engineer ran `git reset --hard HEAD~5` on their local branch and lost five commits of in-progress work. Nothing was pushed. Walk me through the recovery.",
    "roleContext": "Git Scenario #11 (L3) · Undo, Recovery & History Rewriting",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: Deep understanding of refs, reflog, and Git's garbage collection model.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "A <code>--hard</code> reset moves the branch pointer and discards working-tree changes — but the commits themselves are not deleted. They're orphaned (no branch points to them) and will sit in the object database until garbage collection runs (default: ~30 days for unreachable objects, 90 days if reachable from reflog). Recovery via the reflog, which is a per-clone log of every move <code>HEAD</code> (and each branch ref) has made: You'll see something like: <code>def5678</code> is the tip of the work that was wiped. Restore it: Two important caveats: (1) <strong>the reflog is local</strong> — it doesn't help if the commits never existed on this clone (e.g. teammate's machine). (2) Run recovery *before* <code>git gc</code> runs. If you suspect a teammate already ran <code>git gc --prune=now</code>, the objects may genuinely be gone. As a habit, alias <code>reset --hard</code> to require confirmation, and teach the team <code>git stash</code> and <code>git switch -c backup</code> before risky operations. --- ## 🟡 Collaboration & Remote Workflows ---",
        "codeBlock": "git reflog                       # shows HEAD movements\n# or, more targeted:\ngit reflog show feature-branch   # shows that branch's history"
      }
    ],
    "keyTakeaway": "Pro-Tip: A --hard reset moves the branch pointer and discards working-tree changes — but the commits themselves are not deleted. They're or.",
    "elevatorPitch": [
      "Immediate Triage: A --hard reset moves the branch pointer and discards working-tree changes — but the commits the",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "git-q012-in-one-paragraph-what-does-git-pull",
    "title": "Git Q12: In one paragraph what does git pull actually do and why do some teams prefer git pull --rebase [L1]",
    "category": "Git",
    "subCategory": "Collaboration & Remote Workflows",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "Collaboration & Remote Workflows",
      "L1",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "git pull is two commands stitched together: git fetch (download new commits and tags from the remote into origin/<branch>) followed by gi...",
    "question": "In one paragraph: what does `git pull` actually do, and why do some teams prefer `git pull --rebase`?",
    "roleContext": "Git Scenario #12 (L1) · Collaboration & Remote Workflows",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Understanding of fetch + merge composition and history shape preferences.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "<code>git pull</code> is two commands stitched together: <code>git fetch</code> (download new commits and tags from the remote into <code>origin/<branch></code>) followed by <code>git merge origin/<branch></code> into your current branch. If your local branch has commits the remote doesn't, that merge produces a merge commit (\"Merge branch 'main' of …\"), which clutters history with bookkeeping commits that don't represent real work. <code>git pull --rebase</code> replaces the merge step with a rebase: your local commits are temporarily set aside, the new remote commits are applied first, and then your commits are replayed on top. Result: a clean linear history with no noise commits. Many teams set <code>git config --global pull.rebase true</code> so this becomes the default. The tradeoff is that rebase rewrites your *local* commit hashes — fine for unpushed work, but you should never rebase commits others have already pulled. ---"
      }
    ],
    "keyTakeaway": "Pro-Tip: git pull is two commands stitched together: git fetch (download new commits and tags from the remote into origin/) followed by git.",
    "elevatorPitch": [
      "Immediate Triage: git pull is two commands stitched together: git fetch (download new commits and tags from the r",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "git-q013-explain-the-four-states-a-file-can-",
    "title": "Git Q13: Explain the four states a file can be in inside a Git repo untracked modified staged committed Why does Git have a separate staging area [L1]",
    "category": "Git",
    "subCategory": "Collaboration & Remote Workflows",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "Collaboration & Remote Workflows",
      "L1",
      "Version Control",
      "Collaboration",
      "Terraform State"
    ],
    "summary": "A file moves through these states:",
    "question": "Explain the four states a file can be in inside a Git repo: untracked, modified, staged, committed. Why does Git have a separate staging area?",
    "roleContext": "Git Scenario #13 (L1) · Collaboration & Remote Workflows",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Mental model of the index/staging area.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "A file moves through these states:",
        "items": [
          "<strong>Untracked</strong> — exists in your working directory but Git has never been told about it. Shows up under \"Untracked files\" in <code>git status</code>.",
          "<strong>Modified (tracked)</strong> — Git knows about the file, and the working-tree version differs from what's in the last commit. Shows under \"Changes not staged for commit.\"",
          "<strong>Staged</strong> — you ran <code>git add <file></code>, copying its current content into the *index* (staging area). Shows under \"Changes to be committed.\" <code>git commit</code> will record exactly this snapshot."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "The staging area exists so you can build the *next* commit deliberately instead of having every saved file immediately become part of it. You can edit ten files, but <code>git add</code> only the three related ones, then <code>git commit</code> a focused logical change. <code>git add -p</code> takes this further — letting you stage individual hunks within a file. Without the index, you'd have to commit everything at once or stash/branch around it. ---",
        "items": [
          "<strong>Committed</strong> — the staged content has been written into a commit object. Working tree, index, and <code>HEAD</code> all agree."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: Untracked — exists in your working directory but Git has never been told about it. Shows up under \"Untracked files\" in git status..",
    "elevatorPitch": [
      "Untracked — exists in your working directory but Git has never been told about it. Shows up under...",
      "Modified (tracked) — Git knows about the file, and the working-tree version differs from what's i...",
      "Staged — you ran git add , copying its current content into the *index* (staging area). Shows und..."
    ]
  },
  {
    "id": "git-q014-a-critical-bug-fix-commit-exists-on",
    "title": "Git Q14: A critical bug-fix commit exists on develop and you need exactly that one commit on a release branch — without bringing along the other 50 commits on develop How do you do it and what could go wrong [L2]",
    "category": "Git",
    "subCategory": "Collaboration & Remote Workflows",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "Collaboration & Remote Workflows",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": "This is git cherry-pick:",
    "question": "A critical bug-fix commit exists on `develop` and you need exactly that one commit on a release branch — without bringing along the other 50 commits on `develop`. How do you do it, and what could go wrong?",
    "roleContext": "Git Scenario #14 (L2) · Collaboration & Remote Workflows",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Cherry-pick mechanics and its hazards.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "This is <code>git cherry-pick</code>:",
        "items": [
          "<strong>Conflicts</strong> — if the surrounding code on <code>release/1.4</code> is different from <code>develop</code>, the patch may not apply cleanly. Resolve, then <code>git cherry-pick --continue</code>.",
          "<strong>Missing dependencies</strong> — the commit may rely on a refactor or a helper that was introduced in an earlier commit on <code>develop</code> but isn't on <code>release/1.4</code>. The cherry-pick may apply but the code won't compile or behave correctly. You may need to cherry-pick the prerequisite commit too, or write a backport patch.",
          "<strong>Duplicate-looking commits</strong> — when <code>develop</code> is later merged into <code>release</code> (or vice versa), Git usually figures out the commits are equivalent (via patch-id), but in some workflows you can end up with two commits doing the same thing under different SHAs. Use <code>git cherry-pick -x</code> to record \"(cherry picked from commit …)\" in the message — invaluable later for traceability."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Cherry-pick takes the diff that commit introduced and applies it as a *new* commit on the current branch (new SHA, same content). For multiple commits: <code>git cherry-pick <sha1> <sha2> <sha3></code> or a range <code>git cherry-pick A..B</code>. What can go wrong: For ongoing back-port flows (fix on <code>main</code>, port to <code>release/*</code>), some teams prefer a dedicated <code>hotfix/*</code> branch that's merged into both, which avoids cherry-picks entirely. ---",
        "codeBlock": "git switch release/1.4\ngit cherry-pick <commit-sha>\ngit push"
      }
    ],
    "keyTakeaway": "Pro-Tip: Conflicts — if the surrounding code on release/1.4 is different from develop, the patch may not apply cleanly. Resolve, then git c.",
    "elevatorPitch": [
      "Conflicts — if the surrounding code on release/1.4 is different from develop, the patch may not a...",
      "Missing dependencies — the commit may rely on a refactor or a helper that was introduced in an ea...",
      "Duplicate-looking commits — when develop is later merged into release (or vice versa), Git usuall..."
    ]
  },
  {
    "id": "git-q015-production-is-broken-you-know-it-wo",
    "title": "Git Q15: Production is broken You know it worked at the v23 tag but not at HEAD with about 200 commits between them How do you find the exact commit that introduced the bug efficiently [L2]",
    "category": "Git",
    "subCategory": "Collaboration & Remote Workflows",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "Collaboration & Remote Workflows",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Use git bisect — a binary search over commits.",
    "question": "Production is broken. You know it worked at the v2.3 tag but not at HEAD, with about 200 commits between them. How do you find the exact commit that introduced the bug efficiently?",
    "roleContext": "Git Scenario #15 (L2) · Collaboration & Remote Workflows",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: `git bisect` workflow, ability to automate root-cause analysis.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Use <code>git bisect</code> — a binary search over commits. Git checks out a commit roughly halfway between them. You build/test it and report: Each step halves the search space. With 200 commits, you'll converge in ~8 steps (<code>log2(200)</code>). When done, Git prints \"X is the first bad commit.\" Then: <strong>Automate it</strong> if you have a reproducer script that exits 0 (good) or non-zero (bad): Walk away — Git does the rest. Tips: skip commits that don't build with <code>git bisect skip</code>; use <code>--first-parent</code> if your history has many merge commits and you only care about which *merge* introduced the bug. Bisect works because every commit is an immutable snapshot, so each one is independently testable. ---",
        "codeBlock": "git bisect start\ngit bisect bad HEAD       # tell Git the current commit is broken\ngit bisect good v2.3      # tell Git this old tag was fine"
      }
    ],
    "keyTakeaway": "Pro-Tip: Use git bisect — a binary search over commits..",
    "elevatorPitch": [
      "Immediate Triage: Use git bisect — a binary search over commits.",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "git-q016-a-developer-accidentally-committed-",
    "title": "Git Q16: A developer accidentally committed an AWS access key and pushed it to the public repo The team noticed 30 minutes later Whats the correct response in priority order [L2]",
    "category": "Git",
    "subCategory": "Collaboration & Remote Workflows",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "Collaboration & Remote Workflows",
      "L2",
      "Version Control",
      "Collaboration",
      "IAM"
    ],
    "summary": "Treat the credential as compromised the moment it touches a public surface. Order matters:",
    "question": "A developer accidentally committed an AWS access key and pushed it to the public repo. The team noticed 30 minutes later. What's the correct response, in priority order?",
    "roleContext": "Git Scenario #16 (L2) · Collaboration & Remote Workflows",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Incident response thinking; understanding that Git history rewriting alone is not enough.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Treat the credential as compromised the moment it touches a public surface. Order matters:",
        "items": [
          "<strong>Rotate the credential first, before anything else.</strong> In AWS IAM, deactivate the leaked key and issue a new one. Anything you do to Git history is secondary — the key was public for 30 minutes, scrapers are constant, and assume it was harvested.",
          "<strong>Audit usage.</strong> Check CloudTrail for any calls authenticated with that key — region, source IP, services touched. If anything looks suspicious, escalate to security.",
          "<strong>Remove the secret from history.</strong> A plain <code>git revert</code> is <strong>not enough</strong> — the file is still in old commits in <code>.git/objects</code> and visible on GitHub forever. Use <code>git filter-repo</code> (the modern replacement for <code>filter-branch</code>):"
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "or <code>--replace-text</code> to redact a string everywhere. Then force-push (this rewrites history; coordinate with the team). The order is non-negotiable: rotate → audit → scrub → prevent. Reversing 1 and 3 is a common mistake — you can't un-leak a key, but you can stop it from being valid. --- ## 🔴 Advanced ---",
        "items": [
          "<strong>Invalidate forks and caches.</strong> GitHub caches the SHA — open a support ticket asking them to purge the leaked commit, and tell anyone with a fork to re-clone.",
          "<strong>Add prevention.</strong> Pre-commit hook with <code>gitleaks</code> or <code>detect-secrets</code>, plus push protection / secret scanning enabled at the org level so this is blocked next time."
        ],
        "codeBlock": "git filter-repo --path secrets.env --invert-paths"
      }
    ],
    "keyTakeaway": "Pro-Tip: Rotate the credential first, before anything else. In AWS IAM, deactivate the leaked key and issue a new one. Anything you do to G.",
    "elevatorPitch": [
      "Rotate the credential first, before anything else. In AWS IAM, deactivate the leaked key and issu...",
      "Audit usage. Check CloudTrail for any calls authenticated with that key — region, source IP, serv...",
      "Remove the secret from history. A plain git revert is not enough — the file is still in old commi..."
    ]
  },
  {
    "id": "git-q017-your-engineering-org-has-grown-to-5",
    "title": "Git Q17: Your engineering org has grown to 50 developers across three time zones all working on a single backend service You currently use long-lived develop/feature/* branches with weekly merges to main Releases are painful and conflict-heavy How would you change the branching strategy and what tradeoffs are you accepting [L3]",
    "category": "Git",
    "subCategory": "Advanced",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Git",
      "Advanced",
      "L3",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Long-lived feature branches don't scale — the longer a branch lives, the more it diverges, and merges become expensive. The three viable ...",
    "question": "Your engineering org has grown to 50 developers across three time zones, all working on a single backend service. You currently use long-lived `develop`/`feature/*` branches with weekly merges to `main`. Releases are painful and conflict-heavy. How would you change the branching strategy, and what tradeoffs are you accepting?",
    "roleContext": "Git Scenario #17 (L3) · Advanced",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Strategic thinking on branching models for scale.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Long-lived feature branches don't scale — the longer a branch lives, the more it diverges, and merges become expensive. The three viable options:",
        "items": [
          "<strong>Git Flow</strong> (<code>main</code>, <code>develop</code>, <code>release/*</code>, <code>hotfix/*</code>, <code>feature/*</code>) — heavy ceremony, originally designed for shrink-wrapped software with explicit version numbers. For a fast-moving SaaS backend, this is overkill and a step in the wrong direction.",
          "<strong>GitHub Flow</strong> — single <code>main</code>, short-lived feature branches, PR review, deploy from <code>main</code>. Light, simple, well understood.",
          "<strong>Trunk-based development</strong> — everyone commits to (or merges tiny PRs into) <code>main</code> at least daily. No long-lived branches. Incomplete features hide behind feature flags. Continuous deployment from <code>main</code>.",
          "Mandate short-lived branches (≤ 24-48 hours from branch to merge).",
          "Require PR review + green CI to merge — branch protection enforced.",
          "Adopt feature flags so half-built features can land safely behind a toggle."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "For a 50-engineer SaaS team with conflict pain, I'd push toward <strong>trunk-based</strong>: <strong>Tradeoffs you're accepting:</strong> What you gain: dramatically smaller merge conflicts (because everyone is integrating against the same recent <code>main</code>), faster lead time, simpler mental model, and the ability to ship hotfixes without coordinating across release branches. ---",
        "items": [
          "Continuously deploy <code>main</code> to staging; promote to prod on a cadence (or per-merge once confident).",
          "Investment in a feature-flag platform (LaunchDarkly, Unleash, or homegrown) and the discipline to clean up stale flags.",
          "Stronger CI investment — <code>main</code> must always be releasable, which means fast and reliable test suites and probably required status checks, code coverage gates, etc.",
          "Cultural shift — engineers must break work into smaller increments, which some find harder than long branches."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: Git Flow (main, develop, release/*, hotfix/*, feature/*) — heavy ceremony, originally designed for shrink-wrapped software with ex.",
    "elevatorPitch": [
      "Git Flow (main, develop, release/*, hotfix/*, feature/*) — heavy ceremony, originally designed fo...",
      "GitHub Flow — single main, short-lived feature branches, PR review, deploy from main. Light, simp...",
      "Trunk-based development — everyone commits to (or merges tiny PRs into) main at least daily. No l..."
    ]
  },
  {
    "id": "git-q018-your-monorepo-has-grown-to-25-gb-an",
    "title": "Git Q18: Your monorepo has grown to 25 GB and 10 years of history New hires take 45 minutes to clone IDE indexing is slow and most engineers only need ~5% of the tree What Git-side techniques would you use and where do they fall short [L3]",
    "category": "Git",
    "subCategory": "Advanced",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Git",
      "Advanced",
      "L3",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Three orthogonal techniques solve three different problems:",
    "question": "Your monorepo has grown to 25 GB and 10 years of history. New hires take 45 minutes to clone, IDE indexing is slow, and most engineers only need ~5% of the tree. What Git-side techniques would you use, and where do they fall short?",
    "roleContext": "Git Scenario #18 (L3) · Advanced",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Understanding of partial clone, sparse-checkout, LFS, and their limits.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Three orthogonal techniques solve three different problems:",
        "items": [
          "<strong>Partial clone</strong> — defers downloading file blobs until they're actually accessed:",
          "<strong>Sparse-checkout</strong> — limits which paths exist in your working tree:",
          "<strong>Git LFS</strong> for large binaries — design files, ML model artifacts, video. LFS stores a small pointer file in Git and the actual blob on a separate object store, fetched on demand. Adopt this *going forward*; converting historical large blobs requires <code>git lfs migrate import</code> plus a force-push, which rewrites history.",
          "Partial clone makes operations like <code>git log -p</code>, <code>git blame</code>, and bisect across many files trigger on-demand fetches that can be slow or fail offline. Engineers on flaky networks suffer."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Fetches all commits and trees but no file contents. When you <code>checkout</code> or <code>log -p</code> a path, Git lazily fetches just those blobs. Cuts initial clone from 25 GB to a few hundred MB. Requires a server that supports it (GitHub, GitLab, Azure DevOps, Gitea all do). You still have the full history, but only the directories you listed materialize on disk. IDE indexing now sees 5% of the tree. Combine with partial clone for maximum effect (<code>git clone --filter=blob:none --sparse <url></code>). <strong>Where they fall short:</strong> The pragmatic recipe for most teams: sparse + partial clone via a <code>git clone</code> wrapper script for new hires, LFS adopted for any binary > a few MB, and a \"no committing build artifacts\" lint in CI. ---",
        "items": [
          "Sparse-checkout doesn't help operations that *traverse* the repo — <code>git grep</code> over a sparse checkout misses files you didn't materialize, which can be surprising. Cone mode mitigates but doesn't eliminate this.",
          "LFS adds operational dependency on the LFS server, increases hosting cost, and storage isn't free or fast for huge blobs. It's not a silver bullet — for truly large binary pipelines (e.g. game asset workflows), Perforce or a content-addressed object store is sometimes a better fit than Git.",
          "None of these address the *root* problem on Git itself: extremely deep histories on a few hot files (think: lockfiles touched by everyone) can still slow <code>blame</code> and <code>log</code>. At true Google/Microsoft scale you eventually outgrow vanilla Git and look at VFS for Git, Scalar, or Piper-style virtual filesystems."
        ],
        "codeBlock": "git clone --filter=blob:none <url>"
      }
    ],
    "keyTakeaway": "Pro-Tip: Partial clone — defers downloading file blobs until they're actually accessed:.",
    "elevatorPitch": [
      "Partial clone — defers downloading file blobs until they're actually accessed:",
      "Sparse-checkout — limits which paths exist in your working tree:",
      "Git LFS for large binaries — design files, ML model artifacts, video. LFS stores a small pointer ..."
    ]
  },
  {
    "id": "git-q019-your-company-is-enforcing-supply-ch",
    "title": "Git Q19: Your company is enforcing supply-chain integrity The CISO wants every commit on main to have a verifiable author and to be tamper-evident How do you implement this and what attack does it actually prevent [L3]",
    "category": "Git",
    "subCategory": "Advanced",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Git",
      "Advanced",
      "L3",
      "Version Control",
      "Collaboration"
    ],
    "summary": "By default, Git's Author and Committer headers are unauthenticated free-text — anyone can run git config user.email \"ceo@company.com\" and...",
    "question": "Your company is enforcing supply-chain integrity. The CISO wants every commit on `main` to have a verifiable author and to be tamper-evident. How do you implement this, and what attack does it actually prevent?",
    "roleContext": "Git Scenario #19 (L3) · Advanced",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: Understanding of commit signing, identity vs authorship, and threat modeling.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "By default, Git's <code>Author</code> and <code>Committer</code> headers are unauthenticated free-text — anyone can run <code>git config user.email \"ceo@company.com\"</code> and produce commits that look like the CEO. Signing fixes that with cryptographic proof.",
        "items": [
          "<strong>Signing keys per developer.</strong> Either GPG (traditional) or <strong>SSH signing</strong> (Git ≥ 2.34, much simpler — reuse the SSH key engineers already have):",
          "<strong>Allowed-signers file</strong> mapping email → public key, distributed via your IDP / SSO so trust is centralized, not per-laptop.",
          "<strong>Upload the public key to GitHub/GitLab</strong> under \"Signing key.\" Now the platform shows a \"Verified\" badge and exposes verification status via the API.",
          "<strong>Branch protection on <code>main</code></strong> — require signed commits, require linear history, require status checks. Block merges where any commit is unsigned.",
          "<strong>CI verification</strong> — a pre-merge job that runs <code>git verify-commit</code> against each commit in the PR, failing if any commit isn't signed by a key in the allowed-signers list. Don't rely solely on the platform UI badge.",
          "<strong>CODEOWNERS</strong> to require domain-expert review on sensitive paths (e.g. <code>/infra/</code>, <code>/auth/</code>, CI workflows themselves)."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>Implementation:</strong> <strong>What this actually prevents:</strong> **What it does *not* prevent:** Signing answers \"who wrote this commit?\" with cryptography. It does not answer \"is this code safe?\" — that's a separate problem. ---",
        "items": [
          "<strong>Author spoofing</strong> — an attacker who compromises one developer's laptop can no longer forge commits as a different developer (different key).",
          "<strong>Tampering with merged history</strong> — if someone with repo-admin access tries to silently rewrite a past commit, the signature on the original commit chain breaks and CI rejects it.",
          "<strong>Compromised CI tokens</strong> pushing arbitrary code as humans — unsigned commits are blocked, so a leaked PAT can't ship merge-able changes without a key.",
          "A compromised developer laptop with their key on it — the attacker's commits will sign correctly. Mitigations: hardware-backed keys (YubiKey for GPG, or SSH keys in <code>ssh-agent</code> with confirmation), short-lived keys via SSO-issued certificates, and behavioral monitoring.",
          "Malicious code that's *legitimately* authored and signed (insider threat, social-engineered review). That's what CODEOWNERS, mandatory review, and SCA tooling are for."
        ],
        "codeBlock": "git config --global gpg.format ssh\n   git config --global user.signingkey ~/.ssh/id_ed25519.pub\n   git config --global commit.gpgsign true\n   git config --global tag.gpgsign true"
      }
    ],
    "keyTakeaway": "Pro-Tip: Signing keys per developer. Either GPG (traditional) or SSH signing (Git ≥ 2.34, much simpler — reuse the SSH key engineers alread.",
    "elevatorPitch": [
      "Signing keys per developer. Either GPG (traditional) or SSH signing (Git ≥ 2.34, much simpler — r...",
      "Allowed-signers file mapping email → public key, distributed via your IDP / SSO so trust is centr...",
      "Upload the public key to GitHub/GitLab under \"Signing key.\" Now the platform shows a \"Verified\" b..."
    ]
  },
  {
    "id": "git-q020-design-a-branch-protection-and-merg",
    "title": "Git Q20: Design a branch-protection and merge-policy setup for a regulated environment (PCI / SOC 2) with 30 services in a monorepo Walk me through the controls youd put on main and how they interact with developer ergonomics [L3]",
    "category": "Git",
    "subCategory": "Advanced",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Git",
      "Advanced",
      "L3",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "Compliance auditors essentially want to see: every change to main is reviewed by someone other than the author, every change is traced to...",
    "question": "Design a branch-protection and merge-policy setup for a regulated environment (PCI / SOC 2) with 30 services in a monorepo. Walk me through the controls you'd put on `main` and how they interact with developer ergonomics.",
    "roleContext": "Git Scenario #20 (L3) · Advanced",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Holistic policy design balancing compliance, security, and velocity.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Compliance auditors essentially want to see: every change to <code>main</code> is reviewed by someone other than the author, every change is traced to a ticket, every change runs the same controls, and the history is tamper-evident.",
        "items": [
          "<strong>No direct pushes</strong> — pushes only via PR. <code>Restrict who can push to matching branches</code> set to no one (not even admins, with <code>Do not allow bypassing</code> enabled).",
          "<strong>Linear history required</strong> — no merge commits. Forces squash- or rebase-merge, making each <code>main</code> commit a single reviewable, revertable unit.",
          "<strong>Required reviews</strong> — minimum 1 for low-risk paths, 2 for sensitive paths via CODEOWNERS. <code>Dismiss stale reviews</code> on new commits so a reviewer's approval doesn't carry over after the author force-pushes.",
          "<strong>CODEOWNERS</strong> mapping critical paths (<code>/infra/</code>, <code>/auth/</code>, <code>/billing/</code>, <code>/.github/workflows/</code>) to specific owners. Workflows-on-workflows is a common attack vector — protect <code>.github/</code> aggressively.",
          "<strong>Required status checks</strong> — CI must pass: unit tests, integration tests, SAST (e.g. Semgrep), dependency scan (Dependabot/Snyk/Trivy), secret scan (gitleaks), license scan, and the duplicate-Q&A or contract-tests if relevant. Pin the exact check names so they can't be renamed away.",
          "<strong>Signed commits required</strong> — see prior answer.",
          "<strong>Conversation resolution required</strong> — every PR comment must be resolved before merge.",
          "<strong>No bypass for admins</strong> — auditors will ask. The tradeoff: a real emergency (rare) requires temporarily lifting the rule, which is logged in the audit trail and is itself a finding to triage.",
          "<strong>Ticket linking</strong> — PR title regex enforced to include <code>JIRA-1234</code> (or similar). The <code>commit-msg</code> hook on the server rejects PRs without it. Auditors get traceability from ticket → PR → commit → deployment."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>Controls on <code>main</code>:</strong> <strong>Process controls layered on top:</strong> <strong>Where this hits ergonomics:</strong> The principle: <strong>make the secure path the easy path.</strong> If the protected workflow is faster than circumventing it (because of good tooling, fast CI, and sane PR sizes), engineers stays on it; the policy then enforces itself culturally rather than only via lockdown. --- ## 🟠 Stashing, Tags & Everyday Workflows ---",
        "items": [
          "<strong>PR templates</strong> — checkboxes for \"tests added,\" \"security impact considered,\" \"rollback plan.\" Required for sensitive areas via CODEOWNERS-driven PR templates.",
          "<strong>Production deploy gating</strong> — <code>main</code> is continuously deployed to staging, but production promotion goes through a separate change-management workflow (ServiceNow, Linear, etc.) with the deploying engineer different from the commit author. Many auditors require this segregation of duties.",
          "<strong>Audit log retention</strong> — Git host audit logs (GitHub Enterprise, GitLab Premium) retained 1+ year and shipped to your SIEM. The audit log captures every protection-rule change, force-push attempt, and admin override.",
          "Two reviewers required on <code>/auth/</code> slows urgent fixes — mitigate with a documented break-glass process and clear on-call ownership.",
          "Required signed commits adds setup friction for new hires — build it into the laptop bootstrap script and have the onboarding checklist verify a signed test commit before they get repo write access.",
          "Many required checks → slow merges. Invest in CI parallelism and selective testing (run only the affected service's tests when paths under <code>services/foo/</code> change). Without this, developers will route around the system, defeating the policy.",
          "\"No bypass for admins\" feels paranoid until you're sitting in a SOC 2 audit; lean into it and design the emergency process upfront so engineers know what to do without disabling controls."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: No direct pushes — pushes only via PR. Restrict who can push to matching branches set to no one (not even admins, with Do not allo.",
    "elevatorPitch": [
      "No direct pushes — pushes only via PR. Restrict who can push to matching branches set to no one (...",
      "Linear history required — no merge commits. Forces squash- or rebase-merge, making each main comm...",
      "Required reviews — minimum 1 for low-risk paths, 2 for sensitive paths via CODEOWNERS. Dismiss st..."
    ]
  },
  {
    "id": "git-q021-youre-halfway-through-coding-a-feat",
    "title": "Git Q21: Youre halfway through coding a feature when an urgent bug report comes in You need to switch branches immediately but your changes arent ready to commit How do you save your in-progress work [L1]",
    "category": "Git",
    "subCategory": "Stashing, Tags & Everyday Workflows",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "Stashing, Tags & Everyday Workflows",
      "L1",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Use git stash to save your uncommitted changes onto a stack without committing them:",
    "question": "You're halfway through coding a feature when an urgent bug report comes in. You need to switch branches immediately, but your changes aren't ready to commit. How do you save your in-progress work?",
    "roleContext": "Git Scenario #21 (L1) · Stashing, Tags & Everyday Workflows",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Practical understanding of `git stash` and its usage patterns.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Use <code>git stash</code> to save your uncommitted changes onto a stack without committing them:",
        "items": [
          "<code>git stash</code> saves both staged and unstaged changes to tracked files. <strong>Untracked files are not stashed by default</strong> — add <code>-u</code> (or <code>--include-untracked</code>) to include them. Add <code>-a</code> to also include ignored files.",
          "<code>git stash list</code> shows all stashes. They're numbered <code>stash@{0}</code>, <code>stash@{1}</code>, etc.",
          "<code>git stash pop</code> applies the most recent stash and removes it from the stack. <code>git stash apply</code> applies it but keeps it on the stack — safer if you want to apply the same stash to multiple branches."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Key details: Don't use stash as long-term storage — stashes are easy to forget. If the work will sit for more than a few hours, commit it on a throwaway branch instead. ---",
        "items": [
          "<code>git stash drop stash@{2}</code> removes a specific stash. <code>git stash clear</code> removes all.",
          "You can create a branch directly from a stash: <code>git stash branch new-branch stash@{0}</code> — useful if the stash conflicts with current work."
        ],
        "codeBlock": "git stash push -m \"WIP: user profile feature\"\ngit switch hotfix/critical-bug\n# ... fix the bug, commit, push ...\ngit switch feature/user-profile\ngit stash pop"
      }
    ],
    "keyTakeaway": "Pro-Tip: git stash saves both staged and unstaged changes to tracked files. Untracked files are not stashed by default — add -u (or --inclu.",
    "elevatorPitch": [
      "git stash saves both staged and unstaged changes to tracked files. Untracked files are not stashe...",
      "git stash list shows all stashes. They're numbered stash@{0}, stash@{1}, etc.",
      "git stash pop applies the most recent stash and removes it from the stack. git stash apply applie..."
    ]
  },
  {
    "id": "git-q022-what-is-the-difference-between-a-li",
    "title": "Git Q22: What is the difference between a lightweight tag and an annotated tag When should you use each [L1]",
    "category": "Git",
    "subCategory": "... fix the bug, commit, push ...",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "... fix the bug, commit, push ...",
      "L1",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Both point to a commit, but they're stored differently:",
    "question": "What is the difference between a lightweight tag and an annotated tag? When should you use each?",
    "roleContext": "Git Scenario #22 (L1) · ... fix the bug, commit, push ...",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Understanding of Git's tag objects and release workflows.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Both point to a commit, but they're stored differently:",
        "items": [
          "<strong>Lightweight tag</strong> — just a pointer (like a branch that doesn't move). Created with <code>git tag v1.0</code>. No metadata, no message, no signature.",
          "<strong>Annotated tag</strong> — a full Git object with its own SHA, author, date, message, and optional GPG/SSH signature. Created with <code>git tag -a v1.0 -m \"Release 1.0\"</code>.",
          "<strong>Annotated tags</strong> for anything published — releases, versioning, deployment markers. They show up in <code>git describe</code>, they carry context about *who* tagged and *why*, and they can be signed for tamper-evidence. Most CI/CD pipelines trigger on annotated tags."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "When to use each: <code>git push</code> does <strong>not</strong> push tags by default. Use <code>git push --tags</code> or <code>git push origin v1.0</code>. To delete a remote tag: <code>git push origin --delete v1.0</code>. ---",
        "items": [
          "<strong>Lightweight tags</strong> for personal/temporary bookmarks — \"I want to remember this commit.\" They're fine for local use but shouldn't be pushed as release markers."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: Lightweight tag — just a pointer (like a branch that doesn't move). Created with git tag v1.0. No metadata, no message, no signatu.",
    "elevatorPitch": [
      "Lightweight tag — just a pointer (like a branch that doesn't move). Created with git tag v1.0. No...",
      "Annotated tag — a full Git object with its own SHA, author, date, message, and optional GPG/SSH s...",
      "Annotated tags for anything published — releases, versioning, deployment markers. They show up in..."
    ]
  },
  {
    "id": "git-q023-you-committed-with-the-wrong-messag",
    "title": "Git Q23: You committed with the wrong message — or forgot to add a file to the last commit How do you fix it without creating a new commit [L1]",
    "category": "Git",
    "subCategory": "... fix the bug, commit, push ...",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "... fix the bug, commit, push ...",
      "L1",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Use git commit --amend:",
    "question": "You committed with the wrong message — or forgot to add a file to the last commit. How do you fix it without creating a new commit?",
    "roleContext": "Git Scenario #23 (L1) · ... fix the bug, commit, push ...",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: `--amend` usage and awareness of its implications.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Use <code>git commit --amend</code>:",
        "items": [
          "<strong>Fix the message only:</strong>",
          "<strong>Add a forgotten file to the last commit:</strong>"
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<code>--amend</code> replaces the last commit with a new one (new SHA). The old commit becomes orphaned and will be garbage-collected eventually. <strong>Critical rule:</strong> only amend commits that haven't been pushed. If you've already pushed, amending requires a force-push, which rewrites shared history. For pushed commits, create a follow-up commit instead, or use <code>--force-with-lease</code> on your own feature branch if the team agrees. ---",
        "codeBlock": "git commit --amend -m \"Correct commit message\""
      }
    ],
    "keyTakeaway": "Pro-Tip: Fix the message only:.",
    "elevatorPitch": [
      "Fix the message only:",
      "Add a forgotten file to the last commit:"
    ]
  },
  {
    "id": "git-q024-your-team-uses-git-submodules-to-in",
    "title": "Git Q24: Your team uses Git submodules to include a shared library in three different services A developer reports that after cloning the submodule directory is empty What happened and how do you manage submodules correctly [L2]",
    "category": "Git",
    "subCategory": "... fix the bug, commit, push ...",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "... fix the bug, commit, push ...",
      "L2",
      "Version Control",
      "Collaboration",
      "Terraform State"
    ],
    "summary": "git clone does not initialize submodules by default — it clones the parent repo and creates the submodule directory, but leaves it empty....",
    "question": "Your team uses Git submodules to include a shared library in three different services. A developer reports that after cloning, the submodule directory is empty. What happened, and how do you manage submodules correctly?",
    "roleContext": "Git Scenario #24 (L2) · ... fix the bug, commit, push ...",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Submodule mechanics, common pitfalls, and workflow.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "<code>git clone</code> does <strong>not</strong> initialize submodules by default — it clones the parent repo and creates the submodule directory, but leaves it empty. The fix:",
        "items": [
          "The parent repo stores a <code>.gitmodules</code> file (URL + path mapping) and a special tree entry recording the *exact* commit SHA the submodule should point to.",
          "<code>git submodule update</code> checks out that pinned commit inside the submodule directory — it puts the submodule in <strong>detached HEAD</strong> state.",
          "To update the submodule to its latest upstream commit: <code>cd <submodule-dir> && git pull origin main</code>, then go back to the parent repo and <code>git add <submodule-dir> && git commit</code> to record the new SHA.",
          "<strong>Forgetting <code>--recurse-submodules</code> on clone, pull, and checkout</strong> — configure globally: <code>git config --global submodule.recurse true</code>."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Or clone with submodules from the start: How submodules work: Common pitfalls: Many teams eventually migrate away from submodules to package managers (npm, pip, Maven) or monorepo approaches because submodules add significant cognitive overhead. ---",
        "items": [
          "<strong>Committing without updating the submodule pointer</strong> — you update the library but forget to commit the new SHA in the parent repo. Other developers don't get the update.",
          "<strong>Nested submodules</strong> — always use <code>--recursive</code>."
        ],
        "codeBlock": "git submodule update --init --recursive"
      }
    ],
    "keyTakeaway": "Pro-Tip: The parent repo stores a .gitmodules file (URL + path mapping) and a special tree entry recording the *exact* commit SHA the submo.",
    "elevatorPitch": [
      "The parent repo stores a .gitmodules file (URL + path mapping) and a special tree entry recording...",
      "git submodule update checks out that pinned commit inside the submodule directory — it puts the s...",
      "To update the submodule to its latest upstream commit: cd  && git pull origin main, then go back ..."
    ]
  },
  {
    "id": "git-q025-you-need-to-work-on-two-branches-of",
    "title": "Git Q25: You need to work on two branches of the same repo simultaneously — for example testing a fix on release/20 while actively developing on feature/new-api Switching branches back and forth is painful because of build artifacts and IDE reindexing Whats the solution [L2]",
    "category": "Git",
    "subCategory": "... fix the bug, commit, push ...",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "... fix the bug, commit, push ...",
      "L2",
      "Version Control",
      "Collaboration",
      "Terraform State"
    ],
    "summary": "Use git worktree to check out multiple branches simultaneously in separate directories, all backed by the same .git database:",
    "question": "You need to work on two branches of the same repo simultaneously — for example, testing a fix on `release/2.0` while actively developing on `feature/new-api`. Switching branches back and forth is painful because of build artifacts and IDE reindexing. What's the solution?",
    "roleContext": "Git Scenario #25 (L2) · ... fix the bug, commit, push ...",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Awareness of `git worktree` for parallel development.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Use <code>git worktree</code> to check out multiple branches simultaneously in separate directories, all backed by the same <code>.git</code> database:",
        "items": [
          "<code>~/projects/my-repo/</code> → <code>feature/new-api</code>",
          "<code>~/projects/release-2.0-worktree/</code> → <code>release/2.0</code>",
          "<strong>A branch can only be checked out in one worktree at a time.</strong> Git enforces this to prevent conflicting index states.",
          "<code>git worktree list</code> shows all worktrees."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Now you have two working directories: Each has its own working tree, index, and HEAD, but they share the same object store, refs, and config. No extra disk space for the Git history. Key rules: This is vastly better than cloning the repo twice (which doubles disk usage and requires separate fetches) and avoids the constant <code>stash/switch/pop</code> dance. ---",
        "items": [
          "<code>git worktree remove ../release-2.0-worktree</code> cleans it up when done.",
          "Worktrees share refs — a commit made in one worktree is immediately visible in the other (they share <code>.git</code>)."
        ],
        "codeBlock": "# From your main checkout (on feature/new-api):\ngit worktree add ../release-2.0-worktree release/2.0"
      }
    ],
    "keyTakeaway": "Pro-Tip: ~/projects/my-repo/ → feature/new-api.",
    "elevatorPitch": [
      "~/projects/my-repo/ → feature/new-api",
      "~/projects/release-2.0-worktree/ → release/2.0",
      "A branch can only be checked out in one worktree at a time. Git enforces this to prevent conflict..."
    ]
  },
  {
    "id": "git-q026-your-team-wants-to-enforce-coding-s",
    "title": "Git Q26: Your team wants to enforce coding standards and prevent certain mistakes at commit time — for example blocking commits with consolelog() or failing if unit tests dont pass How would you set this up with Git hooks [L2]",
    "category": "Git",
    "subCategory": "From your main checkout (on feature/new-api):",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "From your main checkout (on feature/new-api):",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Git hooks are scripts that run at specific points in the Git workflow. For pre-commit enforcement:",
    "question": "Your team wants to enforce coding standards and prevent certain mistakes at commit time — for example, blocking commits with `console.log()` or failing if unit tests don't pass. How would you set this up with Git hooks?",
    "roleContext": "Git Scenario #26 (L2) · From your main checkout (on feature/new-api):",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Understanding of Git hooks, client-side vs server-side, and tooling like Husky.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Git hooks are scripts that run at specific points in the Git workflow. For pre-commit enforcement:",
        "items": [
          "<strong><code>pre-commit</code> hook</strong> — runs before the commit is created. Exit non-zero to block the commit:",
          "<strong><code>commit-msg</code> hook</strong> — validates the commit message format (e.g., enforce Conventional Commits or JIRA ticket references):",
          "<strong><code>pre-push</code> hook</strong> — run tests before allowing a push.",
          "<strong>Husky</strong> (Node.js) — stores hooks in <code>.husky/</code> in the repo and installs them via <code>npm prepare</code>."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>The distribution problem:</strong> hooks live in <code>.git/hooks/</code>, which is not committed to the repo. Solutions: <strong>Client-side hooks can be bypassed</strong> with <code>git commit --no-verify</code>. For enforcement you can't skip, use <strong>server-side hooks</strong> (<code>pre-receive</code>, <code>update</code>) on the Git server, or platform-based checks (GitHub required status checks, GitLab push rules). ---",
        "items": [
          "<strong>pre-commit framework</strong> (Python) — <code>.pre-commit-config.yaml</code> defines hooks from shared repos.",
          "<strong>lefthook</strong> (Go) — similar, language-agnostic, fast."
        ],
        "codeBlock": "#!/bin/sh\n   # .git/hooks/pre-commit\n   if git diff --cached --name-only | xargs grep -l 'console.log'; then\n     echo \"ERROR: Remove console.log() before committing\"\n     exit 1\n   fi"
      }
    ],
    "keyTakeaway": "Pro-Tip: pre-commit hook — runs before the commit is created. Exit non-zero to block the commit:.",
    "elevatorPitch": [
      "pre-commit hook — runs before the commit is created. Exit non-zero to block the commit:",
      "commit-msg hook — validates the commit message format (e.g., enforce Conventional Commits or JIRA...",
      "pre-push hook — run tests before allowing a push."
    ]
  },
  {
    "id": "git-q027-you-need-to-find-out-who-last-chang",
    "title": "Git Q27: You need to find out who last changed a specific line in a file when they changed it and why Walk me through how youd investigate [L2]",
    "category": "Git",
    "subCategory": "From your main checkout (on feature/new-api):",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "From your main checkout (on feature/new-api):",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Start with git blame:",
    "question": "You need to find out who last changed a specific line in a file, when they changed it, and why. Walk me through how you'd investigate.",
    "roleContext": "Git Scenario #27 (L2) · From your main checkout (on feature/new-api):",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: `git blame`, `git log` for a specific line range, and investigation workflow.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Start with <code>git blame</code>:",
        "items": [
          "<code>git blame --ignore-rev <sha></code> skips a known bulk-formatting commit. Create a <code>.git-blame-ignore-revs</code> file listing such commits and configure it globally.",
          "<code>git blame -w</code> ignores whitespace changes.",
          "<code>git blame -C</code> detects code moved or copied from other files."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "This shows the commit SHA, author, date, and content for line 42. The <code>-L</code> flag limits output to specific lines (<code>-L 40,50</code> for a range). To understand *why* the change was made: If the blame shows a commit like \"Apply formatting\" (not the real author), dig deeper: This blames the file at the commit *before* the formatting change. Or use <code>git log -L 42,42:src/auth/login.js</code> to see the full history of changes to that specific line range — every commit that touched those lines, with diffs. For files that have been renamed, add <code>--follow</code>: Pro tips: ---",
        "codeBlock": "git blame -L 42,42 src/auth/login.js"
      }
    ],
    "keyTakeaway": "Pro-Tip: git blame --ignore-rev  skips a known bulk-formatting commit. Create a .git-blame-ignore-revs file listing such commits and config.",
    "elevatorPitch": [
      "git blame --ignore-rev  skips a known bulk-formatting commit. Create a .git-blame-ignore-revs fil...",
      "git blame -w ignores whitespace changes.",
      "git blame -C detects code moved or copied from other files."
    ]
  },
  {
    "id": "git-q028-you-maintain-a-shared-library-used-",
    "title": "Git Q28: You maintain a shared library used by multiple teams They want updates without a full monorepo migration A colleague suggests git subtree instead of submodules Whats the difference and how does subtree work [L2]",
    "category": "Git",
    "subCategory": "From your main checkout (on feature/new-api):",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "From your main checkout (on feature/new-api):",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": "git subtree embeds the contents of another repo directly into a subdirectory of your repo — no special .gitmodules file, no detached HEAD...",
    "question": "You maintain a shared library used by multiple teams. They want updates without a full monorepo migration. A colleague suggests `git subtree` instead of submodules. What's the difference, and how does subtree work?",
    "roleContext": "Git Scenario #28 (L2) · From your main checkout (on feature/new-api):",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Subtree vs submodule tradeoffs, practical subtree workflow.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "<code>git subtree</code> embeds the contents of another repo directly into a subdirectory of your repo — no special <code>.gitmodules</code> file, no detached HEADs, no initialization step. <strong>Adding a subtree:</strong> This pulls the library's code into <code>libs/shared-lib/</code> as a single squashed commit. The files are regular tracked files in your repo. <strong>Pulling updates:</strong> <strong>Pushing changes back upstream</strong> (if you modify the library in your repo): <strong>Subtree vs submodule tradeoffs:</strong> | Aspect | Submodule | Subtree | |--------|-----------|---------| | Storage | Pointer to external commit | Full code embedded | | Clone behavior | Requires <code>--recurse-submodules</code> | Just works | | Updating | <code>submodule update</code> | <code>subtree pull</code> | | History | Separate repo history | Merged into parent history | | Contributor friction | High (extra commands) | Low (files are just there) | | Pushing changes back | cd into submodule, push | <code>subtree push</code> (can be slow on large repos) | Use subtree when consumers vastly outnumber contributors to the shared library, or when you want zero friction for developers who don't care about the library's internals. ---",
        "codeBlock": "git subtree add --prefix=libs/shared-lib https://github.com/org/shared-lib.git main --squash"
      }
    ],
    "keyTakeaway": "Pro-Tip: git subtree embeds the contents of another repo directly into a subdirectory of your repo — no special .gitmodules file, no detach.",
    "elevatorPitch": [
      "Immediate Triage: git subtree embeds the contents of another repo directly into a subdirectory of your repo — no ",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "git-q029-what-does-git-fetch-do-vs-git-pull-",
    "title": "Git Q29: What does git fetch do vs git pull When would you use git fetch alone [L1]",
    "category": "Git",
    "subCategory": "From your main checkout (on feature/new-api):",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "From your main checkout (on feature/new-api):",
      "L1",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "git fetch downloads new commits, branches, and tags from the remote into your local remote-tracking branches (e.g., origin/main) but does...",
    "question": "What does `git fetch` do vs `git pull`? When would you use `git fetch` alone?",
    "roleContext": "Git Scenario #29 (L1) · From your main checkout (on feature/new-api):",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Understanding of the two-step nature of pull and when to inspect before integrating.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "<code>git fetch</code> downloads new commits, branches, and tags from the remote into your local remote-tracking branches (e.g., <code>origin/main</code>) but <strong>does not touch your working directory or local branches</strong>. Your code stays exactly as it is.",
        "items": [
          "<strong>You want to see what changed before integrating</strong> — after fetching, run <code>git log HEAD..origin/main</code> to see incoming commits, or <code>git diff HEAD origin/main</code> to see the actual changes. Then decide whether to merge, rebase, or wait.",
          "<strong>You're on a different branch</strong> and just want to update your tracking refs for later.",
          "<strong>You want to fetch all branches</strong> without merging any: <code>git fetch --all</code>."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<code>git pull</code> = <code>git fetch</code> + <code>git merge</code> (or <code>git rebase</code> if configured). Use <code>git fetch</code> alone when: Think of <code>fetch</code> as \"check for mail\" and <code>pull</code> as \"check for mail and read it immediately.\" In collaborative workflows, fetching first avoids surprise merge conflicts mid-coding. ---",
        "items": [
          "<strong>In CI/CD scripts</strong> where you need full control over what gets integrated and when."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: You want to see what changed before integrating — after fetching, run git log HEAD..origin/main to see incoming commits, or git di.",
    "elevatorPitch": [
      "You want to see what changed before integrating — after fetching, run git log HEAD..origin/main t...",
      "You're on a different branch and just want to update your tracking refs for later.",
      "You want to fetch all branches without merging any: git fetch --all."
    ]
  },
  {
    "id": "git-q030-your-ci-pipeline-has-a-job-that-nee",
    "title": "Git Q30: Your CI pipeline has a job that needs the last 10 commits for changelog generation but the full repo history (50000 commits) takes too long to clone How do you optimize this [L2]",
    "category": "Git",
    "subCategory": "From your main checkout (on feature/new-api):",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "From your main checkout (on feature/new-api):",
      "L2",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "Use a shallow clone with --depth:",
    "question": "Your CI pipeline has a job that needs the last 10 commits for changelog generation but the full repo history (50,000 commits) takes too long to clone. How do you optimize this?",
    "roleContext": "Git Scenario #30 (L2) · From your main checkout (on feature/new-api):",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Shallow clones, depth limiting, and their tradeoffs.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Use a <strong>shallow clone</strong> with <code>--depth</code>:",
        "items": [
          "<code>--depth=N</code> — last N commits on each fetched branch.",
          "<code>--shallow-since=\"2024-01-01\"</code> — everything since a date.",
          "<code>--shallow-exclude=<ref></code> — everything excluding what's reachable from a ref.",
          "<code>git log</code> only shows the shallow history — earlier commits are invisible."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "This downloads only the last 10 commits and their associated tree/blob objects. The clone is fast and small. For CI systems that already have a cached checkout, use shallow fetch: <strong>Variations:</strong> <strong>Tradeoffs:</strong> <strong>Deepen later if needed:</strong> Most CI platforms (GitHub Actions, GitLab CI) expose a <code>fetch-depth</code> option. Set it to the minimum needed for your job. For jobs that only need to build and test the latest commit (no history needed), <code>--depth=1</code> is ideal. ---",
        "items": [
          "<code>git merge-base</code> may fail if the common ancestor is beyond the shallow boundary, breaking merge/rebase operations.",
          "<code>git blame</code> and <code>git bisect</code> stop at the shallow boundary.",
          "<code>git push</code> from a shallow clone can fail if the server can't find common ancestors."
        ],
        "codeBlock": "git clone --depth=10 <repo-url>"
      }
    ],
    "keyTakeaway": "Pro-Tip: --depth=N — last N commits on each fetched branch..",
    "elevatorPitch": [
      "--depth=N — last N commits on each fetched branch.",
      "--shallow-since=\"2024-01-01\" — everything since a date.",
      "--shallow-exclude= — everything excluding what's reachable from a ref."
    ]
  },
  {
    "id": "git-q031-git-rerere-is-enabled-in-your-confi",
    "title": "Git Q31: git rerere is enabled in your config What does it do and in what workflows does it save the most time [L2]",
    "category": "Git",
    "subCategory": "From your main checkout (on feature/new-api):",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "From your main checkout (on feature/new-api):",
      "L2",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "rerere stands for \"reuse recorded resolution.\" When enabled (git config --global rerere.enabled true), Git silently records how you resol...",
    "question": "`git rerere` is enabled in your config. What does it do, and in what workflows does it save the most time?",
    "roleContext": "Git Scenario #31 (L2) · From your main checkout (on feature/new-api):",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: Understanding of rerere (reuse recorded resolution) and its niche but powerful use case.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "<code>rerere</code> stands for \"<strong>re</strong>use <strong>re</strong>corded <strong>re</strong>solution.\" When enabled (<code>git config --global rerere.enabled true</code>), Git silently records how you resolved each merge conflict. If the *exact same* conflict appears again later, Git applies the same resolution automatically.",
        "items": [
          "You hit a conflict during merge or rebase and manually resolve it.",
          "Git records the conflict (before) and resolution (after) in <code>.git/rr-cache/</code>.",
          "Next time the same conflict appears (same pre-image), Git auto-applies your recorded resolution. You still need to <code>git add</code> and commit, but the file is already correctly resolved.",
          "<strong>Long-lived feature branches</strong> that you repeatedly rebase onto <code>main</code>. Each rebase replays all commits, and the same conflicts keep appearing. With rerere, you resolve each conflict once; subsequent rebases are automatic.",
          "<strong>Topic branch workflows</strong> where you test-merge branches into an integration branch, then discard the merge and re-merge later for the real release. Without rerere, you re-resolve every conflict."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>How it works:</strong> <strong>Where it shines:</strong> <strong>Caveats:</strong> ---",
        "items": [
          "<strong>Cherry-pick-heavy workflows</strong> (backporting fixes across release branches) where similar conflicts recur.",
          "The cache is local — not shared across clones. Each developer's rerere database is separate.",
          "If you resolve a conflict incorrectly, rerere will faithfully re-apply the wrong resolution. Use <code>git rerere forget <file></code> to clear a bad recording.",
          "<code>git rerere gc</code> cleans up old recordings (default: unresolved conflicts older than 15 days, resolved ones older than 60 days)."
        ]
      }
    ],
    "keyTakeaway": "Pro-Tip: You hit a conflict during merge or rebase and manually resolve it..",
    "elevatorPitch": [
      "You hit a conflict during merge or rebase and manually resolve it.",
      "Git records the conflict (before) and resolution (after) in .git/rr-cache/.",
      "Next time the same conflict appears (same pre-image), Git auto-applies your recorded resolution. ..."
    ]
  },
  {
    "id": "git-q032-what-is-a-gitkeep-file-and-why-do-y",
    "title": "Git Q32: What is a gitkeep file and why do you sometimes see empty files with that name committed to repos [L1]",
    "category": "Git",
    "subCategory": "From your main checkout (on feature/new-api):",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "From your main checkout (on feature/new-api):",
      "L1",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Git does not track empty directories. If you create logs/ with nothing in it and run git add logs/, nothing happens — Git has nothing to ...",
    "question": "What is a `.gitkeep` file, and why do you sometimes see empty files with that name committed to repos?",
    "roleContext": "Git Scenario #32 (L1) · From your main checkout (on feature/new-api):",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Understanding that Git tracks content, not directories.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Git does <strong>not</strong> track empty directories. If you create <code>logs/</code> with nothing in it and run <code>git add logs/</code>, nothing happens — Git has nothing to track because the directory has no files.",
        "items": [
          "Ensuring a <code>tmp/</code>, <code>logs/</code>, or <code>uploads/</code> directory exists when someone clones the repo (the app expects the directory to exist at runtime).",
          "Skeleton project templates where the directory structure matters."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<code>.gitkeep</code> is a <strong>convention</strong> (not a Git feature) — it's an empty file placed inside an otherwise-empty directory so Git will track the directory. The name <code>.gitkeep</code> is arbitrary; you could use <code>.placeholder</code> or any filename. <code>.gitkeep</code> is just the community convention. Common use cases: If the directory should exist but its *contents* should be ignored (e.g., <code>logs/</code> should be present but log files shouldn't be committed), combine both: This ignores everything inside <code>logs/</code> except the <code>.gitkeep</code> file, preserving the directory. ---",
        "codeBlock": "# .gitignore\nlogs/*\n!logs/.gitkeep"
      }
    ],
    "keyTakeaway": "Pro-Tip: Ensuring a tmp/, logs/, or uploads/ directory exists when someone clones the repo (the app expects the directory to exist at runti.",
    "elevatorPitch": [
      "Ensuring a tmp/, logs/, or uploads/ directory exists when someone clones the repo (the app expect...",
      "Skeleton project templates where the directory structure matters."
    ]
  },
  {
    "id": "git-q033-you-accidentally-committed-a-500-mb",
    "title": "Git Q33: You accidentally committed a 500 MB video file three commits ago The file was deleted in a later commit but the repo is still huge Why and how do you actually remove it [L2]",
    "category": "Git",
    "subCategory": ".gitignore",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      ".gitignore",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Deleting a file in a new commit does not remove it from history. Git stores every version of every file as an immutable blob object. The ...",
    "question": "You accidentally committed a 500 MB video file three commits ago. The file was deleted in a later commit, but the repo is still huge. Why, and how do you actually remove it?",
    "roleContext": "Git Scenario #33 (L2) · .gitignore",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Understanding of Git's object model and history rewriting for large files.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Deleting a file in a new commit does not remove it from history. Git stores every version of every file as an immutable blob object. The 500 MB blob is still in the object database, reachable from the old commit. Every clone downloads it.",
        "items": [
          "Force-push all branches: <code>git push --force --all && git push --force --tags</code>.",
          "<strong>Every teammate must re-clone</strong> (or <code>git fetch origin && git reset --hard origin/main</code>). Their old local commits reference the rewritten SHAs and will cause confusion.",
          "GitHub/GitLab may still cache the old objects — contact support to trigger garbage collection on the server, or wait for the platform's scheduled GC."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>Finding the large objects:</strong> Or use <code>git-sizer</code> for a comprehensive report. <strong>Removing the file from all history:</strong> The modern tool is <code>git filter-repo</code> (replaces the deprecated <code>git filter-branch</code>): This rewrites every commit that ever contained <code>big-video.mp4</code>, removing the file entirely. All commit SHAs from that point onward change. <strong>After rewriting:</strong> <strong>Prevention:</strong> set up Git LFS for large binaries, and add a pre-receive hook or CI check that rejects files above a size threshold. ---",
        "codeBlock": "git rev-list --objects --all | \\\n  git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | \\\n  sed -n 's/^blob //p' | sort -rnk2 | head -20"
      }
    ],
    "keyTakeaway": "Pro-Tip: Force-push all branches: git push --force --all && git push --force --tags..",
    "elevatorPitch": [
      "Force-push all branches: git push --force --all && git push --force --tags.",
      "Every teammate must re-clone (or git fetch origin && git reset --hard origin/main). Their old loc...",
      "GitHub/GitLab may still cache the old objects — contact support to trigger garbage collection on ..."
    ]
  },
  {
    "id": "git-q034-you-need-to-deliver-a-git-repo-to-a",
    "title": "Git Q34: You need to deliver a Git repo to an air-gapped environment with no network How do you transfer commits [L3]",
    "category": "Git",
    "subCategory": ".gitignore",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Git",
      ".gitignore",
      "L3",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Use git bundle — it packages Git objects into a single file that can be copied via USB, email, or any file-transfer mechanism:",
    "question": "You need to deliver a Git repo to an air-gapped environment with no network. How do you transfer commits?",
    "roleContext": "Git Scenario #34 (L3) · .gitignore",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Knowledge of `git bundle` for offline transfer.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Use <code>git bundle</code> — it packages Git objects into a single file that can be copied via USB, email, or any file-transfer mechanism:",
        "items": [
          "Air-gapped/classified environments (government, defense, healthcare).",
          "Transferring repos between disconnected networks.",
          "Backups — a bundle is a self-contained snapshot of the entire repo."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>Creating a bundle (on the source machine):</strong> <strong>Using the bundle (on the air-gapped machine):</strong> <strong>How it works:</strong> A bundle is essentially a packfile with a header listing the refs it contains and the prerequisite commits it assumes the receiver already has. <code>git bundle verify</code> checks that the receiver has those prerequisites. <strong>Use cases:</strong> The incremental approach (<code>v1.5..main</code>) keeps bundles small by only including new commits. For regular transfers, maintain a marker tag at each delivery point. ---",
        "items": [
          "Bootstrapping repos on machines where installing Git hosting is impractical."
        ],
        "codeBlock": "# Bundle everything:\ngit bundle create repo.bundle --all\n\n# Bundle only new commits since a tag:\ngit bundle create update.bundle v1.5..main"
      }
    ],
    "keyTakeaway": "Pro-Tip: Air-gapped/classified environments (government, defense, healthcare)..",
    "elevatorPitch": [
      "Air-gapped/classified environments (government, defense, healthcare).",
      "Transferring repos between disconnected networks.",
      "Backups — a bundle is a self-contained snapshot of the entire repo."
    ]
  },
  {
    "id": "git-q035-explain-how-git-stores-data-interna",
    "title": "Git Q35: Explain how Git stores data internally What are blobs trees commits and tags at the object level [L3]",
    "category": "Git",
    "subCategory": "Pull updates from an incremental bundle:",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Git",
      "Pull updates from an incremental bundle:",
      "L3",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Git is fundamentally a content-addressable filesystem. Every piece of data is stored as an object identified by its SHA-1 (or SHA-256) ha...",
    "question": "Explain how Git stores data internally. What are blobs, trees, commits, and tags at the object level?",
    "roleContext": "Git Scenario #35 (L3) · Pull updates from an incremental bundle:",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: Deep understanding of Git's content-addressable object model.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Git is fundamentally a content-addressable filesystem. Every piece of data is stored as an <strong>object</strong> identified by its SHA-1 (or SHA-256) hash. There are four object types:",
        "items": [
          "<strong>Blob</strong> — stores file content (just the raw bytes, no filename or permissions). Two files with identical content share the same blob object, regardless of filename or location. This is how Git deduplicates storage.",
          "<strong>Tree</strong> — represents a directory. Contains entries mapping filenames → blob SHAs (for files) or other tree SHAs (for subdirectories), along with file mode (permissions). A tree is a snapshot of a directory at a point in time.",
          "<strong>Commit</strong> — points to one tree (the root tree of the project at that moment) and zero or more parent commits. Contains author, committer, timestamp, and message. The first commit has no parent; merge commits have two or more parents. The commit's SHA is a hash of all this — changing anything (message, author, parent, tree) produces a different SHA."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>How a commit represents a full snapshot:</strong> Each commit stores a <strong>complete snapshot</strong>, not a diff. Git computes diffs on the fly by comparing two commits' trees. This is why checkout is fast (just materialize one tree) and why branches are cheap (a branch is a 41-byte file containing a commit SHA). <strong>Packfiles:</strong> for efficiency, Git periodically packs loose objects into packfiles (<code>.pack</code> + <code>.idx</code>), using delta compression (storing diffs between similar blobs) to reduce disk usage. <code>git gc</code> triggers this. Packing is a storage optimization — the logical model is still immutable, content-addressed objects. --- ## 🟣 Diffing, Logging & Code Archaeology ---",
        "items": [
          "<strong>Tag (annotated)</strong> — points to a commit (or any object) and adds tagger identity, date, message, and optional signature."
        ],
        "codeBlock": "commit abc123\n  └── tree def456 (root directory)\n       ├── blob 111aaa  README.md\n       ├── tree 222bbb  src/\n       │    ├── blob 333ccc  main.py\n       │    └── blob 444ddd  utils.py\n       └── tree 555eee  tests/\n            └── blob 666fff  test_main.py"
      }
    ],
    "keyTakeaway": "Pro-Tip: Blob — stores file content (just the raw bytes, no filename or permissions). Two files with identical content share the same blob .",
    "elevatorPitch": [
      "Blob — stores file content (just the raw bytes, no filename or permissions). Two files with ident...",
      "Tree — represents a directory. Contains entries mapping filenames → blob SHAs (for files) or othe...",
      "Commit — points to one tree (the root tree of the project at that moment) and zero or more parent..."
    ]
  },
  {
    "id": "git-q036-whats-the-difference-between-git-di",
    "title": "Git Q36: Whats the difference between git diff git diff --staged and git diff HEAD When would you use each [L1]",
    "category": "Git",
    "subCategory": "Diffing, Logging & Code Archaeology",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "Diffing, Logging & Code Archaeology",
      "L1",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Git has three important snapshots: the last commit (HEAD), the staging area (index), and the working directory. Each diff variant compare...",
    "question": "What's the difference between `git diff`, `git diff --staged`, and `git diff HEAD`? When would you use each?",
    "roleContext": "Git Scenario #36 (L1) · Diffing, Logging & Code Archaeology",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Understanding of the three-tree architecture (working tree, index, HEAD).. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Git has three important snapshots: the last commit (<code>HEAD</code>), the staging area (index), and the working directory. Each <code>diff</code> variant compares two of these:",
        "items": [
          "<strong><code>git diff</code></strong> — working directory vs staging area. Shows changes you've made but haven't staged yet.",
          "<strong><code>git diff --staged</code></strong> (or <code>--cached</code>) — staging area vs <code>HEAD</code>. Shows what will go into the next commit if you run <code>git commit</code> right now.",
          "<strong><code>git diff HEAD</code></strong> — working directory vs <code>HEAD</code>. Shows *all* changes since the last commit, whether staged or not."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Other useful variations: For reviewing what you're about to commit, the workflow is: <code>git diff</code> to review unstaged changes → <code>git add -p</code> to selectively stage → <code>git diff --staged</code> to verify what's staged → <code>git commit</code>. ---",
        "codeBlock": "git diff main..feature     # diff between two branches\ngit diff HEAD~3..HEAD      # last 3 commits' changes\ngit diff --stat            # summary (files changed, insertions, deletions)\ngit diff --name-only       # just filenames\ngit diff -- path/to/file   # limit to specific file"
      }
    ],
    "keyTakeaway": "Pro-Tip: git diff — working directory vs staging area. Shows changes you've made but haven't staged yet..",
    "elevatorPitch": [
      "git diff — working directory vs staging area. Shows changes you've made but haven't staged yet.",
      "git diff --staged (or --cached) — staging area vs HEAD. Shows what will go into the next commit i...",
      "git diff HEAD — working directory vs HEAD. Shows *all* changes since the last commit, whether sta..."
    ]
  },
  {
    "id": "git-q037-how-do-you-view-the-commit-history-",
    "title": "Git Q37: How do you view the commit history effectively What are the most useful git log options [L1]",
    "category": "Git",
    "subCategory": "Diffing, Logging & Code Archaeology",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "Diffing, Logging & Code Archaeology",
      "L1",
      "Version Control",
      "Collaboration"
    ],
    "summary": "git log has many options to make history readable and filterable:",
    "question": "How do you view the commit history effectively? What are the most useful `git log` options?",
    "roleContext": "Git Scenario #37 (L1) · Diffing, Logging & Code Archaeology",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Practical familiarity with log filtering and formatting.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "<code>git log</code> has many options to make history readable and filterable: <strong>Formatting:</strong> <strong>Filtering:</strong> <strong>Most useful combination for daily work:</strong> This shows the entire branch topology in a compact view. Many developers alias this: For investigating a bug, <code>git log -S \"broken_function\"</code> (pickaxe search) is invaluable — it finds the commit that introduced or removed a specific string. ---",
        "codeBlock": "git log --oneline              # compact: SHA + message\ngit log --graph --oneline      # ASCII branch/merge visualization\ngit log --pretty=format:\"%h %an %ar %s\"  # custom format"
      }
    ],
    "keyTakeaway": "Pro-Tip: git log has many options to make history readable and filterable:.",
    "elevatorPitch": [
      "Immediate Triage: git log has many options to make history readable and filterable:",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "git-q038-you-want-to-generate-a-patch-file-f",
    "title": "Git Q38: You want to generate a patch file from your commits email it to a colleague and have them apply it to their repo How does the patch workflow work in Git [L2]",
    "category": "Git",
    "subCategory": "Diffing, Logging & Code Archaeology",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "Diffing, Logging & Code Archaeology",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Git has built-in support for email-style patch workflows (this is how the Linux kernel is developed):",
    "question": "You want to generate a patch file from your commits, email it to a colleague, and have them apply it to their repo. How does the patch workflow work in Git?",
    "roleContext": "Git Scenario #38 (L2) · Diffing, Logging & Code Archaeology",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Understanding of `git format-patch` and `git am` for email-based or offline collaboration.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Git has built-in support for email-style patch workflows (this is how the Linux kernel is developed):",
        "items": [
          "Contributing to projects that use mailing list workflows (Linux kernel, Git itself).",
          "Transferring changes when you can't push/PR (air-gapped, different hosting platforms).",
          "Code review via email."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>Creating patches:</strong> <code>format-patch</code> creates one <code>.patch</code> file per commit, containing the diff, commit message, author, and date in a format that preserves all metadata. <strong>Applying patches:</strong> <strong>Simpler alternative for one-off diffs:</strong> <code>git apply</code> is simpler but loses commit metadata. Use <code>format-patch</code> + <code>am</code> when you want full commit fidelity. <strong>Use cases:</strong> ---",
        "items": [
          "Backing up specific commits as portable files."
        ],
        "codeBlock": "# Patch for the last 3 commits:\ngit format-patch -3\n\n# Patches for commits on your branch not on main:\ngit format-patch main..HEAD\n\n# Single patch for all changes (combined):\ngit format-patch main..HEAD --stdout > all-changes.patch"
      }
    ],
    "keyTakeaway": "Pro-Tip: Contributing to projects that use mailing list workflows (Linux kernel, Git itself)..",
    "elevatorPitch": [
      "Contributing to projects that use mailing list workflows (Linux kernel, Git itself).",
      "Transferring changes when you can't push/PR (air-gapped, different hosting platforms).",
      "Code review via email."
    ]
  },
  {
    "id": "git-q039-two-developers-merge-different-imag",
    "title": "Git Q39: Two developers merge different image files (PNG) with the same filename Git reports a binary conflict How do you resolve it [L2]",
    "category": "Git",
    "subCategory": "resolve conflicts, then:",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "resolve conflicts, then:",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Git can't merge binary files — there's no line-by-line diff. When both branches modify the same binary file, Git marks it as conflicted a...",
    "question": "Two developers merge different image files (PNG) with the same filename. Git reports a binary conflict. How do you resolve it?",
    "roleContext": "Git Scenario #39 (L2) · resolve conflicts, then:",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: Handling binary file conflicts, custom merge drivers.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Git can't merge binary files — there's no line-by-line diff. When both branches modify the same binary file, Git marks it as conflicted and keeps both versions accessible:",
        "items": [
          "<strong>Git LFS</strong> with file locking — <code>git lfs lock assets/logo.png</code> prevents concurrent edits to binary files. Others see the file as locked and coordinate.",
          "<strong>Custom merge drivers</strong> in <code>.gitattributes</code> — you can define how specific file types are merged:"
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "After choosing, stage and commit: If you need to compare both versions before deciding: Open both files, decide which to keep (or combine them in an image editor), replace the conflicted file, and stage it. <strong>Prevention strategies:</strong> <code>merge=ours</code> automatically keeps the current branch's version for <code>.psd</code> files, avoiding conflicts entirely (with the tradeoff of silently ignoring the other side's changes). ---",
        "codeBlock": "git checkout --ours -- assets/logo.png    # keep your version\ngit checkout --theirs -- assets/logo.png  # keep their version"
      }
    ],
    "keyTakeaway": "Pro-Tip: Git LFS with file locking — git lfs lock assets/logo.png prevents concurrent edits to binary files. Others see the file as locked .",
    "elevatorPitch": [
      "Git LFS with file locking — git lfs lock assets/logo.png prevents concurrent edits to binary file...",
      "Custom merge drivers in .gitattributes — you can define how specific file types are merged:"
    ]
  },
  {
    "id": "git-q040-your-company-has-multiple-git-remot",
    "title": "Git Q40: Your company has multiple Git remotes — the primary on GitHub a mirror on GitLab for CI and a backup on an internal server How do you manage pushing to all of them [L2]",
    "category": "Git",
    "subCategory": "resolve conflicts, then:",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "resolve conflicts, then:",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Git supports multiple remotes natively. Set them up:",
    "question": "Your company has multiple Git remotes — the primary on GitHub, a mirror on GitLab for CI, and a backup on an internal server. How do you manage pushing to all of them?",
    "roleContext": "Git Scenario #40 (L2) · resolve conflicts, then:",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Multi-remote configuration and push strategies.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Git supports multiple remotes natively. Set them up: <strong>Option 1: Push to each explicitly:</strong> <strong>Option 2: Configure a push URL group</strong> — add multiple push URLs to a single remote: Now <code>git push origin main</code> pushes to all three simultaneously. <code>git pull</code> still fetches from the first (fetch) URL. <strong>Option 3: CI-driven mirroring</strong> — a post-push webhook or CI job on GitHub that mirrors to GitLab and the internal server. This is the most reliable approach for teams because it's centralized and auditable. <strong>Verify your setup:</strong> Shows fetch and push URLs for each remote. <strong>Best practice:</strong> designate one remote as the \"source of truth\" (usually <code>origin</code>). Mirrors should be read-only replicas populated by automation, not by developers pushing manually, to avoid divergence. ---",
        "codeBlock": "git remote add github https://github.com/org/repo.git\ngit remote add gitlab https://gitlab.com/org/repo.git\ngit remote add backup git@internal-server:org/repo.git"
      }
    ],
    "keyTakeaway": "Pro-Tip: Git supports multiple remotes natively. Set them up:.",
    "elevatorPitch": [
      "Immediate Triage: Git supports multiple remotes natively. Set them up:",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "git-q041-what-is-a-refspec-and-why-would-you",
    "title": "Git Q41: What is a refspec and why would you need to understand it [L2]",
    "category": "Git",
    "subCategory": "resolve conflicts, then:",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "resolve conflicts, then:",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": "A refspec defines the mapping between remote refs and local refs. It's the rule Git follows when fetching or pushing to know which remote...",
    "question": "What is a refspec, and why would you need to understand it?",
    "roleContext": "Git Scenario #41 (L2) · resolve conflicts, then:",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Understanding of the plumbing behind fetch/push and remote branch mapping.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "A refspec defines the mapping between remote refs and local refs. It's the rule Git follows when fetching or pushing to know *which* remote branch maps to *which* local branch.",
        "items": [
          "The <code>+</code> prefix means force-update (like <code>--force</code>).",
          "<code><src></code> is the source ref pattern.",
          "<code><dst></code> is the destination ref pattern.",
          "<strong>Fetch a specific branch only:</strong>"
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Format: <code>+<src>:<dst></code> <strong>Default fetch refspec</strong> (set by <code>git clone</code>): This means: \"take all branches on the remote (<code>refs/heads/*</code>) and store them locally as <code>refs/remotes/origin/*</code>.\" That's why <code>origin/main</code> exists locally after a fetch. <strong>Practical use cases:</strong> Most developers never write refspecs manually, but understanding them helps debug fetch/push issues and configure advanced workflows like PR testing or mirroring specific branches. ---",
        "items": [
          "<strong>Push to a differently-named remote branch:</strong>",
          "<strong>Delete a remote branch</strong> (push \"nothing\" to it):",
          "<strong>Fetch PR refs from GitHub</strong> (PRs aren't branches by default):"
        ],
        "codeBlock": "[remote \"origin\"]\n    fetch = +refs/heads/*:refs/remotes/origin/*"
      }
    ],
    "keyTakeaway": "Pro-Tip: The + prefix means force-update (like --force)..",
    "elevatorPitch": [
      "The + prefix means force-update (like --force).",
      " is the source ref pattern.",
      " is the destination ref pattern."
    ]
  },
  {
    "id": "git-q042-what-does-git-log-all-graph-oneline",
    "title": "Git Q42: What does git log --all --graph --oneline show and how do you read the output [L1]",
    "category": "Git",
    "subCategory": "resolve conflicts, then:",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "resolve conflicts, then:",
      "L1",
      "Version Control",
      "Collaboration"
    ],
    "summary": "This command shows a compact, visual representation of the entire commit history across all branches:",
    "question": "What does `git log --all --graph --oneline` show, and how do you read the output?",
    "roleContext": "Git Scenario #42 (L1) · resolve conflicts, then:",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Ability to visualize and interpret branch topology.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "This command shows a compact, visual representation of the entire commit history across all branches:",
        "items": [
          "**<code>*</code>** = a commit",
          "<strong><code>|</code></strong> = a branch line continuing vertically",
          "<strong><code>/</code> and <code>\\</code></strong> = branches diverging or converging",
          "<strong><code>|\\</code></strong> = a merge (two parents coming together)"
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "How to read it: This is the single most useful command for understanding \"what happened\" in a repo — which branches exist, where they diverged, and how they were merged. It replaces the need for a GUI in most cases. Add <code>--decorate</code> (usually default) to see branch/tag labels. Add <code>--date=short</code> and <code>--pretty=format:...</code> for more detail. ---",
        "items": [
          "<strong><code>|/</code></strong> = a branch that was merged in",
          "<strong>Text in <code>()</code></strong> = refs (branches, tags, HEAD) pointing to that commit",
          "<strong><code>HEAD -></code></strong> = which branch you're currently on"
        ],
        "codeBlock": "* e4f5g6h (HEAD -> feature/auth) Add JWT validation\n| * a1b2c3d (origin/main, main) Update README\n|/\n* 7h8i9j0 Merge pull request #42\n|\\\n| * k1l2m3n Fix login bug\n|/\n* o4p5q6r Initial commit"
      }
    ],
    "keyTakeaway": "Pro-Tip: ***** = a commit.",
    "elevatorPitch": [
      "***** = a commit",
      "| = a branch line continuing vertically",
      "/ and \\ = branches diverging or converging"
    ]
  },
  {
    "id": "git-q043-what-is-mailmap-and-when-would-you-",
    "title": "Git Q43: What is mailmap and when would you use it [L2]",
    "category": "Git",
    "subCategory": "resolve conflicts, then:",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "resolve conflicts, then:",
      "L2",
      "Version Control",
      "Collaboration"
    ],
    "summary": ".mailmap is a file that maps different author names and emails to a canonical identity. It fixes inconsistencies in git log, git shortlog...",
    "question": "What is `.mailmap` and when would you use it?",
    "roleContext": "Git Scenario #43 (L2) · resolve conflicts, then:",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: Awareness of author normalization in Git history.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "<code>.mailmap</code> is a file that maps different author names and emails to a canonical identity. It fixes inconsistencies in <code>git log</code>, <code>git shortlog</code>, and <code>git blame</code> without rewriting history.",
        "items": [
          "Open-source projects where contributors use different emails (work vs personal).",
          "Post-acquisition consolidation (merging email domains).",
          "Correcting typos in historical author names."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>The problem:</strong> over time, the same person commits with different identities: <strong>The solution — <code>.mailmap</code> in the repo root:</strong> Format: <code>Canonical Name <canonical@email> [Original Name] <original@email></code> After adding this file, <code>git shortlog -sne</code> and <code>git log --format='%aN'</code> consolidate all entries under the canonical identity. <code>git blame</code> also uses it. <strong>Use cases:</strong> The <code>.mailmap</code> file should be committed to the repo. It doesn't change any commit objects — it's a display-time mapping only. --- ## 🔶 Git Configuration & Optimization ---",
        "items": [
          "<code>git shortlog -sne</code> for accurate contribution statistics."
        ],
        "codeBlock": "Alice Smith <alice@company.com>\nAlice <alice@personal.com>\nA. Smith <asmith@oldcompany.com>"
      }
    ],
    "keyTakeaway": "Pro-Tip: Open-source projects where contributors use different emails (work vs personal)..",
    "elevatorPitch": [
      "Open-source projects where contributors use different emails (work vs personal).",
      "Post-acquisition consolidation (merging email domains).",
      "Correcting typos in historical author names."
    ]
  },
  {
    "id": "git-q044-how-do-you-set-up-useful-git-aliase",
    "title": "Git Q44: How do you set up useful Git aliases and what are some productivity aliases every developer should have [L1]",
    "category": "Git",
    "subCategory": "🔶 Git Configuration & Optimization",
    "level": "Junior / Associate DevOps [L1]",
    "badge": "Core Fundamentals [L1]",
    "tags": [
      "Git",
      "🔶 Git Configuration & Optimization",
      "L1",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "Git aliases are shortcuts defined in your Git config:",
    "question": "How do you set up useful Git aliases, and what are some productivity aliases every developer should have?",
    "roleContext": "Git Scenario #44 (L1) · 🔶 Git Configuration & Optimization",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Practical Git workflow optimization.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Git aliases are shortcuts defined in your Git config: Now <code>git co main</code> = <code>git checkout main</code>, etc. <strong>Power aliases:</strong> <strong>Shell command aliases</strong> (prefix with <code>!</code>):** This deletes all local branches already merged into <code>main</code>. Aliases live in <code>~/.gitconfig</code> under <code>[alias]</code>. They're portable — commit a shared alias config in your team's dotfiles repo for consistency. ---",
        "codeBlock": "git config --global alias.co checkout\ngit config --global alias.br branch\ngit config --global alias.ci commit\ngit config --global alias.st status"
      }
    ],
    "keyTakeaway": "Pro-Tip: Git aliases are shortcuts defined in your Git config:.",
    "elevatorPitch": [
      "Immediate Triage: Git aliases are shortcuts defined in your Git config:",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "git-q045-what-does-git-gc-do-and-when-should",
    "title": "Git Q45: What does git gc do and when should you run it manually [L2]",
    "category": "Git",
    "subCategory": "Amend without editing message",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "Amend without editing message",
      "L2",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "git gc (garbage collection) performs housekeeping on the Git object database:",
    "question": "What does `git gc` do, and when should you run it manually?",
    "roleContext": "Git Scenario #45 (L2) · Amend without editing message",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Understanding of Git's garbage collection and object packing.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "<code>git gc</code> (garbage collection) performs housekeeping on the Git object database:",
        "items": [
          "<strong>Packs loose objects</strong> into packfiles — individual objects in <code>.git/objects/</code> are compressed into efficient <code>.pack</code> files with delta compression.",
          "<strong>Removes unreachable objects</strong> — commits, blobs, and trees that no ref (branch, tag, reflog) points to. These are typically from amended commits, rebases, or deleted branches.",
          "<strong>Packs refs</strong> — consolidates individual ref files in <code>.git/refs/</code> into a single <code>packed-refs</code> file.",
          "<strong>Prunes old reflog entries</strong> — entries older than 90 days (reachable) or 30 days (unreachable) by default."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>When Git runs it automatically:</strong> Git triggers <code>gc --auto</code> after certain operations (e.g., when there are more than 6700 loose objects or more than 50 packfiles). You rarely need to run it manually. <strong>When to run manually:</strong> <strong>Aggressive GC:</strong> This spends more CPU time on delta compression for a smaller packfile. Only useful occasionally (e.g., after importing from another VCS). Don't run it routinely — the default compression is good enough and <code>--aggressive</code> is slow. <strong>Caution:</strong> <code>--prune=now</code> immediately deletes unreachable objects. If you're recovering lost commits via reflog, run recovery *before* GC. ---",
        "items": [
          "After large history rewrites (<code>filter-repo</code>, mass deletions) to reclaim disk space.",
          "When the repo feels slow and <code>.git/</code> is unusually large.",
          "Before creating a bundle or archive — ensures maximum compression."
        ],
        "codeBlock": "git gc --aggressive --prune=now"
      }
    ],
    "keyTakeaway": "Pro-Tip: Packs loose objects into packfiles — individual objects in .git/objects/ are compressed into efficient .pack files with delta comp.",
    "elevatorPitch": [
      "Packs loose objects into packfiles — individual objects in .git/objects/ are compressed into effi...",
      "Removes unreachable objects — commits, blobs, and trees that no ref (branch, tag, reflog) points ...",
      "Packs refs — consolidates individual ref files in .git/refs/ into a single packed-refs file."
    ]
  },
  {
    "id": "git-q046-what-is-git-fsck-and-when-would-you",
    "title": "Git Q46: What is git fsck and when would you use it [L2]",
    "category": "Git",
    "subCategory": "Amend without editing message",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Git",
      "Amend without editing message",
      "L2",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "git fsck (file system check) verifies the integrity of the Git object database — it walks every object and checks that nothing is corrupt...",
    "question": "What is `git fsck` and when would you use it?",
    "roleContext": "Git Scenario #46 (L2) · Amend without editing message",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Knowledge of Git's integrity checking and disaster recovery.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "<code>git fsck</code> (file system check) verifies the integrity of the Git object database — it walks every object and checks that nothing is corrupted, missing, or orphaned.",
        "items": [
          "Every object's SHA matches its content (detects corruption).",
          "Every commit's parent exists.",
          "Every tree's referenced blobs and sub-trees exist.",
          "No invalid object types or malformed headers.",
          "<strong><code>dangling commit</code></strong> — a commit not reachable from any branch or tag. Often from amended commits, rebases, or deleted branches. Harmless; <code>git gc</code> cleans them up.",
          "<strong><code>dangling blob</code></strong> — file content not referenced by any tree. Usually from staged-but-uncommitted files."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>What it checks:</strong> <strong>What it reports:</strong> <strong>When to use it:</strong> <strong>Recovery from corruption:</strong> if <code>fsck</code> reports missing objects, try re-fetching from the remote (<code>git fetch origin</code>). If the remote has the objects, they'll fill the gaps. For truly lost objects, restore from a backup or re-clone. ---",
        "items": [
          "<strong><code>missing object</code></strong> — an object referenced but not found. This is actual corruption.",
          "<strong><code>broken link</code></strong> — a tree or commit references an object that doesn't exist.",
          "<strong>After a disk failure or unclean shutdown</strong> — verify the repo isn't corrupted.",
          "<strong>Recovering lost commits</strong> — <code>git fsck --unreachable</code> lists orphaned commits you might want to rescue (alternative to <code>reflog</code>).",
          "<strong>Debugging weird Git errors</strong> — \"fatal: bad object\" errors often point to corruption that <code>fsck</code> can diagnose."
        ],
        "codeBlock": "git fsck --full"
      }
    ],
    "keyTakeaway": "Pro-Tip: Every object's SHA matches its content (detects corruption)..",
    "elevatorPitch": [
      "Every object's SHA matches its content (detects corruption).",
      "Every commit's parent exists.",
      "Every tree's referenced blobs and sub-trees exist."
    ]
  },
  {
    "id": "git-q047-your-team-debates-whether-to-use-sq",
    "title": "Git Q47: Your team debates whether to use squash-merge rebase-merge or regular merge commits when closing PRs What are the tradeoffs of each strategy [L3]",
    "category": "Git",
    "subCategory": "Amend without editing message",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Git",
      "Amend without editing message",
      "L3",
      "Version Control",
      "Collaboration",
      "Rebase"
    ],
    "summary": "Each strategy produces a different commit history shape with different tradeoffs:",
    "question": "Your team debates whether to use squash-merge, rebase-merge, or regular merge commits when closing PRs. What are the tradeoffs of each strategy?",
    "roleContext": "Git Scenario #47 (L3) · Amend without editing message",
    "intro": "\"During a major release branch cut, we encountered this exact scenario and used Git internals to recover cleanly. The interviewer is testing: Understanding merge strategies and their impact on history, bisect, and revert.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Each strategy produces a different commit history shape with different tradeoffs:",
        "items": [
          "<strong>Pros:</strong> preserves full branch history; you can see exactly what happened on the feature branch; easy to revert the entire PR with <code>git revert -m 1 <merge-sha></code>.",
          "<strong>Cons:</strong> history is noisy with \"fix typo\" and \"WIP\" commits; <code>git log</code> on <code>main</code> shows every commit from every branch; <code>git bisect</code> may land on broken intermediate commits.",
          "<strong>Pros:</strong> one clean commit per PR on <code>main</code>; <code>git log</code> is easy to read; each commit is a complete, reviewable unit; <code>git bisect</code> is highly effective.",
          "<strong>Cons:</strong> original branch history is lost (individual commits disappear); the squashed commit has a single author even if the PR had multiple contributors; if the PR is large, the single commit is hard to review or partially revert.",
          "<strong>Pros:</strong> linear history, no merge commits; each commit is preserved individually; clean <code>git log</code>."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>1. Merge commit (<code>--no-ff</code>):</strong> <strong>2. Squash merge:</strong> <strong>3. Rebase merge (fast-forward):</strong> <strong>Recommendation by team maturity:</strong> Many teams use squash for feature PRs and merge commits for release/hotfix merges. ---",
        "items": [
          "<strong>Cons:</strong> no visual grouping of \"this PR's commits\"; harder to revert an entire PR (must revert multiple commits); requires the feature branch to be rebased onto latest <code>main</code> before merging; commit SHAs change.",
          "<strong>Small team, disciplined commits:</strong> rebase-merge — clean, linear, each commit meaningful.",
          "<strong>Medium team, mixed commit quality:</strong> squash-merge — hides messy history, one commit = one PR.",
          "<strong>Large team, compliance needs:</strong> merge commits — full traceability, easy PR-level reverts, audit trail preserved."
        ],
        "codeBlock": "*   Merge PR #42: Add user auth\n|\\\n| * Fix test\n| * Add JWT middleware\n| * Add login endpoint\n|/\n* Previous main commit"
      }
    ],
    "keyTakeaway": "Pro-Tip: Pros: preserves full branch history; you can see exactly what happened on the feature branch; easy to revert the entire PR with gi.",
    "elevatorPitch": [
      "Pros: preserves full branch history; you can see exactly what happened on the feature branch; eas...",
      "Cons: history is noisy with \"fix typo\" and \"WIP\" commits; git log on main shows every commit from...",
      "Pros: one clean commit per PR on main; git log is easy to read; each commit is a complete, review..."
    ]
  },
  {
    "id": "git-q048-youre-setting-up-a-git-server-for-a",
    "title": "Git Q48: Youre setting up a Git server for an organization What are the differences between the four transfer protocols Git supports (Local HTTP SSH Git) and which would you choose [L3]",
    "category": "Git",
    "subCategory": "Amend without editing message",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Git",
      "Amend without editing message",
      "L3",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Git supports four protocols for communication between client and server:",
    "question": "You're setting up a Git server for an organization. What are the differences between the four transfer protocols Git supports (Local, HTTP, SSH, Git), and which would you choose?",
    "roleContext": "Git Scenario #48 (L3) · Amend without editing message",
    "intro": "\"Git is an immutable directed acyclic graph (DAG); knowing commands like git reflog means you never truly lose commits. The interviewer is testing: Understanding of Git transport protocols and security considerations.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Git supports four protocols for communication between client and server:",
        "items": [
          "Uses the filesystem directly. No network, no authentication beyond filesystem permissions.",
          "Fast, simple, but only works on the same machine or NFS mounts.",
          "Use case: shared server with SSH access where repos live on a mounted filesystem.",
          "Runs over standard HTTP(S) — passes through firewalls, proxies, and load balancers.",
          "Authentication via username/password, tokens, or SSO.",
          "Smart HTTP (default since Git 1.6.6) negotiates only needed objects — efficient as SSH.",
          "<strong>Recommended for most organizations</strong> — works everywhere, supports all auth methods, TLS for encryption.",
          "Encrypted, authenticated via SSH keys. No anonymous access possible."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>1. Local protocol (<code>file://</code> or just a path):</strong> <strong>2. HTTP/HTTPS (Smart HTTP):</strong> <strong>3. SSH:</strong> <strong>4. Git protocol (<code>git://</code>):</strong> <strong>For a new organization:</strong> Smart HTTPS with token-based authentication (PATs or OAuth via SSO). It works through corporate proxies, supports granular permissions via the hosting platform, and uses TLS. SSH as a secondary option for developers who prefer it. Never use the raw Git protocol. ---",
        "items": [
          "Well-understood security model; keys can be centrally managed (LDAP, SSO-issued certificates).",
          "No built-in authorization granularity — you either have shell access or you don't. Tools like Gitolite or platform features (GitHub, GitLab) add per-repo/per-branch authorization on top.",
          "<strong>Best for: internal teams</strong> where everyone has SSH keys, and you want simplicity without HTTP infrastructure.",
          "Unauthenticated, unencrypted, read-only by convention. Runs on port 9418.",
          "Fastest protocol (no encryption overhead), but no security.",
          "<strong>Almost never used today.</strong> Was useful for public read-only mirrors; HTTPS has replaced it."
        ],
        "codeBlock": "git clone /srv/git/repo.git"
      }
    ],
    "keyTakeaway": "Pro-Tip: Uses the filesystem directly. No network, no authentication beyond filesystem permissions..",
    "elevatorPitch": [
      "Uses the filesystem directly. No network, no authentication beyond filesystem permissions.",
      "Fast, simple, but only works on the same machine or NFS mounts.",
      "Use case: shared server with SSH access where repos live on a mounted filesystem."
    ]
  },
  {
    "id": "git-q049-your-monorepo-ci-is-slow-because-ev",
    "title": "Git Q49: Your monorepo CI is slow because every PR triggers tests for all 30 services How would you use Git to determine which services are affected by a PR and only run their tests [L3]",
    "category": "Git",
    "subCategory": "Amend without editing message",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Git",
      "Amend without editing message",
      "L3",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Use git diff to identify changed paths and map them to affected services:",
    "question": "Your monorepo CI is slow because every PR triggers tests for all 30 services. How would you use Git to determine which services are affected by a PR and only run their tests?",
    "roleContext": "Git Scenario #49 (L3) · Amend without editing message",
    "intro": "\"In a fast-paced team with dozens of pull requests merged daily, Git workflow hygiene was critical. The interviewer is testing: Using Git diff for selective CI, path-based triggering.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Use <code>git diff</code> to identify changed paths and map them to affected services:",
        "items": [
          "<strong>Bazel/Pants/Buck</strong> — build systems that understand the dependency graph and test only affected targets: <code>bazel test //... --test_tag_filters=-manual</code> with remote caching.",
          "<strong>Nx/Turborepo</strong> (JS/TS monorepos) — <code>nx affected --target=test</code> automatically determines what to test based on the Git diff.",
          "<strong>git diff with <code>--diff-filter</code></strong> — distinguish added, modified, deleted, and renamed files for more precise mapping."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>Step 1: Get changed files in the PR:</strong> The three-dot syntax (<code>...</code>) finds the merge-base and shows only what the PR changed — not what <code>main</code> changed since the branch was created. <strong>Step 2: Map changed paths to services:</strong> This mapping can be a simple bash script, a JSON config file, or defined in CI config: <strong>Step 3: Handle shared dependencies:</strong> The tricky part. If <code>libs/common/</code> changes, every service that depends on it must be tested. Maintain a dependency graph (or use build tools like Bazel, Nx, or Turborepo that understand it natively). <strong>Advanced approaches:</strong> The key insight: <code>git diff --name-only</code> gives you the raw data; the intelligence is in the mapping from file paths to services/test suites. ---",
        "codeBlock": "# Compare PR branch against the merge target:\nCHANGED_FILES=$(git diff --name-only origin/main...HEAD)"
      }
    ],
    "keyTakeaway": "Pro-Tip: Bazel/Pants/Buck — build systems that understand the dependency graph and test only affected targets: bazel test //... --test_tag_.",
    "elevatorPitch": [
      "Bazel/Pants/Buck — build systems that understand the dependency graph and test only affected targ...",
      "Nx/Turborepo (JS/TS monorepos) — nx affected --target=test automatically determines what to test ...",
      "git diff with --diff-filter — distinguish added, modified, deleted, and renamed files for more pr..."
    ]
  },
  {
    "id": "git-q050-a-developer-reports-that-git-push-i",
    "title": "Git Q50: A developer reports that git push is extremely slow (takes 5+ minutes) even for small commits The repo itself is only 500 MB How do you diagnose and fix this [L3]",
    "category": "Git",
    "subCategory": "Determine affected services based on changed paths",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Git",
      "Determine affected services based on changed paths",
      "L3",
      "Version Control",
      "Collaboration"
    ],
    "summary": "Slow pushes with small changesets typically aren't about bandwidth — the bottleneck is usually pack negotiation or server-side processing.",
    "question": "A developer reports that `git push` is extremely slow (takes 5+ minutes) even for small commits. The repo itself is only 500 MB. How do you diagnose and fix this?",
    "roleContext": "Git Scenario #50 (L3) · Determine affected services based on changed paths",
    "intro": "\"When an engineer accidentally creates this branch or commit divergence, I walk them through safe recovery without data loss. The interviewer is testing: Debugging Git performance, understanding pack negotiation, and server-side issues.. I structure my answer around systematic triage first, root cause analysis second, and permanent remediation third.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "Slow pushes with small changesets typically aren't about bandwidth — the bottleneck is usually pack negotiation or server-side processing.",
        "items": [
          "<strong>Enable Git tracing:</strong>",
          "<strong>Check where time is spent:</strong>",
          "<strong>\"Counting objects\" / \"Compressing objects\" takes minutes?</strong> — the local repo has too many loose objects or a bloated object database. Run <code>git gc</code> and <code>git repack -a -d --depth=250 --window=250</code>.",
          "<strong>Negotiation phase is slow?</strong> — Git exchanges \"have/want\" lists with the server. With many branches and tags, this can be large. Prune stale remote refs: <code>git remote prune origin</code>. Delete merged branches.",
          "<strong>Server-side hooks are slow?</strong> — pre-receive or update hooks (code scanning, large file checks, CI triggers) run synchronously. Check with the platform admin.",
          "<strong>Network latency?</strong> — <code>curl -o /dev/null -w \"time_connect: %{time_connect}\\ntime_total: %{time_total}\\n\" <git-server-url></code> to check.",
          "<strong>Check for large files in recent commits:</strong>"
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "<strong>Diagnosis:</strong> This shows every step: DNS resolution, TLS handshake, ref advertisement, pack negotiation, and upload. A single large blob forces a big packfile upload. <strong>Fixes:</strong> If the root cause is many stale refs, regularly run <code>git fetch --prune</code> and delete merged feature branches both locally and remotely. ---",
        "items": [
          "<strong>Run <code>git gc --aggressive</code></strong> if the local object store is fragmented.",
          "<strong>Enable <code>push.negotiate</code> (Git 2.36+):</strong> <code>git config push.negotiate true</code> — uses a more efficient negotiation algorithm.",
          "<strong>Use SSH instead of HTTPS</strong> if TLS overhead is contributing.",
          "<strong>Push only the branch you need:</strong> <code>git push origin main</code> instead of <code>git push --all</code>.",
          "<strong>Consider <code>--thin</code> (default)</strong> — verify it's not disabled. Thin packs send deltas against objects the server already has, reducing transfer size.",
          "<strong>Check for server-side quotas or throttling</strong> — some Git hosting providers rate-limit pushes or run expensive server-side operations."
        ],
        "codeBlock": "GIT_TRACE=1 GIT_TRANSFER_TRACE=1 GIT_CURL_VERBOSE=1 git push origin main"
      }
    ],
    "keyTakeaway": "Pro-Tip: Enable Git tracing:.",
    "elevatorPitch": [
      "Enable Git tracing:",
      "Check where time is spent:",
      "\"Counting objects\" / \"Compressing objects\" takes minutes? — the local repo has too many loose obj..."
    ]
  },
  {
    "id": "kubernetes-q050-explain-the-difference-between-kube",
    "title": "Kubernetes Q50: Explain the difference between kubectl apply and kubectl create When would you use each [L3]",
    "category": "Kubernetes",
    "subCategory": "Advanced Scenarios",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Kubernetes",
      "Advanced Scenarios",
      "L3",
      "Container Orchestration",
      "K8s",
      "Git"
    ],
    "summary": "- kubectl create — imperative. Creates the resource. Fails if it already exists. Good for one-time resource creation.",
    "question": "Explain the difference between `kubectl apply` and `kubectl create`. When would you use each?",
    "roleContext": "Kubernetes Scenario #50 (L3) · Advanced Scenarios",
    "intro": "\"When troubleshooting Kubernetes, I always follow a structured layered model: Pod status -> Events -> Logs -> Network. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "In CI/CD pipelines, always use <code>kubectl apply</code> — it's idempotent. Running the pipeline twice won't fail.",
        "items": [
          "<strong><code>kubectl create</code></strong> — imperative. Creates the resource. Fails if it already exists. Good for one-time resource creation.",
          "<strong><code>kubectl apply</code></strong> — declarative. Creates if not exists, updates if it does. Tracks changes using the <code>kubectl.kubernetes.io/last-applied-configuration</code> annotation. Good for GitOps and automation."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Use <code>kubectl create</code> when you specifically want the command to fail if the resource exists (e.g., to prevent accidental overwrites in a script). <code>kubectl apply</code> with <code>--server-side</code> (SSA) is the modern approach — the server handles merge strategy instead of the client annotation. ---"
      }
    ],
    "keyTakeaway": "Pro-Tip: kubectl create — imperative. Creates the resource. Fails if it already exists. Good for one-time resource creation..",
    "elevatorPitch": [
      "kubectl create — imperative. Creates the resource. Fails if it already exists. Good for one-time ...",
      "kubectl apply — declarative. Creates if not exists, updates if it does. Tracks changes using the ..."
    ]
  },
  {
    "id": "kubernetes-q057-you-need-to-run-a-pod-that-requires",
    "title": "Kubernetes Q57: You need to run a pod that requires access to the host network (like a network monitoring tool) How do you configure this [L3]",
    "category": "Kubernetes",
    "subCategory": "Advanced Scenarios",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Kubernetes",
      "Advanced Scenarios",
      "L3",
      "Container Orchestration",
      "K8s",
      "Git"
    ],
    "summary": "yaml",
    "question": "You need to run a pod that requires access to the host network (like a network monitoring tool). How do you configure this?",
    "roleContext": "Kubernetes Scenario #57 (L3) · Advanced Scenarios",
    "intro": "\"In our production Kubernetes clusters running microservices on EKS/AKS, this was a classic operational challenge. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "initial-triage-and-mechanics",
        "title": "Initial Diagnostics & Root Cause Analysis",
        "description": "<code>hostNetwork: true</code> makes the pod share the node's network namespace. It can bind to host ports and see all host network interfaces.",
        "items": [
          "The pod can sniff all traffic on the node.",
          "Port conflicts — if the pod binds port 80, it conflicts with anything else on port 80 on the host.",
          "Should only be used for legitimate infrastructure tools (network debuggers, CNI components)."
        ]
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "safe-remediation-and-fix",
        "title": "Remediation & Permanent Safeguards",
        "description": "Security implications: ---",
        "items": [
          "Block with PSA policy in production namespaces."
        ],
        "codeBlock": "spec:\n  hostNetwork: true\n  hostPID: true  # if also needs host PID namespace"
      }
    ],
    "keyTakeaway": "Pro-Tip: The pod can sniff all traffic on the node..",
    "elevatorPitch": [
      "The pod can sniff all traffic on the node.",
      "Port conflicts — if the pod binds port 80, it conflicts with anything else on port 80 on the host.",
      "Should only be used for legitimate infrastructure tools (network debuggers, CNI components)."
    ]
  },
  {
    "id": "kubernetes-q099-how-would-you-migrate-a-stateful-wo",
    "title": "Kubernetes Q99: How would you migrate a stateful workload from one Kubernetes cluster to another with minimal downtime [L3]",
    "category": "Kubernetes",
    "subCategory": "Advanced Scenarios",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Kubernetes",
      "Advanced Scenarios",
      "L3",
      "Container Orchestration",
      "K8s",
      "Git"
    ],
    "summary": "1) Snapshot PVC data (Velero). 2) Deploy workload in new cluster from same Git source. 3) Restore data snapshot to new cluster PVCs. 4) T...",
    "question": "How would you migrate a stateful workload from one Kubernetes cluster to another with minimal downtime?",
    "roleContext": "Kubernetes Scenario #99 (L3) · Advanced Scenarios",
    "intro": "\"In one of our high-traffic production clusters, our SRE team handled this incident using a standardized runbook. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "1) Snapshot PVC data (Velero). 2) Deploy workload in new cluster from same Git source. 3) Restore data snapshot to new cluster PVCs. 4) Test new cluster. 5) Switch DNS/load balancer to new cluster. 6) Decommission old cluster."
      }
    ],
    "keyTakeaway": "Pro-Tip: 1) Snapshot PVC data (Velero). 2) Deploy workload in new cluster from same Git source. 3) Restore data snapshot to new cluster PVC.",
    "elevatorPitch": [
      "Immediate Triage: 1) Snapshot PVC data (Velero). 2) Deploy workload in new cluster from same Git source. 3) Resto",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "kubernetes-q147-what-is-kubectl-apply-prune",
    "title": "Kubernetes Q147: What is kubectl apply --prune [L2]",
    "category": "Kubernetes",
    "subCategory": "Additional Kubernetes Scenarios (Q101-Q200)",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Kubernetes",
      "Additional Kubernetes Scenarios (Q101-Q200)",
      "L2",
      "Container Orchestration",
      "K8s",
      "Git"
    ],
    "summary": "When combined with a label selector, it deletes resources that were previously applied with kubectl apply but are no longer in the curren...",
    "question": "What is `kubectl apply --prune`?",
    "roleContext": "Kubernetes Scenario #147 (L2) · Additional Kubernetes Scenarios (Q101-Q200)",
    "intro": "\"In one of our high-traffic production clusters, our SRE team handled this incident using a standardized runbook. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "When combined with a label selector, it deletes resources that were previously applied with <code>kubectl apply</code> but are no longer in the current manifest set. Useful for GitOps without a full GitOps controller — cleans up old resources."
      }
    ],
    "keyTakeaway": "Pro-Tip: When combined with a label selector, it deletes resources that were previously applied with kubectl apply but are no longer in the.",
    "elevatorPitch": [
      "Immediate Triage: When combined with a label selector, it deletes resources that were previously applied with kub",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "terraform-q051-how-do-you-manage-terraform-infrast",
    "title": "Terraform Q51: How do you manage Terraform infrastructure across 50 AWS accounts in an AWS Organization [L3]",
    "category": "Terraform",
    "subCategory": "Use VPC ID from another module",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Terraform",
      "Use VPC ID from another module",
      "L3",
      "IaC",
      "Cloud Infrastructure",
      "Git"
    ],
    "summary": "Use a CI/CD system per account (GitHub Actions with OIDC, separate role per account). Shared modules in a central registry. Terragrunt or...",
    "question": "How do you manage Terraform infrastructure across 50 AWS accounts in an AWS Organization?",
    "roleContext": "Terraform Scenario #51 (L3) · Use VPC ID from another module",
    "intro": "\"In our enterprise Terraform repository, we designed reusable modules and remote backends to prevent this exact issue. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Use a CI/CD system per account (GitHub Actions with OIDC, separate role per account). Shared modules in a central registry. Terragrunt or Terraform Cloud for orchestration. Account vending machine (Control Tower) creates new accounts pre-wired for Terraform."
      }
    ],
    "keyTakeaway": "Pro-Tip: Use a CI/CD system per account (GitHub Actions with OIDC, separate role per account). Shared modules in a central registry. Terrag.",
    "elevatorPitch": [
      "Immediate Triage: Use a CI/CD system per account (GitHub Actions with OIDC, separate role per account). Shared mo",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "terraform-q063-a-developer-accidentally-committed-",
    "title": "Terraform Q63: A developer accidentally committed terraformtfvars with production values including secrets What should you do [L2]",
    "category": "Terraform",
    "subCategory": "Use VPC ID from another module",
    "level": "Senior DevOps / SRE [L2]",
    "badge": "Production Scenario [L2]",
    "tags": [
      "Terraform",
      "Use VPC ID from another module",
      "L2",
      "IaC",
      "Cloud Infrastructure",
      "Git"
    ],
    "summary": "Remove the sensitive file from Git tracking, rotate every exposed secret, and replace the workflow with a safer input method such as CI v...",
    "question": "A developer accidentally committed `terraform.tfvars` with production values, including secrets. What should you do?",
    "roleContext": "Terraform Scenario #63 (L2) · Use VPC ID from another module",
    "intro": "\"In our enterprise Terraform repository, we designed reusable modules and remote backends to prevent this exact issue. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Remove the sensitive file from Git tracking, rotate every exposed secret, and replace the workflow with a safer input method such as CI variables, Vault, AWS Secrets Manager, or environment variables. Add <code>.gitignore</code> rules so local tfvars files are not committed, and review whether the state file also contains those secrets because state storage needs the same level of protection."
      }
    ],
    "keyTakeaway": "Pro-Tip: Remove the sensitive file from Git tracking, rotate every exposed secret, and replace the workflow with a safer input method such .",
    "elevatorPitch": [
      "Immediate Triage: Remove the sensitive file from Git tracking, rotate every exposed secret, and replace the workf",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "terraform-q075-how-do-you-use-terraform-in-a-regul",
    "title": "Terraform Q75: How do you use Terraform in a regulated environment where every infrastructure change needs an auditable approval trail [L3]",
    "category": "Terraform",
    "subCategory": "Use VPC ID from another module",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Terraform",
      "Use VPC ID from another module",
      "L3",
      "IaC",
      "Cloud Infrastructure",
      "Git"
    ],
    "summary": "Run Terraform through CI/CD only, store plans as build artifacts, require pull request review plus manual approval before apply, and keep...",
    "question": "How do you use Terraform in a regulated environment where every infrastructure change needs an auditable approval trail?",
    "roleContext": "Terraform Scenario #75 (L3) · Use VPC ID from another module",
    "intro": "\"In our enterprise Terraform repository, we designed reusable modules and remote backends to prevent this exact issue. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Run Terraform through CI/CD only, store plans as build artifacts, require pull request review plus manual approval before <code>apply</code>, and keep remote state with version history. Terraform Cloud, GitHub Actions, or similar systems can provide plan/apply logs tied to user identities. The key point is that the approved plan and the applied plan must match, so avoid re-planning between approval and apply."
      }
    ],
    "keyTakeaway": "Pro-Tip: Run Terraform through CI/CD only, store plans as build artifacts, require pull request review plus manual approval before apply, a.",
    "elevatorPitch": [
      "Immediate Triage: Run Terraform through CI/CD only, store plans as build artifacts, require pull request review p",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "terraform-q088-your-remote-module-source-points-to",
    "title": "Terraform Q88: Your remote module source points to a Git branch and a new commit on that branch changed production plans unexpectedly How do you prevent this [L3]",
    "category": "Terraform",
    "subCategory": "Use VPC ID from another module",
    "level": "Staff SRE / Principal Architect [L3]",
    "badge": "Staff SRE Scenario [L3]",
    "tags": [
      "Terraform",
      "Use VPC ID from another module",
      "L3",
      "IaC",
      "Cloud Infrastructure",
      "Git"
    ],
    "summary": "Pin module sources to immutable versions such as tags or commit SHAs. Use a release process for shared modules, test the new version in n...",
    "question": "Your remote module source points to a Git branch, and a new commit on that branch changed production plans unexpectedly. How do you prevent this?",
    "roleContext": "Terraform Scenario #88 (L3) · Use VPC ID from another module",
    "intro": "\"Treat Terraform code with the same rigor as application code: pre-merge plans, state locks, and automated drift detection. When addressing this question, I walk the interviewer through our production incident runbook: isolating the blast radius, checking diagnostic logs and metrics, and applying a safe fix.\"",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "model-solution",
        "title": "Production Solution & Architecture",
        "description": "Pin module sources to immutable versions such as tags or commit SHAs. Use a release process for shared modules, test the new version in non-production first, and update module references intentionally. Branch-based module sources are convenient during development, but they make production infrastructure depend on whatever code happens to be at the branch head."
      }
    ],
    "keyTakeaway": "Pro-Tip: Pin module sources to immutable versions such as tags or commit SHAs. Use a release process for shared modules, test the new versi.",
    "elevatorPitch": [
      "Immediate Triage: Pin module sources to immutable versions such as tags or commit SHAs. Use a release process for",
      "Run targeted verification commands before modifying configuration.",
      "Automate permanent guardrails (CI check, alerts, IaC policy) to prevent recurrence."
    ]
  },
  {
    "id": "gha-manage-large-workflow-files-efficiently",
    "title": "Managing Large, Monolithic GitHub Actions Workflow Files Efficiently",
    "category": "CI/CD",
    "subCategory": "GitHub Actions",
    "level": "Senior DevOps / SRE",
    "badge": "CI/CD Architecture",
    "tags": [
      "CI/CD",
      "GitHub Actions",
      "Refactoring",
      "Reusable Workflows",
      "Composite Actions",
      "Actionlint"
    ],
    "summary": "How to refactor and modularize complex 900+ line GitHub Actions workflows: decomposing into reusable workflows and composite actions, utilizing build matrices, and enforcing automated linting with actionlint.",
    "question": "How do you manage and maintain large GitHub Actions workflow files efficiently?",
    "roleContext": "Release Engineering & CI/CD Pipeline Maintenance",
    "intro": "I reduce workflow complexity by splitting monolithic workflows into reusable workflows (workflow_call) and composite actions, using matrix strategies for repetitive build tasks, and externalizing environment-specific configuration into GitHub variables and secrets. I also enforce standardized naming, clear comments for non-obvious conditional logic, and automated CI linting using actionlint.",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "modularization-and-composite-actions",
        "title": "Decomposing Monoliths into Reusable Workflows & Composite Actions",
        "description": "Transform 1000-line single YAML files into modular, maintainable building blocks:",
        "items": [
          "<strong>Composite Actions for Step Bundles:</strong> Group repetitive boilerplate steps (e.g. checkout, cache setup, environment configuration) into local composite actions (<code>./.github/actions/setup-build-env</code>).",
          "<strong>Reusable Workflows for Pipelines:</strong> Extract discrete lifecycle stages (build, security-scan, deploy-staging, deploy-prod) into separate reusable workflow files with explicit inputs and outputs.",
          "<strong>Matrix Execution:</strong> Replace copy-pasted jobs for multiple services with a dynamic matrix strategy."
        ],
        "codeBlock": "# Matrix strategy to eliminate repeated job definitions\njobs:\n  build-services:\n    runs-on: ubuntu-latest\n    strategy:\n      matrix:\n        service: [api, worker, payment, notification]\n    steps:\n      - uses: actions/checkout@v4\n      - name: Setup environment\n        uses: ./.github/actions/setup-build-env\n      - name: Build service\n        run: make build SERVICE=${{ matrix.service }}"
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "actionlint-and-ci-governance",
        "title": "Static Workflow Linting with Actionlint & CI Governance",
        "description": "Prevent YAML syntax errors, missing inputs, and unescaped script expressions before PRs merge:",
        "items": [
          "<strong>Actionlint in Pre-Commit & CI:</strong> Run <code>actionlint</code> to check GitHub Actions expressions (<code>${{ }}</code>), shell scripts inside <code>run</code> steps, and event webhook schemas.",
          "<strong>Central Orchestrator Workflow:</strong> Maintain a slim, high-level caller workflow that orchestrates dependencies between jobs using <code>needs: [build, security-scan]</code>.",
          "<strong>Documentation & Granular Permissions:</strong> Enforce job-level <code>permissions</code> blocks and add markdown documentation in <code>.github/workflows/README.md</code>."
        ],
        "codeBlock": "# Install and run actionlint locally and in CI\nnpm i -g actionlint\nactionlint\n\n# Run via Docker without local installation\ndocker run --rm -v $(pwd):/repo --workdir /repo rhysd/actionlint:latest"
      }
    ],
    "keyTakeaway": "Break 900+ line monolithic workflows into composite actions for steps and reusable workflows for stages. Use matrix strategies for multi-service builds and enforce automated linting with actionlint.",
    "elevatorPitch": [
      "Decompose large workflows into local composite actions for repeated steps and reusable workflows for major pipeline stages.",
      "Use matrix strategies to consolidate duplicate service build jobs into a single clean configuration.",
      "Lint all workflow files automatically with actionlint in CI to prevent syntax and expression errors before deployment."
    ]
  },
  {
    "id": "gha-public-vs-private-workflow-repositories",
    "title": "Public vs Private Workflow Repositories in GitHub Actions: Security & Access Architecture",
    "category": "CI/CD",
    "subCategory": "GitHub Actions",
    "level": "Senior DevOps / SRE",
    "badge": "Technical Deep-Dive",
    "tags": [
      "CI/CD",
      "GitHub Actions",
      "Security",
      "Reusable Workflows",
      "Secrets Management",
      "Access Control"
    ],
    "summary": "Architectural comparison and security trade-offs between public and private GitHub Actions workflow repositories: internal sharing policies, caller permissions, secret inheritance risks, and organization-level repository access.",
    "question": "What is the difference between public and private workflow repositories in GitHub Actions, and how do you secure them?",
    "roleContext": "DevSecOps & CI/CD Security Governance",
    "intro": "Public workflow repositories can be referenced across any repository on GitHub but must never contain sensitive defaults, internal URLs, or privileged assumptions. Private workflow repositories are restricted to authorized organization members or configured repositories, making them ideal for proprietary deployment pipelines. In both cases, secret access is controlled by the caller workflow and organization policy rather than the reusable workflow file alone.",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "architectural-differences-and-access",
        "title": "Architectural Differences & Organization Access Settings",
        "description": "Understand how GitHub evaluates access permissions between caller workflows and workflow repositories:",
        "items": [
          "<strong>Public Workflow Repos:</strong> Accessible to anyone across GitHub. Anyone can consume the workflow by referencing <code>org/repo/.github/workflows/file.yml@v1</code>. Useful for open-source linting and testing actions.",
          "<strong>Private Workflow Repos:</strong> Access is restricted by GitHub organization settings (Repository Settings -> Actions -> Access -> 'Accessible from repositories in this organization').",
          "<strong>Internal Repositories:</strong> In GitHub Enterprise, 'internal' visibility allows all enterprise members to consume shared workflows without making them publicly accessible."
        ],
        "codeBlock": "# Referencing public shared workflows\nuses: org/public-workflows/.github/workflows/lint.yml@v1\n\n# Referencing private/internal enterprise workflows\nuses: org/platform-workflows/.github/workflows/deploy.yml@v3\n\n# Inspect repository visibility via GitHub CLI\ngh repo view org/platform-workflows --json name,visibility,defaultBranchRef"
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "security-and-secret-hardening",
        "title": "Secret Inheritance & Supply Chain Security Hardening",
        "description": "Guard against credential leaks and untrusted workflow modification:",
        "items": [
          "<strong>Secret Management:</strong> Reusable workflows cannot access caller secrets unless explicitly passed via <code>secrets:</code> or inherited via <code>secrets: inherit</code>.",
          "<strong>Never Hardcode Secrets or Internal Endpoints:</strong> Public reusable workflows must accept all endpoints, tokens, and configs strictly via typed <code>inputs:</code>.",
          "<strong>Tag & Commit Pinning:</strong> Pin reusable workflows to immutable full commit SHAs (e.g., <code>@a1b2c3d...</code>) in production pipelines rather than mutable branch names to protect against supply-chain tampering."
        ],
        "codeBlock": "# Secure invocation of reusable workflow with explicit secrets\njobs:\n  deploy:\n    uses: org/platform-workflows/.github/workflows/deploy.yml@v3\n    with:\n      environment: 'production'\n    secrets:\n      AWS_ROLE_ARN: ${{ secrets.PROD_AWS_ROLE_ARN }}"
      }
    ],
    "keyTakeaway": "Public repos are for open, generic utility actions and must never contain internal assumptions. Private/internal repos host proprietary release logic with organization-level access controls and explicit secret passing.",
    "elevatorPitch": [
      "Public workflow repos provide global access for generic tasks but require strict input sanitization and zero internal assumptions.",
      "Private and internal workflow repos are restricted to organization members for sensitive deployment logic.",
      "Control secret access strictly via explicit caller bindings or secrets: inherit, and pin workflows to commit SHAs for supply chain security."
    ]
  },
  {
    "id": "gha-implement-workflow-concurrency-controls",
    "title": "Implementing Workflow Concurrency in GitHub Actions to Prevent Race Conditions",
    "category": "CI/CD",
    "subCategory": "GitHub Actions",
    "level": "Senior DevOps / SRE",
    "badge": "CI/CD Architecture",
    "tags": [
      "CI/CD",
      "GitHub Actions",
      "Concurrency",
      "Race Conditions",
      "Cost Optimization",
      "Deployment Safety"
    ],
    "summary": "How to configure GitHub Actions concurrency groups: cancelling redundant pull-request builds with cancel-in-progress to save runner minutes, while serializing production deployments to eliminate conflicting releases.",
    "question": "How do you implement workflow concurrency in GitHub Actions, and when should you cancel in-progress runs?",
    "roleContext": "Release Engineering & CI/CD Optimization",
    "intro": "I use the concurrency key to prevent duplicate, overlapping runs for the same branch, environment, or deployment target. For pull request CI builds, I set cancel-in-progress: true to terminate stale runs when developers push new commits, conserving runner minutes. For production deployments, I serialize runs using a static environment concurrency group and set cancel-in-progress: false to ensure ongoing releases finish safely without race conditions.",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "pr-branch-concurrency",
        "title": "Branch-Level CI Concurrency & Auto-Cancellation",
        "description": "Save CI runner minutes and reduce queue congestion during rapid pull request iterations:",
        "items": [
          "<strong>Dynamic Concurrency Group:</strong> Combine the workflow name and git branch reference (<code>github.workflow</code> and <code>github.ref</code>) to scope concurrency per PR.",
          "<strong>Cancel In-Progress:</strong> Setting <code>cancel-in-progress: true</code> automatically aborts the currently running job as soon as a new push occurs on the same branch.",
          "<strong>Cost Optimization:</strong> Eliminates runner waste by not testing obsolete commits that have already been superseded."
        ],
        "codeBlock": "# Branch-level CI concurrency configuration\nname: CI Pipeline\non:\n  pull_request:\n    branches: [main]\n\nconcurrency:\n  group: ci-${{ github.workflow }}-${{ github.ref }}\n  cancel-in-progress: true"
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "production-deployment-serialization",
        "title": "Production Deployment Serialization & Safety",
        "description": "Prevent simultaneous deployments from conflicting over state locks, database migrations, or cloud resources:",
        "items": [
          "<strong>Static Environment Group:</strong> Group deployments by target environment name (e.g. <code>deploy-production-api</code>).",
          "<strong>Queue Without Aborting:</strong> Set <code>cancel-in-progress: false</code> so that if two merges happen back-to-back, the second deployment waits in queue until the first completes successfully.",
          "<strong>Job-Level Concurrency:</strong> Apply concurrency at the individual job level rather than the entire workflow if only the deploy phase requires mutual exclusion."
        ],
        "codeBlock": "# Environment-level deployment concurrency (Mutual Exclusion)\nname: Deploy to Production\non:\n  push:\n    branches: [main]\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    concurrency:\n      group: deploy-production-api\n      cancel-in-progress: false\n    steps:\n      - uses: actions/checkout@v4\n      - name: Deploy application\n        run: ./deploy.sh production"
      }
    ],
    "keyTakeaway": "Use cancel-in-progress: true for pull requests to cancel obsolete builds and save runner costs. Use cancel-in-progress: false with static environment groups for production to serialize releases safely.",
    "elevatorPitch": [
      "Apply concurrency groups using github.workflow and github.ref to isolate concurrency scopes.",
      "Cancel superseded pull-request builds with cancel-in-progress: true to optimize runner utilization and reduce queue times.",
      "Serialize production deployments with cancel-in-progress: false to prevent race conditions and conflicting releases."
    ]
  },
  {
    "id": "gha-troubleshoot-and-handle-failed-workflows",
    "title": "Troubleshooting & Handling Failed GitHub Actions Workflows at Scale",
    "category": "CI/CD",
    "subCategory": "GitHub Actions",
    "level": "Senior DevOps / SRE",
    "badge": "Production Incident",
    "tags": [
      "CI/CD",
      "GitHub Actions",
      "Troubleshooting",
      "gh CLI",
      "Debugging",
      "Retries",
      "Resilience"
    ],
    "summary": "Systematic workflow failure triage: categorizing failures into code, transient infrastructure, or pipeline design defects, using the GitHub CLI (gh) for rapid inspection, re-running failed jobs, and implementing automated retries.",
    "question": "How do you troubleshoot, triage, and handle failed workflows in GitHub Actions?",
    "roleContext": "Senior DevOps & CI/CD Pipeline Reliability Triage",
    "intro": "I treat workflow failures as either code defects, transient infrastructure/external dependency issues, or pipeline design bugs. First I inspect the failed job logs and artifacts using the gh CLI, rerun failed jobs for transient network issues, and correlate with external provider outages. Then I fix the root cause and implement guardrails like step retries, cache optimizations, debug logging, or quarantining flaky tests.",
    "steps": [
      {
        "stepNumber": "1️⃣",
        "stepId": "gh-cli-triage-and-logs",
        "title": "Rapid Triage Using the GitHub CLI (gh)",
        "description": "Diagnose workflow failures quickly without clicking through multiple web UI menus:",
        "items": [
          "<strong>Inspect Failed Logs:</strong> Use <code>gh run view &lt;run-id&gt; --log-failed</code> to output only the exact failing step lines.",
          "<strong>Download Artifacts:</strong> Retrieve test reports, screenshots, and logs using <code>gh run download</code>.",
          "<strong>Rerun Only Failed Jobs:</strong> Avoid re-executing successful 30-minute test jobs by using <code>gh run rerun --failed</code> to retry only the affected component.",
          "<strong>Debug Logging:</strong> Enable runner and step diagnostic logging by setting repository secrets <code>ACTIONS_STEP_DEBUG=true</code> and <code>ACTIONS_RUNNER_DEBUG=true</code>."
        ],
        "codeBlock": "# List recent pipeline runs\ngh run list --limit 20\n\n# View only the failed log output directly in terminal\ngh run view 12345678 --log-failed\n\n# Download diagnostic artifacts\ngh run download 12345678 --dir ./artifacts/\n\n# Re-run only the failed jobs in the workflow\ngh run rerun 12345678 --failed"
      },
      {
        "stepNumber": "2️⃣",
        "stepId": "remediation-and-resilience",
        "title": "Implementing Pipeline Resilience & Automated Retries",
        "description": "Prevent recurring transient failures from blocking development velocity:",
        "items": [
          "<strong>Step-Level Retries:</strong> Use retry actions (such as <code>nick-fields/retry</code>) for network-sensitive operations like Docker image pushes or registry downloads.",
          "<strong>API Rate Limit Mitigation:</strong> Authenticate all external API calls (e.g. GitHub API, Docker Hub) using personal or bot tokens to avoid anonymous IP rate limits.",
          "<strong>Flaky Test Isolation:</strong> Quarantine flaky end-to-end tests into a non-blocking job or rerun failed tests once before failing the entire pipeline.",
          "<strong>Notification Webhooks:</strong> Configure Slack / PagerDuty alert webhooks triggered only on <code>if: failure()</code> to notify on-call engineers immediately."
        ],
        "codeBlock": "# Example automated retry for network-sensitive step\n- name: Push container image with exponential retry\n  uses: nick-fields/retry@v3\n  with:\n    timeout_minutes: 5\n    max_attempts: 3\n    retry_wait_seconds: 10\n    command: docker push ghcr.io/org/api:${{ github.sha }}"
      }
    ],
    "keyTakeaway": "Categorize failures into code vs transient vs configuration bugs. Use the gh CLI for fast log extraction and rerun-failed operations, and add automated step retries for network-sensitive registry and package downloads.",
    "elevatorPitch": [
      "Diagnose pipeline failures rapidly with gh run view --log-failed and re-run only failed jobs.",
      "Enable ACTIONS_STEP_DEBUG and ACTIONS_RUNNER_DEBUG to uncover hidden runner and network issues.",
      "Harden pipelines against transient failures with exponential retries and authenticated registry access."
    ]
  }
]